# דוח שלב 8 — תמחור מקצועי, מחירי קנייה ורווחיות

פרויקט Supabase: `qxdpsomelzpvphkhkqrw` (Recipe Notebook, eu-central-1)
מיגרציות חדשות: `0013`, `0014`, `0015`, `0016`, `0017` — **כולן הוחלו בפועל**
ענף: `claude/recipe-notebook-initial-review-fwfb2e`

`git status design_handoff_recipe_notebook/` — **ריק**. הפרוטוטייפ לא נגע.
`git status packages/engine/src` — **ריק**. **לא נדרש שינוי מנוע בשלב הזה.**

---

## 0. הסריקה, ומה היא מצאה

| מה | מצב לפני שלב 8 | מה נעשה |
|---|---|---|
| `ingredient_catalog` | קיימת עם `purchase_unit`, `package_qty`, `package_price` ועמודות `price`/`price_unit` **מחושבות** | הורחבה: הרכישה כפי שנעשתה. **לא נבנתה מחדש** |
| מחיר מרכזי + snapshot היסטורי | קיים ומוכח (שלב 7) | **לא שונה**. נבדק שוב מול Postgres — §6 |
| `compute()` / `calcState` / full-partial-none | קיימים, כולל `ComputedRow.priced` משלב 7 | **הספיקו כפי שהם.** אין שינוי מנוע |
| שרשרת הצפיפויות §5.1 | מתכון → כיול → טבלה | **לא נגעתי**, לפי ההחלטה בסעיף J |
| אלרגנים | כפי שמומש בשלב 7 | לא שונה |
| `recipes.sale_price`, `target_fc` | קיימים | `sale_price` **לא נשמר בפועל** — ראו §2.1 |

---

## 1. סכימה — מה נוסף, ולמה

### 1.1 הרכישה כפי שהיא נעשתה (דרישה A)

`package_price` (מחיר אריזה **אחת**) → `purchase_total` (מה ששולם בסך הכול),
בתוספת `package_count`. הסיבה פשוטה: אף אחד לא מחלק ₪72 בשש אריזות של 500
גרם בראש, ליד הדלפק. מי שנאלץ לחשב — יחשב לא נכון.

| מה שהוזן | מה שהמסד גזר |
|---|---|
| 1 × 5 ק"ג ב-₪200 | ₪40 לק"ג · ₪4 ל-100 גר' · ₪0.04 לגרם |
| 6 × 500 גרם ב-**₪72 סה"כ** | 3 ק"ג → ₪24 לק"ג |
| 1 × 30 יחידות ב-₪39 | ₪1.30 ליחידה |
| 12 × 1 ליטר ב-₪180 | ₪15 לליטר |

כל השורות האלה נבדקו **מול Postgres החי**, לא מול קוד לקוח — §5, בדיקות 1-6.

### 1.2 מקור אמת אחד (דרישה B)

העובדות הן `purchase_total`, `package_count`, `package_qty`, `purchase_unit`.
**כל** מחיר ליחידה הוא `GENERATED STORED`:

- `purchase_price` — ₪ ליחידת בסיס **כפי שנקנה** (מוצג להשוואה)
- `price` — ₪ ליחידת בסיס **שמיש**, וזה מה שהמנוע קורא
- `price_unit` — `ק"ג` / `ליטר` / `יח'`

לקוח **לא יכול** לכתוב אותן: המסד דוחה ניסיון כזה (§5, בדיקה 37). ₪/ק"ג,
₪/100 גר' ו-₪/גרם הם אותו מספר בשלושה קני מידה, נגזרים ב-UI ולא נשמרים —
ולכן לא יכולים להיפרד זה מזה.

**המרות שלא נעשות:** אין המרת ליטר לק"ג בלי צפיפות אמינה, ואין המרה ל-100
יחידות. "₪0.13 ל-100 ביצים" אינו ביצה קטנה יותר, אלא שטות. `conversionsOf`
מחזיר קנה מידה אחד בלבד למחיר לפי יחידה.

### 1.3 היסטוריית רכישות (דרישה C)

טבלה חדשה `ingredient_purchases`, **append-only**, עם RLS של שורות המשתמש
בלבד ובלי חריג לשורות מערכת — מחיר קנייה וספק הם נתון עסקי פרטי, ואין דבר
כזה "ספק משותף".

- `record_purchase()` מוסיפה לוג **ומעדכנת את המחיר הפעיל בטרנזקציה אחת**,
  כך שאין מצב שבו ההיסטוריה והמחיר בתוקף חולקים.
- **המחיר הפעיל חד-משמעי**: הוא השורה ב-`ingredient_catalog`. הלוג הוא
  היסטוריה ואינו יכול להיחשב למחיר.
- הטבלה ממופתחת ב-`key` ולא ב-id של הקטלוג, כדי שרכישה תשרוד שינוי שם או
  יצירה מחדש של חומר הגלם.
- `purchase_history()` מחזירה מהחדש לישן עם `prev_price` (דרך `lag`) ו-
  `pct_change`. אין אחוז כשאין מחיר קודם, ואין אחוז כשהמחיר הקודם היה 0 —
  חילוק באפס אינו אחוז, ואחוז מומצא גרוע מכלום.
- **עריכה שאינה רכישה אינה נרשמת כרכישה.** תיקון שם או הוספת אלרגן עוברים
  ב-`saveCatalogItem` ולא מוסיפים שורה להיסטוריה (`purchaseChanged`).

### 1.4 ניצולת ופחת (דרישה D)

`usable_pct`, **אופציונלי**:

| | עלות קנייה | עלות שמישה |
|---|---|---|
| 10 ק"ג ב-₪200, בלי הצהרת ניצולת | ₪20 לק"ג | ₪20 לק"ג |
| 10 ק"ג ב-₪200, ניצולת 80% | ₪20 לק"ג | **₪25 לק"ג** |
| ניצולת 100% | ₪20 | ₪20 |

`NULL` פירושו "אף אחד לא הצהיר על פחת", ולכן שתי העלויות זהות — **לא אפס**.
ניצולת 0% ומעל 100% נדחות ברמת ה-CHECK: מ-0% לא יוצא כלום ואין לו עלות
לחשב, ומעל 100% פירושו שהניקוי ייצר חומר.

המנוע קורא את **העלות השמישה**, כי כמויות במתכון מתייחסות לחומר נקי.

### 1.5 פירוט העלות והצד המכירתי (דרישות E, F, G)

`recipes`: `packaging_cost`, `labor_cost`, `other_cost` — **כולן NULL כברירת
מחדל**, `sale_price_basis` (`batch` / `unit`) ו-`target_gm`.

**שכירות, חשמל ותקורה אינם מחושבים אוטומטית.** אין מודל שמגדיר איך לחלק
אותם למתכון אחד, ומספר שנראה כמו מדידה אבל הוא ניחוש גרוע מריק. המשתמש יכול
להזין "עלויות נוספות", והמסך אומר במפורש מה **לא** הוזן.

`sale_price_basis` נשמר ולא מנוחש. קריאת מחיר ליחידה כמחיר למתכון הופכת פוד
קוסט של 30% ל-300%, ושום דבר במסך לא היה מגלה מה קרה.

---

## 2. תקלות אמיתיות שנמצאו — לא תקלות בטסטים

### 2.1 `sale_price` לא נשמר בכלל (קדם לשלב 8) — מיגרציה 0014

מיגרציה 0012 הוסיפה את `recipes.sale_price`, והלקוח שולח אותו בכל שמירה בתוך
`p_recipe`. אבל `save_recipe` **מונה** את העמודות שהיא כותבת, ואף אחד לא
הוסיף את `sale_price` לרשימה. התוצאה: מחיר המכירה נקלט בטופס, נשלח למסד,
ו**נזרק בשקט**. `restore_recipe_version` גם היא לא שיחזרה אותו.

למה אף טסט לא תפס: הכפיל בזיכרון מחיל את השורה **כולה**, במקום למנות עמודות.
רק Postgres אמיתי יכול היה להראות את זה.

**התיקון אינו "להוסיף שם לשתי רשימות של 100 שורות"** — כך בדיוק נוצרה
התקלה. השדות החדשים נמצאים ב-helper **אחד** (`apply_recipe_costing`) ששתי
הפונקציות קוראות. הוספת שדה תמחור מעתה נוגעת בפקודה אחת.

נבדק חי: יצירה שומרת, גרסה מקפיאה, שחזור מחזיר, ומפתח חסר נשאר NULL (§5,
בדיקות 22-28).

### 2.2 `ON CONFLICT (owner_id, key)` לא תפס אף אילוץ — מיגרציה 0015

נמצא **בהרצה מול המסד החי**, לא מול כפיל:

```
there is no unique or exclusion constraint matching the ON CONFLICT specification
```

מיגרציה 0011 אכפה "חומר גלם אחד לכל key בחשבון" באמצעות **אינדקס ייחודי על
ביטוי**: `unique (coalesce(owner_id, '000…0'), key)`. ה-coalesce שם כי לשורת
מערכת אין owner, ואינדקס ייחודי רגיל היה מתייחס לכל NULL כשונה. הוא עשה את
עבודתו — אבל `ON CONFLICT (owner_id, key)` **אינו יכול** להתאים לאינדקס על
ביטוי. זה שבר:

- את `record_purchase` (0013), שהיא כל דרך רישום המחיר בשלב 8, וגם
- את `saveCatalogItem` ב-Repository, ששולח ל-PostgREST
  `on_conflict=owner_id,key` — כלומר **נתיב ה-upsert של שלב 7 היה שבור מול
  המסד האמיתי**, והכפיל בזיכרון הסתיר את זה, כי הוא עושה upsert בחיפוש שורה
  ולא כמו Postgres.

התיקון: `UNIQUE NULLS NOT DISTINCT (owner_id, key)` — בדיוק מה שה-coalesce
חיקה (NULL-ים משתווים, ולכן שתי שורות מערכת עדיין לא יכולות לחלוק key), אבל
כאילוץ אמיתי שניתן להסקה. הפרויקט מריץ Postgres 17.6, שתומך בזה. אינדקס
הביטוי נמחק כדי לא לאכוף את אותו כלל פעמיים.

### 2.3 הטריגר `touch_price_updated_at` שבר **כל** כתיבה לקטלוג — מיגרציה 0016

תקלה שלי, שהוכנסה ב-0013 ונמצאה בהרצה החיה:

```
record "new" has no field "package_price"
CONTEXT: SQL expression "new.package_price is not null"
         PL/pgSQL function public.touch_price_updated_at() line 4
```

הטריגר (0011) פועל BEFORE כל insert/update על `ingredient_catalog` וקרא את
`new.package_price` — ש-0013 שינתה ל-`purchase_total`. גוף plpgsql **אינו
נבדק** מול שינוי שם עמודה, ולכן השינוי עבר בהצלחה ואז כל כתיבה למרכז חומרי
הגלם נכשלה: הקטלוג היה למעשה לקריאה בלבד.

גם כאן הכפיל הסתיר: הוא מדמה עמודות מחושבות אבל לא טריגרים. בדקתי שאין
פונקציה נוספת שמזכירה את העמודה שהשתנתה — יש שלוש, וכולן שלי ומעודכנות.

התיקון גם מרחיב את הטריגר: הוא עוקב עכשיו אחרי **כל** מה שהמחיר נגזר ממנו
(סכום, כמות, מספר אריזות, ניצולת, יחידה), כי שינוי במספר האריזות מזיז את
המחיר בדיוק כמו שינוי בסכום. שינוי שם או אלרגן עדיין **לא** מזיז את התאריך.

### 2.4 `search_path` פתוח בטריגר (קדם לשלב 8) — מיגרציה 0017

ה-linter של Supabase סימן את `touch_price_updated_at` כפונקציה היחידה בסכימה
עם `search_path` שהתפקיד יכול לשנות. הממצא **קודם לשלב 8** (0011 יצרה אותה
בלי ההגדרה). תוקן כאן כי 0016 שכתבה את הגוף בכל מקרה. החשיפה קטנה ואומר זאת
במדויק: הפונקציה SECURITY INVOKER, ה-EXECUTE שלול מ-`public`, `anon`
ו-`authenticated`, והגוף קורא רק ל-`now()`. אחרי התיקון: **`get_advisors`
security מחזיר רשימה ריקה.**

### 2.5 `עלות כוללת` הייתה שם של שני דברים שונים

לפני שלב 8 הפאנל "עלות ותמחור" קרא ל-`computed.cost` — **עלות חומרי הגלם** —
בשם "עלות כוללת". משהוספה עלות כוללת אמיתית (חומרי גלם + אריזה + עבודה +
נוספות), השם הזה הפך למטעה. שלושת המקומות ששמו כך שונו ל"עלות חומרי גלם",
ו"עלות כוללת" פירושו עכשיו דבר אחד בלבד.

### 2.6 סבב ה-round-trip לא בדק את עמודות התמחור בכלל

`mapper-roundtrip.mts` בנה את ה-INSERT מרשימת עמודות **כתובה ביד**, וזו כבר
נסחפה: `sale_price` (שלב 7) וחמש עמודות שלב 8 לא היו בה. כלומר הבדיקה היחידה
שמעמידה את ה-mappers מול טיפוסי עמודה אמיתיים — לא בדקה אותן. הרשימה נגזרת
עכשיו ממפתחות אובייקט השורה, ולכן לא יכולה להיסחף שוב. נוספו גם assertions
מפורשים על 0 מוזן מול שדה לא מוזן.

---

## 3. חישובים — Margin, Markup ו-Food Cost

```
Gross Margin % = (מחיר − עלות כוללת) / מחיר  × 100
Food Cost %    = עלות חומרי הגלם   / מחיר   × 100
Markup %       = רווח              / עלות    × 100
```

פריט ב-₪100 שעלותו ₪40: **60% רווח גולמי** ו-**150% Markup**. שני מספרים
שונים, כל אחד בשם שלו, ו-`profitability.test.ts` מאשר שהם **אינם** שווים על
אותם נתונים. ה-Markup מוצג תמיד תחת השם "Markup % (רווח חלקי עלות)".

Food Cost מחושב מעלות **חומרי הגלם** ולא מהעלות הכוללת — אחרת המספר שכל שף
מכיר היה מוגדר מחדש בשקט.

### מחיר מחושב לפי יעד (דרישה G)

| יעד | ממה מחושב | דוגמה |
|---|---|---|
| Food Cost | **עלות חומרי הגלם** | ₪42 ביעד 30% → **₪140** |
| רווח גולמי | **העלות הכוללת** | ₪40 ביעד 60% → **₪100** |

הפאנל כותב במפורש: *"אלה מחירים שמתאימים ליעדים שהוגדרו, ולא 'המחיר
הנכון'"*. יעד Food Cost של 0% נדחה (היה דורש מחיר אינסופי), וכך גם יעד רווח
גולמי של 100% ומעלה.

חישוב יעד רווח גולמי מעלות חומרי הגלם בלבד היה אומר ₪6.25 במקום ₪100 — יש
טסט שמקבע בדיוק את ההבדל הזה.

---

## 4. NULL, 0, וחלקי (דרישה H)

| מצב | מה מוצג |
|---|---|
| חומר גלם אחד בלי מחיר | **אין** עלות כוללת, אין רווח, אין מרווח. עלות חומרי הגלם מוצגת ומסומנת כחלקית |
| אף חומר גלם ללא מחיר | אין עלות בכלל, עם הסבר |
| פער מחיר **שני מפלסים** של תת-מתכונים מטה | אותו סירוב בדיוק — עם בקרה חיובית שמוכיחה שהעץ הזה *כן* מייצר עלות ברגע שהשוקולד מתומחר |
| `labor_cost = 0` | 0. "אין עלות כזאת" |
| `labor_cost = NULL` | "—", והמסך מונה מה לא הוזן |
| `sale_price = 0` | הרווח (ההפסד) מוצג; המרווח ואחוז הפוד קוסט **אינם מוגדרים** — חילוק באפס |
| עלות כוללת 0 | מרווח 100%, Markup **לא מוגדר** |

מרווח מעלות חסרה הוא מחמיא ושגוי — בדיוק בכיוון שמפסיד כסף. לכן אין עלות
כוללת כשעלות חומרי הגלם חלקית.

---

## 5. הרצה מול Postgres החי

| מערך | תוצאה |
|---|---|
| `supabase/tests/costing.sql` (חדש) | **41/41** עברו, כולל ניקוי מלא |

שתי הבדיקות של תאריך עדכון המחיר (ords 29, 291 בקובץ) נוספו לקובץ **אחרי**
ההרצה של 41 הבדיקות, ונבדקו בפרוב נפרד מול אותו מסד: שינוי שם משאיר את
התאריך במקומו, ושינוי ניצולת מזיז אותו ואת המחיר ל-₪50. אומר את זה במקום
לספור אותן בתוך ה-41.
| `supabase/tests/pricing.sql` (שלב 7, עודכן לשם העמודה) | **28/28** |
| `supabase/tests/rls-isolation.sql` | **42/42** |
| `supabase/tests/delete-guard.sql` | **17/17** |
| `supabase/tests/sub-recipe-hardening.sql` | **15/15** |
| `roundtrip:verify` (mapper) | OK, 10 assertions |
| `roundtrip:versions:verify` | OK, 26 assertions |

מה ש-`costing.sql` מוכיח בפועל, מול המסד: ארבעת צורות הרכישה; קנה המידה
הכפול; רכישה של ₪0 מול רכישה בלי סכום; אריזה של כלום ואפס אריזות (נדחות);
ניצולת 80% / 100% / בלי הצהרה / 0% / 120%; שלוש רכישות חמאה משני ספקים
ואחוזי השינוי ביניהן (+25%, −10%); היעדר אחוז ממחיר קודם 0; תאריך עדכון
המחיר שזז מפחת ולא זז משינוי שם; שמירת שדות התמחור; NULL מול 0; הקפאת גרסה
ושחזורה; ובידוד בין חשבונות דרך RPC ודרך כתיבה ידנית של `owner_id`.

**בידוד (דרישה I), מול המסד:** B לא רואה אף רכישה של A, `purchase_history`
על ה-key של A מחזירה לו 0 שורות (וה-key אינו סוד — "חמאה 82%" הוא מה שכל
אחד היה קורא לחמאה), רישום אותו key אצל B יוצר חומר גלם של B במחיר של B
ומשאיר את A ללא שינוי, וכתיבה ידנית של `owner_id` של B נדחית. `anon` לא רואה
כלום ולא יכול לקרוא לאף אחת מהפונקציות.

---

## 6. מה שלא נשבר

| יכולת | איך נבדקה |
|---|---|
| מחיר מרכזי → עלות מתכון | `PricingFlow.test.tsx` (14), `pricing.sql` |
| גרסה היסטורית שומרת את המחיר שהיה | `pricing.sql` §11-14, `costing.sql` §24-27 |
| Sub-recipes, מחסום מחיקה, מעגלים | `sub-recipe-hardening.sql`, `delete-guard.sql` |
| null ≠ 0, מחיר 0 ≠ מחיר חסר | שני ה-round-trips + `costing.sql` §7-8, §23 |
| RLS, Auth | `rls-isolation.sql` 42/42 |
| נגישות ו-responsive | `stage5.mjs` — 54/54, אפס גלישה אופקית בטלפון ובטאבלט |
| שרשרת הצפיפויות §5.1 | לא נגעה. הקטלוג מציג מצב ומפנה לכיול, כפי שהיה |

סה"כ: **650 טסטים ב-web** (32 קבצים) ו-**195 במנוע** (8 קבצים) — כולם עוברים.
`typecheck`, `build`, `schema:check` (14 טבלות, 166 עמודות, 12 פונקציות
מוצהרות, 17 נבדקות להרשאות) ו-`seed:check` — נקיים.

---

## 7. מגבלות שנותרו — במפורש

1. **HTTP E2E אמיתי עדיין לא נבדק.** מגבלת ה-egress על `*.supabase.co` בעינה.
   `stage5.mjs` רץ מול build שבו משתני הסביבה של Supabase **ריקים**, כלומר מול
   ה-Repository של הדמו. **אין בו אף בקשת PostgREST או GoTrue**, והוא אינו
   הוכחה לנתיב ה-HTTP. הצד של המסד מוכח דרך ה-management API בלבד (§5). שתי
   התקלות 2.2 ו-2.3 הן דוגמה חיה לפער הזה: שתיהן עברו את כל 650 הטסטים
   בזיכרון ונתפסו רק בהרצה החיה.
2. **מרכז חומרי הגלם אינו נגיש במצב דמו** — אין חשבון, ולכן אין מחירים. זה
   מכוון: מחירי דמו הם בסיס עלות מומצא.
3. **אין המרת ליטר↔ק"ג במחירים.** נדרשת צפיפות אמינה, ולא נמציא אחת.
4. **שחזור גרסה שנוצרה לפני שלב 8** מאפס את שדות התמחור — ל-snapshot ההוא
   אין את המפתחות האלה, וזו האמת על אותו מסמך, בדיוק כמו כל שדה nullable אחר
   שגרסה יכולה להקדים.
5. **חלוקת תקורה** אינה ממומשת, ובכוונה — ראו §1.5.
6. הטקסט ב-`0014`/`0015`/`0016`/`0017` שהוחל בפועל מקוצר בהערות לעומת הקבצים
   בריפו; הפקודות זהות. אומר את זה במקום להציג התאמה מלאה בית-בבית.

---

## 8. Presenzo ו-WhatsApp AI Assistant

**לא נגעתי בשום צורה** ב-Presenzo Production (`fglnzrpuffswapjsnppg`), ולא
ב-WhatsApp AI Assistant (`tbkcxrpenmpcwjicnxnd`) — שנשאר INACTIVE. כל הפעולות
בדוח הזה בוצעו על `qxdpsomelzpvphkhkqrw` בלבד. מפתח `service_role` לא נכנס
לדפדפן; הלקוח משתמש במפתח ה-anon/publishable בלבד.

---

## 9. מצב המסד בסוף הריצה

```
users 0 · profiles 0 · recipes 0 · ingredients 0 · steps 0 · issues 0
trials 0 · batches 0 · versions 0 · notes 0 · calibrations 0
ingredient_catalog 0 · ingredient_purchases 0
density_table 34   ← נתוני עזר מקצועיים, אמורים להיות שם
```

כל ה-fixtures נמחקו, וכל מערך בדיקות מאשר את הניקוי של עצמו כשורה בטבלת
התוצאות שלו.
