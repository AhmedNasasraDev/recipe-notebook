# מחברת מתכונים — סרטון הצגה

משך: 06:36 · 9 פרקים · 64 כתוביות

הכתוביות מוטבעות בתמונה עצמה. הזמנים כאן הם תוכן עניינים ומדויקים לכמה שניות.

## פרקים

- **00:00** — פתיחה
- **00:24** — א. הבית והניווט
- **00:56** — ב. המחברת
- **01:20** — ג. יצירת מתכון
- **02:27** — ד. עבודה עם מתכון
- **02:47** — ה. מצב הכנה
- **03:41** — ו. חומרי גלם ועלויות
- **04:34** — ז. שאר המערכת
- **06:11** — סיום

## כל הכתוביות, לפי הסדר

- `00:00` מחברת מתכונים
- `00:03` מערכת עבודה לקונדיטוריה מקצועית — לקונדיטור עצמאי, למאפייה ולבית ספר לקונדיטוריה.
- `00:10` מתכון אחד שמחושב נכון בכל כמות: עלות אמיתית, המרות לפי הכלים של המטבח, ותהליך הכנה לצד התנור.
- `00:18` מה שרואים כאן הוא האפליקציה עצמה. כל לחיצה, הקלדה ושמירה מתרחשות באמת.
- `00:24` הבית והניווט
- `00:27` הבית פותח את יום העבודה: חיפוש, כניסות מהירות, וחזרה למה שהופסק באמצע.
- `00:34` הכניסות המהירות מובילות למחברת, לחומרי הגלם, לכלי המדידה, לקבוצות ולתכנון הייצור.
- `00:42` הקטגוריות מציגות כמה מתכונים יש בכל אחת, ומתכוני הבסיס מציגים את העלות לקילוגרם.
- `00:50` פס הניווט התחתון מחלק את המערכת לארבעה אזורים: בית, מחברת, קבוצות ועוד.
- `00:56` מחברת המתכונים
- `01:01` המחברת מרכזת את כל המתכונים: תמונה, קטגוריה, תשואה ומשקל ליחידה.
- `01:06` סינון לפי קטגוריה. הסינון נשמר, כך שחזרה לרשימה מחזירה אותה כפי שהייתה.
- `01:10` שלושה מתכוני בצקים.
- `01:14` החיפוש סורק שמות, תגיות ורכיבים — ולא רק את שם המתכון.
- `01:20` יצירת מתכון
- `01:23` אשף בארבעה שלבים. שלב ראשון: שם, קטגוריה ותגיות.
- `01:26` הקטגוריה נבחרת מתוך הקטגוריות של המערכת.
- `01:32` אפשר לבחור תמונה כבר בשלב הזה. במתכון חדש היא נשמרת יחד עם השמירה הראשונה.
- `01:39` שלב שני: חומרי הגלם. שם, כמות ויחידת מידה לכל שורה.
- `01:50` המערכת מחשבת תוך כדי הקלדה: משקל כולל, ומה חסר כדי שהחישוב יהיה מלא.
- `01:57` שלב שלישי: אופן ההכנה — שלבים, זמנים וטמפרטורות.
- `02:00` שלב רביעי: סיכום לפני שמירה.
- `02:05` המתכון נשמר ונפתח, עם כל מה שהוקלד בו.
- `02:10` התמונה שנבחרה הומרה, נשמרה ונפתחת בראש הדף, בגובה קבוע ובלי עיוות.
- `02:15` מי שמורשה לערוך יכול לקבוע איזו נקודה בתמונה תישאר במרכז.
- `02:20` הנקודה שנבחרה נשמרת יחד עם התמונה.
- `02:27` עבודה עם מתכון
- `02:32` כמה להכין: לפי מספר יחידות, לפי משקל סופי, או לפי המלאי שקיים בפועל.
- `02:35` כל הכמויות במתכון מחושבות מחדש, יחד עם המשקל הכולל ומשקל היחידה.
- `02:42` לכל שורה יש המרה — מגרמים לכוסות ובחזרה — לפי הכלים שנמדדו במערכת.
- `02:47` מצב הכנה
- `02:55` מיז אן פלאס: כל חומרי הגלם בכמות של ההכנה הזאת, לשקילה ולסימון לפני שמתחילים.
- `03:04` המסך סופר כמה נותרו, ומאפשר להתחיל גם לפני שהכול סומן.
- `03:12` הכול מוכן.
- `03:17` שלבי העבודה מופיעים אחד בכל פעם, בטקסט גדול שנקרא מרחוק.
- `03:24` טיימר לשלב, לפי הזמן שנרשם במתכון.
- `03:31` ההתקדמות נשמרת על המכשיר. אפשר לצאת ולחזור.
- `03:37` החזרה ממשיכה מהשלב שבו ההכנה נעצרה.
- `03:41` חומרי גלם ועלויות
- `03:44` מרכז חומרי הגלם: מחיר אחד לכל חומר, במקום אחד.
- `03:49` בכל מסך פנימי יש חזרה, שמחזירה למסך הקודם.
- `03:55` חמאה: עשר חבילות של קילוגרם, בשלוש מאות וארבעים שקלים. שלושים וארבעה שקלים לקילוגרם.
- `04:05` הספק העלה מחיר. אותן עשר חבילות עולות עכשיו ארבע מאות ועשרים שקלים.
- `04:13` המחיר לקילוגרם עלה משלושים וארבעה לארבעים ושניים שקלים.
- `04:22` מחיר במרכז הוא המקור לכל שורת מתכון שלא הוזן בה מחיר משלה.
- `04:25` ובמתכון: עלות חומרי הגלם, עלות לקילוגרם ועלות ליחידה, מחושבות שורה אחר שורה.
- `04:34` שאר המערכת
- `04:37` דף הזמנה: מה להזמין לאצווה הזאת, בכמויות ובעלויות, מוכן להדפסה.
- `04:46` תווית מוצר: רכיבים לפי סדר יורד, אלרגנים ומשקל.
- `04:51` כלי המדידה: הכוס והכף של המטבח הזה נמדדות פעם אחת.
- `04:58` כל המרה במערכת מחושבת לפי הכלים האלה, ולא לפי תקן כללי.
- `05:03` תכנון ייצור: יום עבודה עם כמה מתכונים, ורשימת רכש אחת.
- `05:15` רשימת הרכש מסכמת את חומרי הגלם של כל היום, ומסמנת מה שאין לו מחיר.
- `05:24` קבוצות וקורסים. המסכים וכללי ההרשאות הם קוד אמיתי, ובהדגמה הזאת הם רצים מקומית — בלי שרת ובלי משתמשים אחרים.
- `05:36` קורס עם שיעורים ועם המתכונים שהמדריך פרסם לתלמידים.
- `05:43` צ׳אט הקבוצה. בהדגמה מקומית ההודעה נשמרת במסך הזה בלבד, ואינה נשלחת לאיש.
- `05:46` ההודעה מופיעה בשיחה.
- `05:49` חברים והרשאות: תפקידים, דרגות, ומי רשאי לעשות מה בכל מתכון.
- `05:58` הגדרות: פרופיל עבודה, שפה וכיול הכלים.
- `06:06` המסך ״עוד״ מרכז את כל מה שאינו מתכון.
- `06:11` סביבת ההדגמה
- `06:15` נתוני דוגמה, בלי שרת ובלי חשבון. המתכונים, החישובים, ההמרות, העלויות ומצב ההכנה פועלים כאן במלואם.
- `06:24` הקבוצות והצ׳אט רצים מקומית בלבד. אין בהדגמה הזאת סנכרון בענן ואין עבודה משותפת בין משתמשים.
- `06:31` מתכון אחד שמחושב נכון בכל כמות — מהרעיון, דרך העלות, ועד ההכנה במטבח.

## מה אמיתי ומה סימולציה בהקלטה

- **אמיתי** — כל המסכים, הניווט, האשף, חישוב הכמויות, ההמרות, העלויות,
  האלרגנים, מצב ההכנה, הטיימר, שמירת ההתקדמות, כלי המדידה, תמונת המתכון
  והתאמת המיקום שלה. זה קוד המוצר מחשב לעצמו.
- **סימולציה מקומית** — הקבוצות, הצ׳אט, ההזמנות ותוכנית הייצור. הסכימה,
  ההרשאות וה־RLS קיימים כמיגרציות, אבל אין שרת רץ בסביבה הזאת, ולכן
  המשתמשים והמסירה מדומים. הכתוביות אומרות זאת במסכים האלה.
- **לא מומש** — «פענוח חכם» של מתכון מודבק (דורש proxy עם מפתח בצד שרת),
  ומצב הסקיילינג «נפח סופי». שניהם אינם מוצגים בסרטון.
- **קיים במוצר ואינו מוצג בסרטון הזה** — איסוף האלרגנים האוטומטי וההערה
  האישית. בהקלטה נשאר גיליון ההמרה פתוח מעליהם, ולכן הקטע הזה נחתך
  בעריכת הסנכרון במקום להשאיר כיתוב מעל מסך שאינו מתאים לו. ראה
  SYNC-NOTES.md.

