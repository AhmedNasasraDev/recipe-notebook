# מחברת מתכונים — סרטון הצגה

משך: 07:01 · 9 פרקים · 64 כתוביות

הכתוביות מוטבעות בתמונה עצמה. הזמנים כאן הם תוכן עניינים ומדויקים לכמה שניות.

## פרקים

- **00:00** — פתיחה
- **00:25** — א. הבית והניווט
- **00:58** — ב. המחברת
- **01:26** — ג. יצירת מתכון
- **02:38** — ד. עבודה עם מתכון
- **03:02** — ה. מצב הכנה
- **03:56** — ו. חומרי גלם ועלויות
- **04:51** — ז. שאר המערכת
- **06:32** — סיום

## כל הכתוביות, לפי הסדר

- `00:00` מחברת מתכונים
- `00:03` מערכת עבודה לקונדיטוריה מקצועית — לקונדיטור עצמאי, למאפייה ולבית ספר לקונדיטוריה.
- `00:10` מתכון אחד שמחושב נכון בכל כמות: עלות אמיתית, המרות לפי הכלים של המטבח, ותהליך הכנה לצד התנור.
- `00:19` מה שרואים כאן הוא האפליקציה עצמה. כל לחיצה, הקלדה ושמירה מתרחשות באמת.
- `00:25` הבית והניווט
- `00:28` הבית פותח את יום העבודה: חיפוש, כניסות מהירות, וחזרה למה שהופסק באמצע.
- `00:35` הכניסות המהירות מובילות למחברת, לחומרי הגלם, לכלי המדידה, לקבוצות ולתכנון הייצור.
- `00:43` הקטגוריות מציגות כמה מתכונים יש בכל אחת, ומתכוני הבסיס מציגים את העלות לקילוגרם.
- `00:51` פס הניווט התחתון מחלק את המערכת לארבעה אזורים: בית, מחברת, קבוצות ועוד.
- `00:58` מחברת המתכונים
- `01:02` המחברת מרכזת את כל המתכונים: תמונה, קטגוריה, תשואה ומשקל ליחידה.
- `01:08` סינון לפי קטגוריה. הסינון נשמר, כך שחזרה לרשימה מחזירה אותה כפי שהייתה.
- `01:15` שלושה מתכוני בצקים.
- `01:19` החיפוש סורק שמות, תגיות ורכיבים — ולא רק את שם המתכון.
- `01:26` יצירת מתכון
- `01:28` אשף בארבעה שלבים. שלב ראשון: שם, קטגוריה ותגיות.
- `01:34` הקטגוריה נבחרת מתוך הקטגוריות של המערכת.
- `01:39` אפשר לבחור תמונה כבר בשלב הזה. במתכון חדש היא נשמרת יחד עם השמירה הראשונה.
- `01:47` שלב שני: חומרי הגלם. שם, כמות ויחידת מידה לכל שורה.
- `01:57` המערכת מחשבת תוך כדי הקלדה: משקל כולל, ומה חסר כדי שהחישוב יהיה מלא.
- `02:05` שלב שלישי: אופן ההכנה — שלבים, זמנים וטמפרטורות.
- `02:10` שלב רביעי: סיכום לפני שמירה.
- `02:16` המתכון נשמר ונפתח, עם כל מה שהוקלד בו.
- `02:20` התמונה שנבחרה הומרה, נשמרה ונפתחת בראש הדף, בגובה קבוע ובלי עיוות.
- `02:26` מי שמורשה לערוך יכול לקבוע איזו נקודה בתמונה תישאר במרכז.
- `02:31` הנקודה שנבחרה נשמרת יחד עם התמונה.
- `02:38` עבודה עם מתכון
- `02:43` כמה להכין: לפי מספר יחידות, לפי משקל סופי, או לפי המלאי שקיים בפועל.
- `02:49` כל הכמויות במתכון מחושבות מחדש, יחד עם המשקל הכולל ומשקל היחידה.
- `02:56` לכל שורה יש המרה — מגרמים לכוסות ובחזרה — לפי הכלים שנמדדו במערכת.
- `03:02` מצב הכנה
- `03:10` מיז אן פלאס: כל חומרי הגלם בכמות של ההכנה הזאת, לשקילה ולסימון לפני שמתחילים.
- `03:20` המסך סופר כמה נותרו, ומאפשר להתחיל גם לפני שהכול סומן.
- `03:28` הכול מוכן.
- `03:33` שלבי העבודה מופיעים אחד בכל פעם, בטקסט גדול שנקרא מרחוק.
- `03:40` טיימר לשלב, לפי הזמן שנרשם במתכון.
- `03:47` ההתקדמות נשמרת על המכשיר. אפשר לצאת ולחזור.
- `03:52` החזרה ממשיכה מהשלב שבו ההכנה נעצרה.
- `03:56` חומרי גלם ועלויות
- `04:00` מרכז חומרי הגלם: מחיר אחד לכל חומר, במקום אחד.
- `04:04` בכל מסך פנימי יש חזרה, שמחזירה למסך הקודם.
- `04:10` חמאה: עשר חבילות של קילוגרם, בשלוש מאות וארבעים שקלים. שלושים וארבעה שקלים לקילוגרם.
- `04:20` הספק העלה מחיר. אותן עשר חבילות עולות עכשיו ארבע מאות ועשרים שקלים.
- `04:28` המחיר לקילוגרם עלה משלושים וארבעה לארבעים ושניים שקלים.
- `04:38` מחיר במרכז הוא המקור לכל שורת מתכון שלא הוזן בה מחיר משלה.
- `04:43` ובמתכון: עלות חומרי הגלם, עלות לקילוגרם ועלות ליחידה, מחושבות שורה אחר שורה.
- `04:51` שאר המערכת
- `04:54` דף הזמנה: מה להזמין לאצווה הזאת, בכמויות ובעלויות, מוכן להדפסה.
- `05:03` תווית מוצר: רכיבים לפי סדר יורד, אלרגנים ומשקל.
- `05:09` כלי המדידה: הכוס והכף של המטבח הזה נמדדות פעם אחת.
- `05:16` כל המרה במערכת מחושבת לפי הכלים האלה, ולא לפי תקן כללי.
- `05:21` תכנון ייצור: יום עבודה עם כמה מתכונים, ורשימת רכש אחת.
- `05:33` רשימת הרכש מסכמת את חומרי הגלם של כל היום, ומסמנת מה שאין לו מחיר.
- `05:41` קבוצות וקורסים. המסכים וכללי ההרשאות הם קוד אמיתי, ובהדגמה הזאת הם רצים מקומית — בלי שרת ובלי משתמשים אחרים.
- `05:53` קורס עם שיעורים ועם המתכונים שהמדריך פרסם לתלמידים.
- `06:00` צ׳אט הקבוצה. בהדגמה מקומית ההודעה נשמרת במסך הזה בלבד, ואינה נשלחת לאיש.
- `06:07` ההודעה מופיעה בשיחה.
- `06:10` חברים והרשאות: תפקידים, דרגות, ומי רשאי לעשות מה בכל מתכון.
- `06:19` הגדרות: פרופיל עבודה, שפה וכיול הכלים.
- `06:27` המסך ״עוד״ מרכז את כל מה שאינו מתכון.
- `06:32` סביבת ההדגמה
- `06:36` נתוני דוגמה, בלי שרת ובלי חשבון. המתכונים, החישובים, ההמרות, העלויות ומצב ההכנה פועלים כאן במלואם.
- `06:45` הקבוצות והצ׳אט רצים מקומית בלבד. אין בהדגמה הזאת סנכרון בענן ואין עבודה משותפת בין משתמשים.
- `06:53` מתכון אחד שמחושב נכון בכל כמות — מהרעיון, דרך העלות, ועד ההכנה במטבח.

## מה אמיתי ומה סימולציה בהקלטה

- **אמיתי** — כל המסכים, הניווט, האשף, חישוב הכמויות, ההמרות, העלויות,
  האלרגנים, מצב ההכנה, הטיימר, שמירת ההתקדמות, כלי המדידה, תמונת המתכון
  והתאמת המיקום שלה. זה קוד המוצר מחשב לעצמו.
- **סימולציה מקומית** — הקבוצות, הצ׳אט, ההזמנות ותוכנית הייצור. הסכימה,
  ההרשאות וה־RLS קיימים כמיגרציות, אבל אין שרת רץ בסביבה הזאת, ולכן
  המשתמשים והמסירה מדומים. הכתוביות אומרות זאת במסכים האלה.
- **לא מומש** — «פענוח חכם» של מתכון מודבק (דורש proxy עם מפתח בצד שרת),
  ומצב הסקיילינג «נפח סופי». שניהם אינם מוצגים בסרטון.
- **קיים במוצר ואינו מוצג בסרטון הזה** — איסוף האלרגנים האוטומטי וההערה
  האישית. בהקלטה נשאר גיליון ההמרה פתוח מעליהם, ולכן הקטע הזה נחתך
  בעריכת הסנכרון במקום להשאיר כיתוב מעל מסך שאינו מתאים לו. ראה
  SYNC-NOTES.md.

