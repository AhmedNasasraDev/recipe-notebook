# מחברת מתכונים — סרטון הצגה

משך: 06:46 · 9 פרקים · 67 כתוביות

הכתוביות מוטבעות בתמונה עצמה. הזמנים כאן הם תוכן עניינים ומדויקים לכמה שניות.

## פרקים

- **00:01** — פתיחה
- **00:24** — א. הבית והניווט
- **00:57** — ב. המחברת
- **01:19** — ג. יצירת מתכון
- **02:26** — ד. עבודה עם מתכון
- **03:02** — ה. מצב הכנה
- **03:54** — ו. חומרי גלם ועלויות
- **04:46** — ז. שאר המערכת
- **06:22** — סיום

## כל הכתוביות, לפי הסדר

- `00:01` מחברת מתכונים
- `00:04` מערכת עבודה לקונדיטוריה מקצועית — לקונדיטור עצמאי, למאפייה ולבית ספר לקונדיטוריה.
- `00:10` מתכון אחד שמחושב נכון בכל כמות: עלות אמיתית, המרות לפי הכלים של המטבח, ותהליך הכנה לצד התנור.
- `00:18` מה שרואים כאן הוא האפליקציה עצמה. כל לחיצה, הקלדה ושמירה מתרחשות באמת.
- `00:24` הבית והניווט
- `00:27` הבית פותח את יום העבודה: חיפוש, כניסות מהירות, וחזרה למה שהופסק באמצע.
- `00:34` הכניסות המהירות מובילות למחברת, לחומרי הגלם, לכלי המדידה, לקבוצות ולתכנון הייצור.
- `00:42` הקטגוריות מציגות כמה מתכונים יש בכל אחת, ומתכוני הבסיס מציגים את העלות לקילוגרם.
- `00:50` פס הניווט התחתון מחלק את המערכת לארבעה אזורים: בית, מחברת, קבוצות ועוד.
- `00:57` מחברת המתכונים
- `01:00` המחברת מרכזת את כל המתכונים: תמונה, קטגוריה, תשואה ומשקל ליחידה.
- `01:06` סינון לפי קטגוריה. הסינון נשמר, כך שחזרה לרשימה מחזירה אותה כפי שהייתה.
- `01:09` שלושה מתכוני בצקים.
- `01:13` החיפוש סורק שמות, תגיות ורכיבים — ולא רק את שם המתכון.
- `01:19` יצירת מתכון
- `01:22` אשף בארבעה שלבים. שלב ראשון: שם, קטגוריה ותגיות.
- `01:25` הקטגוריה נבחרת מתוך הקטגוריות של המערכת.
- `01:30` אפשר לבחור תמונה כבר בשלב הזה. במתכון חדש היא נשמרת יחד עם השמירה הראשונה.
- `01:38` שלב שני: חומרי הגלם. שם, כמות ויחידת מידה לכל שורה.
- `01:48` המערכת מחשבת תוך כדי הקלדה: משקל כולל, ומה חסר כדי שהחישוב יהיה מלא.
- `01:55` שלב שלישי: אופן ההכנה — שלבים, זמנים וטמפרטורות.
- `01:58` שלב רביעי: סיכום לפני שמירה.
- `02:03` המתכון נשמר ונפתח, עם כל מה שהוקלד בו.
- `02:07` התמונה שנבחרה הומרה, נשמרה ונפתחת בראש הדף, בגובה קבוע ובלי עיוות.
- `02:13` מי שמורשה לערוך יכול לקבוע איזו נקודה בתמונה תישאר במרכז.
- `02:18` הנקודה שנבחרה נשמרת יחד עם התמונה.
- `02:26` עבודה עם מתכון
- `02:29` כמה להכין: לפי מספר יחידות, לפי משקל סופי, או לפי המלאי שקיים בפועל.
- `02:33` כל הכמויות במתכון מחושבות מחדש, יחד עם המשקל הכולל ומשקל היחידה.
- `02:39` לכל שורה יש המרה — מגרמים לכוסות ובחזרה — לפי הכלים שנמדדו במערכת.
- `02:44` העלויות מחושבות לכל שורה, לכל האצווה, ליחידה ולקילוגרם.
- `02:50` האלרגנים נאספים אוטומטית מחומרי הגלם של המתכון.
- `02:56` ההערה האישית נשארת פרטית, ואינה חלק מהמתכון שמשותף עם אחרים.
- `03:02` מצב הכנה
- `03:08` מיז אן פלאס: כל חומרי הגלם בכמות של ההכנה הזאת, לשקילה ולסימון לפני שמתחילים.
- `03:18` המסך סופר כמה נותרו, ומאפשר להתחיל גם לפני שהכול סומן.
- `03:26` הכול מוכן.
- `03:30` שלבי העבודה מופיעים אחד בכל פעם, בטקסט גדול שנקרא מרחוק.
- `03:38` טיימר לשלב, לפי הזמן שנרשם במתכון.
- `03:44` ההתקדמות נשמרת על המכשיר. אפשר לצאת ולחזור.
- `03:50` החזרה ממשיכה מהשלב שבו ההכנה נעצרה.
- `03:54` חומרי גלם ועלויות
- `03:57` מרכז חומרי הגלם: מחיר אחד לכל חומר, במקום אחד.
- `04:01` בכל מסך פנימי יש חזרה, שמחזירה למסך הקודם.
- `04:08` חמאה: עשר חבילות של קילוגרם, בשלוש מאות וארבעים שקלים. שלושים וארבעה שקלים לקילוגרם.
- `04:17` הספק העלה מחיר. אותן עשר חבילות עולות עכשיו ארבע מאות ועשרים שקלים.
- `04:25` המחיר לקילוגרם עלה משלושים וארבעה לארבעים ושניים שקלים.
- `04:34` מחיר במרכז הוא המקור לכל שורת מתכון שלא הוזן בה מחיר משלה.
- `04:37` ובמתכון: עלות חומרי הגלם, עלות לקילוגרם ועלות ליחידה, מחושבות שורה אחר שורה.
- `04:46` שאר המערכת
- `04:48` דף הזמנה: מה להזמין לאצווה הזאת, בכמויות ובעלויות, מוכן להדפסה.
- `04:58` תווית מוצר: רכיבים לפי סדר יורד, אלרגנים ומשקל.
- `05:03` כלי המדידה: הכוס והכף של המטבח הזה נמדדות פעם אחת.
- `05:09` כל המרה במערכת מחושבת לפי הכלים האלה, ולא לפי תקן כללי.
- `05:16` תכנון ייצור: יום עבודה עם כמה מתכונים, ורשימת רכש אחת.
- `05:26` רשימת הרכש מסכמת את חומרי הגלם של כל היום, ומסמנת מה שאין לו מחיר.
- `05:35` קבוצות וקורסים. המסכים וכללי ההרשאות הם קוד אמיתי, ובהדגמה הזאת הם רצים מקומית — בלי שרת ובלי משתמשים אחרים.
- `05:46` קורס עם שיעורים ועם המתכונים שהמדריך פרסם לתלמידים.
- `05:53` צ׳אט הקבוצה. בהדגמה מקומית ההודעה נשמרת במסך הזה בלבד, ואינה נשלחת לאיש.
- `05:57` ההודעה מופיעה בשיחה.
- `06:01` חברים והרשאות: תפקידים, דרגות, ומי רשאי לעשות מה בכל מתכון.
- `06:10` הגדרות: פרופיל עבודה, שפה וכיול הכלים.
- `06:17` המסך ״עוד״ מרכז את כל מה שאינו מתכון.
- `06:22` סביבת ההדגמה
- `06:25` נתוני דוגמה, בלי שרת ובלי חשבון. המתכונים, החישובים, ההמרות, העלויות ומצב ההכנה פועלים כאן במלואם.
- `06:33` הקבוצות והצ׳אט רצים מקומית בלבד. אין בהדגמה הזאת סנכרון בענן ואין עבודה משותפת בין משתמשים.
- `06:41` מתכון אחד שמחושב נכון בכל כמות — מהרעיון, דרך העלות, ועד ההכנה במטבח.

## מה אמיתי ומה סימולציה בהקלטה

- **אמיתי** — כל המסכים, הניווט, האשף, חישוב הכמויות, ההמרות, העלויות,
  האלרגנים, מצב ההכנה, הטיימר, שמירת ההתקדמות, כלי המדידה, תמונת המתכון
  והתאמת המיקום שלה. זה קוד המוצר מחשב לעצמו.
- **סימולציה מקומית** — הקבוצות, הצ׳אט, ההזמנות ותוכנית הייצור. הסכימה,
  ההרשאות וה־RLS קיימים כמיגרציות, אבל אין שרת רץ בסביבה הזאת, ולכן
  המשתמשים והמסירה מדומים. הכתוביות אומרות זאת במסכים האלה.
- **לא מומש** — «פענוח חכם» של מתכון מודבק (דורש proxy עם מפתח בצד שרת),
  ומצב הסקיילינג «נפח סופי». שניהם אינם מוצגים בסרטון.

