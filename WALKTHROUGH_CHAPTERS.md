# מחברת מתכונים — סרטון הצגה

משך: 04:41 · 9 פרקים · 61 כתוביות

הכתוביות מוטבעות בתמונה עצמה. הזמנים כאן הם תוכן עניינים ומדויקים לכמה שניות.

## פרקים

- **00:01** — פתיחה
- **00:07** — א. הבית והניווט
- **00:23** — ב. המחברת
- **00:45** — ג. יצירת מתכון
- **01:36** — ד. עבודה עם מתכון
- **02:10** — ה. מצב הכנה
- **02:55** — ו. חומרי גלם ועלויות
- **03:18** — ז. שאר המערכת
- **04:34** — סיום

## כל הכתוביות, לפי הסדר

- `00:01` מחברת מתכונים — מערכת עבודה לקונדיטוריה מקצועית
- `00:04` ההקלטה הזאת היא האפליקציה עצמה, רצה בדפדפן. כל לחיצה, הקלדה ושמירה כאן אמיתיות.
- `00:07` מסך הבית והניווט
- `00:09` הבית הוא נקודת הפתיחה: חיפוש, שש דרכי כניסה, והמשך מאיפה שעצרת.
- `00:13` פעולות מהירות — המחברת, חומרי גלם, כלי מדידה, קבוצות, תכנון וקטגוריות.
- `00:17` קטגוריות עם מספר המתכונים בכל אחת, ומתכוני הבסיס עם העלות לקילוגרם.
- `00:21` פס הניווט התחתון מחזיק ארבעה אזורים: בית, מחברת, קבוצות ועוד.
- `00:23` מחברת המתכונים והקטגוריות
- `00:26` המחברת: כל המתכונים, עם תמונה, קטגוריה, תשואה ומשקל ליחידה.
- `00:32` סינון לפי קטגוריה — הסינון נשמר בכתובת, כך שחזרה לרשימה מחזירה אותו.
- `00:35` שלושה מתכוני בצקים.
- `00:39` חיפוש חופשי לפי שם, תג או רכיב.
- `00:41` החיפוש עובד על שמות, תגים ורכיבים — לא רק על השם.
- `00:45` יצירת מתכון — אשף בארבעה שלבים
- `00:49` שלב 1 — פרטים: שם, קטגוריה, תגים, ותמונה.
- `00:55` בחירת קטגוריה מתוך הקטגוריות של המערכת.
- `00:58` תמונה אפשר לבחור כבר כאן. במתכון חדש היא מועלית ברגע השמירה הראשונה — לפני זה אין עוד מתכון לשמור אותה עליו.
- `01:03` שלב 2 — חומרי גלם: שם, כמות ויחידה לכל שורה.
- `01:14` המערכת מחשבת תוך כדי: משקל כולל, שלמות החישוב ומה חסר.
- `01:21` שלב 3 — אופן ההכנה: שלבים, זמנים וטמפרטורות.
- `01:25` שלב 4 — סיכום, ואז שמירה.
- `01:30` נשמר. המתכון נפתח, והתוכן שהוקלד נמצא בו.
- `01:36` עבודה עם מתכון
- `01:39` תמונת המתכון בראש הדף, בגובה קבוע וללא מתיחה.
- `01:42` למי שמורשה לערוך: התאמת המיקום שנשאר במרכז החיתוך.
- `01:46` שינוי כמות להכנה: לפי יחידות, לפי משקל, או לפי המלאי שיש בפועל.
- `01:51` כל הכמויות במתכון חושבו מחדש — כולל המשקל הכולל ומשקל היחידה.
- `01:55` כל שורה מציגה המרה: מגרמים לכוסות וחזרה, לפי הכלים שנמדדו בהגדרות.
- `01:59` עלויות: עלות לכל שורה, עלות כוללת, עלות ליחידה ולקילוגרם.
- `02:04` אלרגנים נאספים אוטומטית מחומרי הגלם שבמתכון.
- `02:07` הערה אישית — שמורה לחשבון בלבד, ואינה נוסעת עם המתכון בשיתוף.
- `02:10` תהליך הכנה מלא
- `02:17` מיז־אן־פלאס: כל חומרי הגלם בכמות של ההכנה הזאת, לסימון לפני שמתחילים.
- `02:23` המסך אומר כמה נותרו — ואפשר לעבור להכנה גם לפני שהכול סומן.
- `02:29` הכול מוכן.
- `02:33` שלבי העבודה, אחד בכל פעם, בטקסט גדול למטבח.
- `02:38` טיימר לשלב, מתוך הזמן שנרשם במתכון.
- `02:44` ההתקדמות נשמרת על המכשיר — יציאה וחזרה מחזירות לאותו שלב.
- `02:51` חזרנו — וההכנה ממשיכה מהשלב שבו עצרנו, לא מההתחלה.
- `02:55` חומרי גלם ועלויות
- `02:58` מרכז חומרי הגלם: מחיר אחד לכל חומר, במקום אחד.
- `03:01` כאן היה חסר כפתור חזרה — הוא נוסף, והוא חוזר למסך שממנו באת.
- `03:05` נשנה את מחיר החמאה ונראה מה זה עושה לעלות של מתכון.
- `03:10` 340 ש״ח ל־10 ק״ג הופכים ל־52 ש״ח לקילוגרם.
- `03:15` אותו מתכון, אחרי שינוי המחיר: העלויות חושבו מחדש מהמחיר החדש.
- `03:18` שאר מסכי המערכת
- `03:21` דף הזמנה: מה להזמין לאצווה הזאת, בכמויות ובעלויות. מיועד להדפסה.
- `03:28` תווית מוצר: רכיבים לפי סדר יורד, אלרגנים ומשקל — גם היא להדפסה.
- `03:33` כלי המדידה: הכוס והכף של המטבח הזה, נמדדות פעם אחת.
- `03:37` כל המרה במערכת נעשית לפי הכלים האלה, ולא לפי תקן שרירותי.
- `03:41` תכנון ייצור: יום עבודה עם כמה מתכונים ורשימת רכש אחת.
- `03:47` רשימת הרכש מסכמת את כל חומרי הגלם של היום, עם מה שאין לו מחיר.
- `03:55` קבוצות וקורסים — כאן ואילך זו סימולציה מקומית: אין שרת ואין משתמשים אחרים.
- `04:00` קורס עם שיעורים ומתכונים שהמדריך פרסם לתלמידים.
- `04:05` צ׳אט הקבוצה. ההרשאות אמיתיות בקוד; המשתמשים כאן מדומים.
- `04:12` ההודעה נשלחה ומופיעה בשיחה.
- `04:15` חברים והרשאות: תפקידים, דרגות, ומי יכול לעשות מה בכל מתכון.
- `04:23` הגדרות: פרופיל עבודה, שפה, וכיול הכלים.
- `04:30` ״עוד״ מרכז את כל מה שאינו מתכון: חומרי גלם, תכנון, כלים והגדרות.
- `04:34` מקונדיטור אחד ועד קורס שלם — מתכון, עלות, הכנה וצוות במקום אחד.
- `04:38` הקלטה של המערכת כפי שהיא רצה. ללא שרת בסביבה הזאת — ולכן הקבוצות והצ׳אט מדומים.

## מה אמיתי ומה סימולציה בהקלטה

- **אמיתי** — כל המסכים, הניווט, האשף, חישוב הכמויות, ההמרות, העלויות,
  האלרגנים, מצב ההכנה, הטיימר, שמירת ההתקדמות, כלי המדידה, תמונת המתכון
  והתאמת המיקום שלה. זה קוד המוצר מחשב לעצמו.
- **סימולציה מקומית** — הקבוצות, הצ׳אט, ההזמנות ותוכנית הייצור. הסכימה,
  ההרשאות וה־RLS קיימים כמיגרציות, אבל אין שרת רץ בסביבה הזאת, ולכן
  המשתמשים והמסירה מדומים. הכתוביות אומרות זאת במסכים האלה.
- **לא מומש** — «פענוח חכם» של מתכון מודבק (דורש proxy עם מפתח בצד שרת),
  ומצב הסקיילינג «נפח סופי». שניהם אינם מוצגים בסרטון.

