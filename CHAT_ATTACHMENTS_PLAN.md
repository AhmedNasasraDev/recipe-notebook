# צירוף תמונה או מתכון לצ׳אט — מה קיים היום ותוכנית מימוש

מסמך תכנון בלבד. **לא בוצע שום שינוי בסכימה, בהרשאות או בממשק עבור היכולת
הזאת**, ולא נוסף כפתור דמה. הנתונים למטה נבדקו בקוד ובמיגרציות, עם הפניות
מדויקות.

---

## 1. מה נתמך היום

### הצ׳אט עצמו
| מה | איפה | מצב |
| --- | --- | --- |
| חוזה השליחה | `apps/web/src/data/repository.ts:450` | `{ groupId, body, replyToId?, kind?: 'text' \| 'announcement' }` — **אין שדה צירוף** |
| טיפוס ההודעה | `apps/web/src/features/groups/types.ts:95` | `id, seq, groupId, authorId, body, kind, replyToId, editedAt, deletedAt, createdAt` — **אין צירוף** |
| המימוש מול השרת | `apps/web/src/data/supabaseGroups.ts:799` | `insert` ל־`group_messages` בלבד |
| הטבלה | `supabase/migrations/0032_group_chat.sql:87` | `body text`, `kind check (text/announcement/system)`, `channel_id` שמור לעתיד |
| תיבת הכתיבה | `apps/web/src/features/groups/GroupChat.tsx:494` | `textarea` + כפתור שליחה יחיד. **אין פקד צירוף** |

### מה שכבר בנוי ואפשר להישען עליו
1. **דלי תמונות פרטי עובד** — `recipe-images`, `public = false`, תקרה 2MB,
   `allowed_mime_types = ['image/webp']` (`0029_recipe_images.sql:54`).
   ההמרה ל־WebP בצד הלקוח כבר מוחקת EXIF (כולל GPS) — זה נכתב שם במפורש.
   יש טבלת אינדקס `public.recipe_images` עם RLS: קריאה לפי
   `can_read_recipe(recipe_id)`, כתיבה לפי `owns_recipe(recipe_id)`, ומדיניות
   מקבילה על `storage.objects` לפי המקטע הראשון בנתיב.
2. **URL חתום** כבר קיים בשכבת הנתונים (`RecipeImageRepository`,
   `repository.ts:293`, כולל `recipeThumbs`).
3. **שיתוף מתכון לקבוצה כבר קיים** — `publishRecipe(lessonId, recipeId, name)`
   ו־`group_recipe_items` (`0036_publish_recipe_and_item_notes.sql`), עם
   `setItemPerms` ו־`saveGroupCopy`.
4. **המיגרציה 0032 תכננה את זה מראש.** בכותרת שלה כתוב במפורש:
   `attachments  a future group_message_attachments(message_id, storage_path…)`
   ו־"None of these needs this schema to change".

### שלוש עובדות שמכתיבות את התכנון
* **אין `DELETE` על הודעות.** `0034` שולל את ההרשאה, והמחיקה היא רכה: `0035`
  מעביר את הטקסט ל־`group_message_removals` שרק דרגה ≥ 2 קוראת. לכן **צירוף
  חייב מנגנון הרחקה משלו במחיקה רכה** — אחרת הודעה "נמחקה" והתמונה שלה עדיין
  נגישה בקישור חתום.
* **`author_id` חייב להיות המשתמש עצמו** (`messages_insert`), כדי שלא תיכתב
  הודעה בשם אחר. אותו כלל חייב לחול על מעלה הקובץ.
* **מתכון פרטי הוא פרטי.** `can_read_recipe` לא נותן לחבר קבוצה לראות מתכון
  שלא פורסם. צירוף מתכון לצ׳אט **לא יכול** להיות "קישור למתכון שלי" — זה היה
  פותח דלת קריאה שלא קיימת היום.

---

## 2. התוכנית — שני סוגי צירוף, שני מסלולים שונים

### מסלול א׳ — צירוף מתכון (זול, בלי סכימה חדשה, בלי הרשאה חדשה)
צירוף מתכון = **הפניה לפריט קבוצה קיים**, לא למתכון האישי.

* שלב 1: בוחר מתכון מתוך מה שכבר מפורסם בקבוצה (`group_recipe_items`).
* שלב 2: אם המתכון עוד לא מפורסם — מוצג לו במפורש
  "כדי לצרף את המתכון צריך לפרסם אותו לקבוצה", והפרסום נעשה בזרימה הקיימת
  (`publishRecipe`) בהסכמה מודעת. **אין פרסום שקט.**
* שדה חדש בטבלה: `item_id uuid references public.group_recipe_items(id) on delete set null`.
  זה **עמודה אחת**, לא טבלה, כי צירוף מתכון הוא יחיד ולא רשימה.
* הרשאות: **אפס שינוי.** הקריאה נשארת `group_rank(group_id) >= 1`, והמתכון
  נקרא דרך `can_read_recipe` כמו בכל מקום אחר. אם הפריט בוטל —
  `on delete set null` והכרטיס אומר "המתכון הוסר מהקבוצה".
* מחיקה רכה: אין בעיה — אין קובץ להרחיק, וההפניה עצמה אינה תוכן.

**עלות מוערכת: כ־2–3 שעות עבודה.** מיגרציה אחת קצרה, כרטיס תצוגה, בורר, בדיקות.

### מסלול ב׳ — צירוף תמונה (יקר יותר, וכאן הסיכון)
* **דלי נפרד**, לא `recipe-images`: `chat-images`, `public = false`, 2MB,
  `image/webp` בלבד. הסיבה שהוא נפרד: המדיניות של `recipe-images` נשענת על
  "המקטע הראשון בנתיב הוא recipe_id", וצירוף בצ׳אט אינו שייך למתכון. דחיסה
  לתוך הדלי הקיים תחייב לשבור את הכלל הזה — זה בדיוק סוג השינוי שמייצר חור.
* **נתיב**: `{group_id}/{author_id}/{uuid}.webp`. שני המקטעים הראשונים הם מה
  שהמדיניות שואלת עליו.
* **טבלה**: `public.group_message_attachments(id, message_id, storage_path unique,
  width, height, bytes, created_at)` — בדיוק מה ש־0032 הבטיחה.
* **RLS**: קריאה `group_rank(group_id) >= 1` **דרך ההודעה**; הוספה רק
  `author_id = auth.uid()` ודרגה ≥ 1; אין `update`, אין `delete` ללקוח.
* **הרחקה במחיקה רכה**: הטריגר `guard_message_update` הקיים מורחב — כשמסומן
  `deleted_at`, הנתיב עובר ל־`group_message_removals` (או לטבלה מקבילה) ו־RLS
  על `storage.objects` מפסיקה להתאים. **זו הנקודה שחייבת בדיקה אמיתית**, כי
  URL חתום שכבר הונפק ממשיך לעבוד עד שתוקפו יפוג — ולכן התוקף צריך להיות קצר
  (דקות, לא שעות).
* **המרה בצד הלקוח** — שימוש חוזר בנתיב ההמרה של `addRecipeImage`, שכבר מפיק
  WebP ומוחק EXIF.

**עלות מוערכת: כ־6–9 שעות עבודה**, מהן חצי על המחיקה הרכה והתוקף החתום.

---

## 3. מה חסר בהחלטה — ואני צריך תשובה לפני מימוש מסלול ב׳
1. **מי רשאי לצרף תמונה?** כל חבר (דרגה ≥ 1) או רק דרגה ≥ 2? בקורס עם
   תלמידים זה הפרש משמעותי.
2. **מה קורה לתמונה כשחבר עוזב או מוסר מהקבוצה?** התמונה נשארת בשיחה (כמו
   בווטסאפ) או נעלמת? זו החלטת מוצר, לא טכנית.
3. **מגבלת נפח לקבוצה.** 2MB להודעה בקורס עם 40 תלמידים זה גיגה־בייטים
   בחודשים. צריך תקרה, או מחיקה אוטומטית לפי גיל.
4. **תוקף ה־URL החתום** — כמה דקות.

## 4. ההמלצה שלי
לבצע את **מסלול א׳ (מתכון) קודם ובנפרד**: הוא זול, לא נוגע בהרשאות, לא מוסיף
דלי, ונותן את רוב הערך בקורס — שם מה שרוצים לצרף הוא מתכון. מסלול ב׳ (תמונה)
רק אחרי שארבע השאלות שלמעלה נענו, כי מחיקה רכה של קובץ היא מקום שקל לעשות בו
חור אמיתי.

**לא מבוצע דבר מהמסמך הזה עד אישור מפורש.**
