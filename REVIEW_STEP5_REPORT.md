# דוח שלב 5 — גרסאות מתכון (Versioning) ותת־מתכונים (Sub-Recipes)

קומיט: `eaa2f29` על `claude/recipe-notebook-initial-review-fwfb2e`
פרויקט Supabase: `qxdpsomelzpvphkhkqrw` (Recipe Notebook, eu-central-1)
מיגרציה חדשה: `0007_versions_and_sub_recipe_guards.sql` — **הוחלה בפועל**

---

## 0. הסריקה שקדמה לקוד, ושני פערים שהיא חשפה

לפני כתיבת קוד סרקתי את הקיים. זה מה שכבר עבד ולכן **לא** נבנה מחדש:

| מה | מצב לפני שלב 5 | מה נעשה בשלב 5 |
|---|---|---|
| טבלת `recipe_versions` (מיגרציה 0003) | קיימת, עם RLS | **לא** נוצרה מחדש. נוספו לה פונקציות שכותבות אליה |
| עמודת `ingredients.sub_recipe_id` (0002) | קיימת, `ON DELETE SET NULL` | **לא** שונתה. נוסף לה טריגר הגנה |
| חישוב תת־מתכון במנוע — משקל, עלות pro-rata, אלרגנים, provenance, full/partial/none | **מלא ותקין** | **אף שורה לא נגעה**. ה-UI רק מקשר |
| `compute()`, `calcState()`, `weakest()` | קיימים | נקראים כמו שהם, גם על snapshot |
| `SupabaseRepository`, `mappers.ts`, `AppDataProvider` | קיימים | הורחבו, לא נכתבו מחדש |
| `versionDiff` בפרוטוטייפ (שורה 1663) | קיים כמפרט | הועבר ל-TypeScript לפי §9 |

`git status packages/engine/src/` — **ריק**. המנוע לא שונה.

### פער 1 — השמירה לא הייתה אטומית, ו-§9 הופך את זה לבלתי נסבל

השמירה הקיימת הייתה **ארבע** קריאות נפרדות ל-PostgREST: `upsert` על
`recipes`, ואז `delete`+`insert` על `ingredients`, `steps`, `issues`.
זה עבד, אבל §9 דורש שלפני UPDATE יישמר snapshot של המצב הקודם. אם
ה-snapshot וה-UPDATE הם שתי קריאות client נפרדות, קיימים שני מצבים
שבירים אמיתיים:

* ה-snapshot נשמר, ה-UPDATE נכשל → בהיסטוריה יש גרסה שמתארת שינוי
  **שלא קרה**.
* ה-UPDATE הצליח, ה-snapshot נכשל → המצב הקודם **נעלם לבלי שוב**.

**PostgREST אינו יכול לפתוח transaction מרובה-הצהרות.** לכן אין סידור
של קריאות client שפותר את זה — זו לא בעיית תכנון client אלא מגבלת
פרוטוקול. הפתרון היחיד שמקיים דרישה 9 הוא **RPC אחד** בצד המסד.

זו בחירה שדרשה הצדקה, כי RPC יכול להיות דלת אחורית לעקיפת הרשאות. לכן
כל הפונקציות ב-0007 הן **SECURITY INVOKER** ולא DEFINER: הן רצות
בהרשאות הקורא, כל ה-RLS policies חלים עליהן בדיוק כמו על קריאה רגילה,
והן נותנות אטומיות **בלי** להוסיף שום סמכות. היחידה שהיא DEFINER היא
טריגר ההגנה על הקישור, ושם זה הכרחי — הוא חייב לראות את הבעלים של
**שני** המתכונים, ואת השני ה-RLS של הקורא מסתיר ממנו.

בונוס: אותו שינוי תיקן גם את אי-האטומיות שהייתה קיימת **לפני** שלב 5
בהחלפת שורות הבת.

### פער 2 — היה אפשר לקשר תת־מתכון של חשבון אחר

זה לא נמצא בקריאת קוד אלא **בבדיקה יזומה מול המסד**. ה-RLS על
`ingredients` בודק את **מתכון האב** (`owns_recipe(recipe_id)`), ובדיקת
מפתח זר (foreign key) ב-Postgres רצה **בעקיפת RLS**. התוצאה: אפשר היה
לכתוב ב-`sub_recipe_id` מזהה של מתכון של חשבון אחר, והמסד היה מקבל.

**שום נתון לא דלף** — המנוע מחפש את תת־המתכון ברשימה שה-RLS כבר סינן,
ולכן קישור כזה פשוט לא נפתר ונתן אזהרה. אבל דרישה 15 מבקשת **אי-אפשרות**,
לא חוסר-תועלת. `CHECK` אינו יכול להכיל שאילתת משנה, ולכן ההגנה היא
טריגר `check_sub_recipe_link` (SECURITY DEFINER) שדוחה:

| מצב | קוד שגיאה |
|---|---|
| הפניה לעצמו | `23514` (check_violation) |
| תת־מתכון שלא קיים | `23503` (foreign_key_violation) |
| תת־מתכון של חשבון אחר | `42501` (insufficient_privilege) |
| מעגל בכל עומק | `23514` |

זיהוי המעגל הוא `WITH RECURSIVE ... UNION` (ולא `UNION ALL`) — ה-`UNION`
הוא מה שמבטיח עצירה על גרף שכבר מכיל מעגל.

---

## 1. מה נבנה — דרישות 1 עד 9 (Versioning)

| # | דרישה | איפה |
|---|---|---|
| 1 | snapshot אמיתי של המצב הקודם לפני UPDATE | `save_recipe` — מסלול יצירה **לא** יוצר גרסה (אין מצב קודם לתאר) |
| 2 | הגרסה מכילה מספיק כדי לשחזר במלואו | `recipe_snapshot()` → `{recipe, ingredients, steps, issues}` |
| 3 | היסטוריית גרסאות ב-UI | `VersionHistory.tsx` — ציר זמן V1…Vn + "נוכחית" |
| 4 | זמן יצירה ומידע מזהה שימושי | תאריך ושעה, `what` מ-`versionDiff`, ותקציר "מה המתכון **היה**" |
| 5 | צפייה בגרסה לפני שחזור | `VersionViewer` — מריץ `compute()` ו-`calcState()` **אמיתיים** על ה-snapshot |
| 6 | שחזור | `restore_recipe_version()` |
| 7 | שחזור לא מוחק היסטוריה | השחזור שומר **קודם** את המצב הנוכחי כגרסה. שחזור שגוי הוא עצמו הפיך |
| 8 | RLS חוסם צפייה/יצירה/שחזור בגרסאות של אחר | נבדק מול Postgres, 11 בדיקות — §5 |
| 9 | אין מצב של UPDATE שהצליח ו-version שנכשל | RPC אחד, transaction אחד |

**מה ה-snapshot לא כולל, במכוון:** `recipe_versions` עצמה (אחרת V2 היה
מכיל את V1, V3 את שתיהן, והשורות היו גדלות בלי גבול), ו-`trials`/`batches`
— אלה **רשומות של אירועים** (§13/§13a), לא הנוסחה. שחזור נוסחה אינו
אמור למחוק יומן ניסיונות. השחזור גם **אינו** משחזר את `locked`, `id`,
`owner_id` ו-`created_at`.

**מקור אמת אחד:** `snapshotToRecipe()` הוא עטיפה דקה מעל `bundleToRecipe()`
— אותו mapper שקורא את השורות החיות. גרסה לא יכולה להתפרש אחרת מהשורות
שממנן נלקחה.

## 2. מה נבנה — דרישות 10 עד 17 (Sub-Recipes)

| # | דרישה | איפה |
|---|---|---|
| 10 | לבדוק תמיכה קיימת ולא לשכפל | המנוע פותר תת־מתכונים במלואו. **לא נוסף חישוב** |
| 11 | UI לקישור רכיב למתכון אחר | בורר `<select>` בכל שורת רכיב |
| 12 | רק מתכונים שמותר לראות | רשימת המועמדים **היא** המחברת שה-RLS סינן. אין מסלול קוד שיכול להציע אחר |
| 13 | מניעת הפניה לעצמו | הבורר **לא כולל** את המתכון הנערך. המסד דוחה `23514` |
| 14 | מניעת מעגלים, גם עקיפים | `wouldCreateCycle` בצד הלקוח + `WITH RECURSIVE` במסד |
| 15 | לא לקשר מתכון של אחר גם במזהה ידני | טריגר SECURITY DEFINER, `42501` |
| 16 | החישוב עובר במנוע הקיים | `compute()` בלבד. אין נוסחה ב-UI |
| 17 | שמירת ההשפעה על כמויות/עלות/תשואה/אלרגנים/full-partial-none | נבדק ב-`subRecipe.test.ts` |

הבורר **מציג** אפשרות שיוצרת מעגל ומשאיר אותה מושבתת עם הסיבה לידה
(`— הקישור הזה יוצר מעגל בין מתכונים`), במקום להעלים אותה. מתכון שנעלם
מרשימה נקרא כמו באג. היחיד שמושמט לגמרי הוא המתכון הנערך עצמו.

**הכפילות בין `subRecipe.ts` לטריגר מכוונת**, והיא מתועדת בראש הקובץ:
**הסמכות היא המסד**. הצד הלקוח קיים כדי שהבורר יציע רק אפשרויות תקפות
ויסביר את השאר — לא כדי להגן. שתי הגרסאות נבדקות באותם מקרים.

---

## 3. תקלות אמיתיות שנמצאו ותוקנו בשלב הזה

### 3.1 `versionDiff` מיזג שני רכיבים בעלי זהות זהה — תקלה פעילה

נמצא ע"י טסט ה-flow. `factsOf` בנה `Map` לפי `ingredientKeyOf` **בלבד**.
שום אילוץ לא מחייב ש-`ingredient_key` יהיה ייחודי בתוך מתכון, ומים
שנוספים בשני שלבים הם נוסחת מחמצת רגילה לחלוטין. שתי השורות נדרסו לאחת,
לשני הצדדים יצא מספר שורות שונה, והתוצאה הייתה התיאור **הסותר את עצמו**:

```
נוסף קמח מלא, מים · הוסר מים
```

תוקן: המפתח כולל כעת את **מספר המופע** (`key#1`, `key#2`), כך שהמופע
ה-n בצד אחד מושווה למופע ה-n בצד השני. וגם: `nameList` מסיר כפילויות,
אחרת "שונתה כמות: מים, מים" היה בולע שני מקומות מתוך שלושה ש-§9 מתיר.
4 טסטים חדשים ב-`versionDiff.test.ts`.

### 3.2 העורך שכתב בשקט את `ingredient_key` — תקלה סמויה

ל-`IngredientDraft` לא היה שדה ל-`ingredientKey`. לכן טעינת מתכון
**זרקה** אותו, והשמירה יצרה אותו מחדש מהשם (`mappers.ts:382`). זהו
המפתח שהמנוע משתמש בו להתאמת **כיול אישי** ו-**צפיפות** (B4).

**היקף אמיתי:** סמוי, לא פעיל. היום אין ב-UI שום מסלול שכותב מפתח שונה
מ-`normalizeName(name)` (`ingredient_catalog` קיימת בסכימה אבל בלי מימוש),
ולכן אף נתון של משתמש לא נפגע בפועל. עדיין תיקנתי — זה העורך שמשנה
נתון שמור בלי שביקשו ממנו.

הכלל שנבחר: המפתח שורד כל עריכה **חוץ** משינוי אמיתי בשם. עריכת כמות או
מחיר לא משנה מה השורה **היא**; הקלדה מחדש של השם היא המשתמש שאומר שזה
משהו אחר. השוואת השמות היא `normalizeName` של המנוע, כך שרווח או גרשיים
אינם "שינוי". 7 טסטים ב-`draft.test.ts`.

### 3.3 העורך הסתיר את הקישור לתת־מתכון גם בשורה שכבר מקושרת

נמצא ב-E2E בדפדפן אמיתי (Playwright סירב ללחוץ על אלמנט שאינו נראה).
הבורר יושב בתוך `<details>` בשם "פרטים נוספים ל…", והוא היה מכווץ **גם
בשורה שכבר מקושרת** — כלומר השדה בעל ההשפעה הגדולה ביותר על השורה (§18.6:
נשקלת, לא מומרת לנפח, והעלות מתגלגלת מהבסיס) היה גם הנסתר ביותר, ובמצב
מכווץ **לא היה שום סימן** שהשורה מקושרת בכלל.

תוקן: שורה מקושרת נפתחת כברירת מחדל, וה-summary שלה נושא את **שם** מתכון
הבסיס. 3 בדיקות E2E.

### 3.4 מציג הגרסה הדפיס את הגרמים פעמיים

`600 גר' 600 גר'` — רמז הגרמים המחושב הוצג גם בשורה שנכתבה כבר בגרמים.
מסך המתכון כבר החזיק את הכלל הנכון ("הרמז שווה הצגה רק כשהוא מוסיף
משהו"); המציג שוכפל בלעדיו. תוקן לאותו כלל.

### 3.5 `rls-isolation.sql` דיווח על 42 כשלונות שקריים

תקלת שיטה בסקריפט הבדיקה עצמו, והיא חמורה יותר מקריסה. ל-`DO` יש
`exception when others` כרשת ביטחון, ומטפל חריגות ב-plpgsql **מגלגל
אחורה את ה-subtransaction** — כולל כל `set_config(..., true)` שנעשה לפניו.
כך חריגה **אחת** לא הכשילה בדיקה אחת אלא **ריקנה את כולן**, וטבלת
התוצאות הציגה 42 שורות `FAIL` כולל אלה שעברו. זה נקרא כמו מסד שבור,
לא כמו סקריפט שבור.

תוקן: כל probe שעלול להרים חריגה עטוף ב-`begin/exception` משלו, והמלכודת
מתועדת בראש הסקריפט.

### 3.6 התנהגות שנחשפה ולא "תוקנה" בכוונה

`anon` **מסורב לגמרי** בקריאה מטבלת בת (`permission denied for function
owns_recipe`) במקום לקבל 0 שורות. הסיבה: מיגרציה 0006 שללה מ-`anon` את
ה-EXECUTE על `owns_recipe`, וכל policy של טבלת בת קוראת לה בשם. זה מצב
**הדוק יותר** מ-0 שורות (אף שורה לא נבחנת), והוא היה החלטה מנומקת. לכן
הבדיקה החדשה מאשרת את **הסירוב** ולא הרחבתי את ההרשאה כדי "לתקן" אסתטיקה.

---

## 4. שגיאות שיטה שלי, ולמה הן חשובות

| מה חשבתי | מה באמת | איך התגלה |
|---|---|---|
| ה-`updated_at` משתנה בין שתי שמירות באותו DO block | `now()` הוא זמן ה-**transaction** — שתי כתיבות באותו transaction מקבלות אותו ערך. הבדיקת concurrency דיווחה ACCEPTED בלי לבדוק כלום | הפרדה לשתי קריאות `execute_sql` → REFUSED, נכון |
| B מצליח ל-INSERT גרסה אצל A | ה-INSERT שלי השתמש בתת-שאילתה שה-RLS רוקן, אז **אפס שורות** נכתבו ושום דבר לא הרים חריגה | מזהה מילולי → REFUSED ע"י RLS |
| ה-GUC שהגדרתי שורד לקריאה הבאה | `set_config(..., true)` הוא transaction-local. בדיקת "מתכון שנמחק" בדקה **כלום** | נכתב מחדש עצמאי לחלוטין בתוך transaction אחד |
| `subRecipeOptions` צריך להציג את המתכון הנערך מושבת | בורר לא צריך להציע את מה שאתה עורך עכשיו | כתיבת הטסט |
| התשואה נעה עם `yieldUnits` | `yieldUnits` מזין את `unitsActual`, לא את `actualYield` | טסט ה-fallback של §9 |
| המשקל הכולל של 1020 גר' יוצג כ-"1,020 גר'" | הפורמטר עובר לק"ג מעל קילו: `1.02 ק"ג` | טסט ה-VersionViewer |

כולן תוקנו, וכולן בכיוון אחד: בדיקה שנראתה ירוקה בלי לבדוק דבר. זו
הסכנה האמיתית, לא כשלון גלוי.

---

## 5. הוכחות מול Postgres אמיתי

### 5.1 סבב מלא של גרסה ושחזור — 25 בדיקות, כולן עברו

`supabase/scripts/version-roundtrip.mts` + `npm run roundtrip:versions:verify`.
שתי פאזות: המסלול האמיתי (draft → Recipe → rows) מייצר SQL ו-digest; ה-SQL
רץ מול הפרויקט; ומה שחוזר עובר ב-`snapshotToRecipe`/`bundleToRecipe`
האמיתיים והושווה ל-digest.

למה זה נחוץ בנפרד מ-`mapper-roundtrip`: `recipe_snapshot` בונה מסמך
`jsonb` **בתוך** המסד מ-`to_jsonb(row)`. מה ש-Postgres שם שם אינו מה
ש-JavaScript היה מסדר — `numeric` , `NULL` שקיים-והוא-null במקום חסר,
`timestamptz` שמעובד ע"י Postgres. דרישה 2 מחייבת שהמסמך **ישחזר** את
המתכון, וזה נבדק ולא הוערך בעין.

```
ok   the update took exactly one version, and the create took none
ok   it is tagged V1
ok   the snapshot reconstructs the pre-edit recipe exactly
ok   the live row still holds the NEW quantity, so the two are not confused
ok   restoring V1 reproduces the pre-edit recipe exactly
ok   the restore added a version rather than consuming one
ok   and V1 is still there
ok   snapshot: an untouched measured yield is ABSENT, not 0
ok   snapshot: a measured water percentage of 0 is 0
ok   snapshot: an unset water percentage is absent
ok   snapshot: a price of 0 is 0, not missing
ok   snapshot: an unpriced ingredient has no price
ok   snapshot: the pre-edit quantity is the one preserved
ok   snapshot: the tags survived as an array of two
ok   snapshot: the gershayim and percent in the text survived
ok   snapshot: the ingredient order survived
ok   restore: ... (אותן תשע בדיקות, על השורות החיות אחרי השחזור)

VERSION ROUND TRIP OK — digest 336413b2271fe80a0facae692fa3d02fd26a1f2683999b4b5381a43dca6971cb
```

`null ≠ 0` ו-`price 0 ≠ אין מחיר` שרדו **גם** את ה-snapshot **וגם** את
השחזור. שתי הבדיקות האלה הן הליבה של מודל הנתונים כולו.

### 5.2 בידוד RLS — 42 בדיקות, כולן עברו, הניקוי מאומת

`supabase/tests/rls-isolation.sql`, הורחב בשלב 5. הסקריפט מחליף `role`
ל-`authenticated` ומגדיר `request.jwt.claims` — **בדיוק** מה ש-PostgREST
עושה. זה מסלול האכיפה, לא סימולציה שלו.

```
passed: 42   failed: 0   probe_status: probes complete
```

הבדיקות החדשות (דרישות 8 ו-15):

| תחום | בדיקה | תוצאה |
|---|---|---|
| `recipe_versions` | A, בשאילתה ללא סינון, רואה רק את הגרסה של עצמו | 1 |
| `recipe_versions` | A שמסנן לפי `recipe_id` של B — לא מקבל כלום | 0 |
| `recipe_versions` | A לא יכול לקרוא את **הטקסט** של הגרסה של B | (none) |
| `recipe_versions` | A לא יכול לשתול גרסה בהיסטוריה של B | refused |
| `recipe_versions` | UPDATE של A על גרסה של B — אפס שורות | 0 |
| `recipe_versions` | DELETE של A על גרסה של B — אפס שורות | 0 |
| `recipe_versions` | A לא יכול לשחזר גרסה של B דרך ה-RPC | refused |
| `recipe_versions` | A כן קורא את הגרסה של עצמו | 1 |
| `recipe_versions` | B, בכיוון ההפוך, לא רואה את הגרסה של A | 0 |
| `anon` | מסורב לגמרי מטבלת הגרסאות | refused: 42501 |
| `sub_recipe_id` | A לא יכול ל-INSERT קישור למתכון של B, גם במזהה ידני | refused: 42501 |
| `sub_recipe_id` | A לא יכול ב-UPDATE להפוך שורה קיימת לקישור למתכון של B | refused: 42501 |
| `sub_recipe_id` | הפניה לעצמו נדחית כ-check violation | refused: 23514 |
| `sub_recipe_id` | ואף אחד מהניסיונות לא השאיר קישור | 0 |

ה-UPDATE נבדק בנפרד מה-INSERT בכוונה: טריגר שהוגדר ל-INSERT בלבד היה
מחמיץ אותו.

### 5.3 אטומיות ומעגלים — נבדקו ישירות

| מה נבדק | תוצאה |
|---|---|
| יצירה לא יוצרת גרסה | 0 גרסאות |
| V1 מחזיק את המצב שלפני העריכה | 420 גר' בעוד השורה החיה 450 |
| ה-snapshot אינו מקנן גרסאות | מפתחות: `ingredients, issues, recipe, steps` בלבד |
| כשל **בתוך** ה-RPC (קישור חוצה-חשבונות אחרי שכבר נכתבו שורת הגרסה וה-UPDATE של האב) | `fully_rolled_back: true` |
| טוקן `updated_at` מיושן, בשתי transactions נפרדות | REFUSED; טוקן עדכני — ACCEPTED |
| הפניה לעצמו / מעגל ישיר / מעגל בעומק 3 | כולם REFUSED |
| מחיקת מתכון בסיס | הקישור התלוי הופך ל-NULL, ו-**גרסה ישנה של המתכון התלוי אינה ניתנת לשחזור** — סורבה בנקיות, שום דבר לא נכתב חצי |

### 5.4 המסד נשאר נקי

לאחר כל הבדיקות:
```
recipes 0 · ingredients 0 · steps 0 · issues 0 · recipe_versions 0
profiles 0 · auth.users 0 · calibrations 0 · private_notes 0 · trials 0 · batches 0
density_table 34   (נתוני ייחוס מקצועיים — אמורים להישאר)
```
**אין fixtures שנשארו במסד.**

---

## 6. טסטים — 579 עוברים

| חבילה | קבצים | טסטים |
|---|---|---|
| `packages/engine` | 7 | 180 |
| `apps/web` | 23 | 399 |

חדשים בשלב 5:

| קובץ | טסטים | מה הוא מכסה |
|---|---|---|
| `data/versions.test.ts` | 29 | ה-repository: יצירה ללא גרסה, snapshot, שחזור, טוקן מיושן, כשלים |
| `features/recipe/subRecipe.test.ts` | 25 | מעגלים, הפניה לעצמו, הבורר, ו-**השפעת תת־המתכון דרך המנוע** |
| `features/recipe/versionDiff.test.ts` | 21 | §9 סעיף-סעיף, שתי הסטיות מהפרוטוטייפ, וזהות משותפת (3.1) |
| `features/recipe/VersionHistory.test.tsx` | 20 | ציר הזמן, צפייה, אישור שחזור, נעול/שגיאה/עסוק, snapshot פגום |
| `app/VersionFlow.test.tsx` | 14 | **כל המסלול** דרך עץ הרכיבים האמיתי |
| `features/recipe/draft.test.ts` | +7 | שרידות `ingredient_key` (3.2) |

לא הסתפקתי ב-happy path. מכוסה במפורש: כשל ביצירת גרסה, כשל ב-UPDATE,
שחזור, שחזור של גרסה **ישנה** אחרי כמה שמירות, שחזור **חוזר** על אותה
גרסה, concurrency, מתכון שנמחק, תת־מתכון שנמחק, הפניה לעצמו, מעגל ישיר,
מעגל עקיף (עד עומק 5), מזהה לא מורשה, תמחור חסר/חלקי, ו-null/0 בסבב מלא.

`VersionFlow.test.tsx` בודק דבר שבדיקת יחידה לא יכולה: ששחזור מוסיף
גרסה ולא צורך אחת, ושאחרי שחזור חוזר עדיין יש `V1, V2, V3, V4` — כלומר
**שום גרסה לא נמחקה בדרך**.

---

## 7. E2E בדפדפן — 50 בדיקות (22 + 28)

`node apps/web/e2e/stage4.mjs` → 22/22 · `node apps/web/e2e/stage5.mjs` → 28/28

הסקריפט החדש בודק את מה שמצב ה-demo **באמת** יכול להוכיח בדפדפן אמיתי:

* פאנל הגרסאות עולה במסך המתכון, בטלפון ובטאבלט
* המצב הריק **אמיתי ולא placeholder** — "אין עוד היסטוריה" ולא "אין
  גרסאות עדיין", כי בלי שרת אין היסטוריה **בכלל** ולא "עדיין"
* השחזור מושבת עם סיבה ("אין כרגע חיבור"), ולא מוסתר
* בורר תת־המתכון: 8 בוררים, 8 שמות נגישים שונים, כולם ≥40px
* עריכת הגנאש **אינה** מציעה את הגנאש עצמו (13)
* בריוש השוקולד (שצורך את הגנאש) **מוצע ומושבת** עם הסיבה "מעגל" (14)
* Chromium **מסרב** לבחור אפשרות מושבתת — כלומר הבחירה הפסולה אינה רק
  מיואשת, היא בלתי-נגישה למצביע
* 0px גלילה אופקית בטלפון (402px) ובטאבלט (1024px)
* אפס שגיאות דף, ואפס פניות למקור חיצוני

---

## 8. מגבלות — במפורש

### 8.1 אין E2E אמיתי מול PostgREST/GoTrue. מגבלת ה-egress עוד בתוקף.

מדיניות היציאה של הסביבה **חוסמת `*.supabase.co`**. לכן שום טסט בקוד הזה
אינו פותח חיבור HTTP לפרויקט. ה-E2E בדפדפן רץ על build שנבנה עם משתני
הסביבה של Supabase **ריקים** (מצב demo, repository לקריאה בלבד).

**ה-E2E במצב demo אינו הוכחה למסלול HTTP אמיתי, ואני לא מציג אותו ככזה.**
פיצול העדויות:

| מה | מה זה מוכיח | מה זה **לא** מוכיח |
|---|---|---|
| `VersionFlow.test.tsx` | כל המסלול דרך הרכיבים, ה-mappers וה-repository האמיתיים | את קפיצת ה-HTTP; ואת ה-**אטומיות** — הכפיל הוא JavaScript ואינו יכול לגלגל אחורה |
| `version-roundtrip` | ה-snapshot והשחזור מול Postgres אמיתי, כולל טיפוסי עמודות ו-null/0 | את שכבת PostgREST מעל |
| `rls-isolation.sql` | דרישות 8 ו-15 מול Postgres, כ-`authenticated` וכ-`anon` | HTTP |
| E2E demo | ה-UI החדש במנוע דפדפן אמיתי | שום דבר שנוגע לשרת |

הגבול הנבדק ב-RLS הוא אותו גבול בשני המקרים — RLS נאכף ב-Postgres, לא
ב-PostgREST. אבל את ה-JWT של GoTrue ואת שכבת ה-HTTP לא בדקתי, ואי אפשר
לבדוק אותן מכאן.

### 8.2 גרסה שמפנה למתכון בסיס שנמחק אינה ניתנת לשחזור

מגבלה אמיתית, לא באג. `sub_recipe_id` הוא `ON DELETE SET NULL`. אם מחקת
מתכון בסיס, גרסאות **ישנות** של מתכונים שהשתמשו בו מכילות snapshot שמפנה
למזהה שלא קיים. השחזור **מסרב** ליצור קישור תלוי באוויר, ומתגלגל אחורה
בנקיות — שום דבר לא נכתב חצי.

הפחתת נזק: אישור המחיקה מונה בשמם את המתכונים שמשתמשים בזה שנמחק
(`recipes_using()`), ומזהיר על זה במפורש. פתרון אמיתי ידרוש החלטת מוצר
שלא קיבלתי: להקפיא עותק של הבסיס בתוך ה-snapshot (מקור אמת שני — נגד
ההנחיה), או לחסום מחיקה של מתכון שיש לו תלויות (שינוי התנהגות שלא בוקש).

### 8.3 מה שלא נבנה

* **מסך השוואה בין שתי גרסאות שרירותיות** — §9 מזכיר אותו. `versionDiff`
  קיים ועובד על כל שני מצבים, אבל ה-UI מציג רק תיאור לגרסה ומציג
  לגרסה בודדת. אין מסך "V1 מול V3".
* **גילוי (discoverability) של בורר תת־המתכון** — הוא יושב בתוך
  "פרטים נוספים" של השורה. שורה **מקושרת** כבר לא מסתירה את זה (3.3),
  אבל **יצירת** קישור חדש עדיין דורשת לפתוח disclosure. תיקון ראוי הוא
  שינוי מבנה הטופס, והחלטתי לא להרחיב את שלב 5 לשם.
* **`ingredient_catalog`** — קיימת בסכימה, בלי UI. `ingredient_key`
  עכשיו שורד את העורך (3.2), כך שכשיהיה קטלוג הוא לא ישכתב נתונים.
* **שחזור חלקי** (רק הרכיבים, רק השלבים) — לא בוקש.

---

## 9. מה נשמר, ונבדק שנשמר

| התחייבות | מצב |
|---|---|
| `null ≠ 0` | נבדק ב-snapshot, בשחזור, ב-mappers, ב-draft ומול Postgres |
| `price 0 ≠ אין מחיר` | נבדק מול Postgres; ושורת תת־מתכון ללא מחיר **אינה** מסומנת כחסרת מחיר |
| RLS ובידוד משתמשים | 42/42 מול Postgres |
| Auth | לא נגעתי |
| `SupabaseRepository` | הורחב, לא נכתב מחדש |
| responsive בטלפון ובטאבלט | 0px גלילה אופקית בשניהם |
| accessibility | שמות נגישים ייחודיים לכל גרסה ולכל בורר; `role="alertdialog"` לאישור השחזור |
| **הפרוטוטייפ המקורי** | `git status design_handoff_recipe_notebook/` — **ריק** |
| **`packages/engine/src`** | `git status` — **ריק**. אף שורה לא שונתה |
| **Presenzo Production** (`fglnzrpuffswapjsnppg`) | **לא נגעתי בו בשום צורה**. אין שאילתה, אין מיגרציה, אין קריאה |
| **WhatsApp AI Assistant** (`tbkcxrpenmpcwjicnxnd`) | **INACTIVE**, כפי שהיה. לא הופעל, לא נמחק, לא שונה. נשאר בחשבון לשחזור עתידי |
| `service_role` בדפדפן | אין. רק המפתח הציבורי, ו-`looksLikeServiceRoleKey` עוד שומר |

---

## 10. סיכום הרצות

```
npm test              180 engine + 399 web = 579 עוברים
npm run typecheck     נקי
npm run build         נקי
npm run schema:check  database.types.ts תואם לסכימה: 13 טבלות, 136 עמודות
npm run seed:check    0005_density_seed.sql מעודכן (34 שורות)
rls-isolation.sql     42/42, probes complete, הניקוי מאומת
roundtrip:versions    25/25, digest תואם
e2e stage4            22/22
e2e stage5            28/28
```
