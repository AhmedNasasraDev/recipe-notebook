# דוח שלב 6 — חסימת מחיקה של מתכון בשימוש, והשוואת גרסאות מלאה

קומיט: `81d5fb0` על `claude/recipe-notebook-initial-review-fwfb2e`
פרויקט Supabase: `qxdpsomelzpvphkhkqrw` (Recipe Notebook, eu-central-1)
מיגרציות חדשות: `0008`, `0009`, `0010` — **כולן הוחלו בפועל**

---

## 0. הסריקה שקדמה לקוד

| מה | מצב לפני שלב 6 | מה נעשה |
|---|---|---|
| `ingredients.sub_recipe_id` | `ON DELETE SET NULL` | **שונה** ל-`NO ACTION DEFERRABLE INITIALLY DEFERRED` (0008) |
| `recipes_using()` (0007) | קיימת, SECURITY INVOKER, מסוננת ב-RLS | **נותרה כמו שהיא**. היא כבר בדיוק מה שדרישה 3 צריכה |
| `check_sub_recipe_link` (0007) | חוסמת self-reference, מעגלים וחשבון אחר | **לא שונתה**. היא גם מה שדרישה 6 נשענת עליו |
| `versionDiff` | מחזירה **מחרוזת** אחת | הופכה מבפנים: `compareRecipes()` עושה את העבודה, `versionDiff()` הוא פורמטר עליה |
| חישוב תת־מתכון במנוע | מלא — משקל, עלות pro-rata, אלרגנים, provenance | **אף שורה לא נגעה**. אין נוסחה חדשה ב-UI (דרישה 19) |
| `calcState` (`completeness.ts`) | קרא **רק את הרמה העליונה** | **תוקן** — ראו §3.1. זהו קוד ה-web, לא המנוע |
| `SupabaseRepository.deleteRecipe` | `.delete()` ישיר על הטבלה | עובר דרך `delete_recipe` וממפה 23503 |

`git status packages/engine/src/` ו-`design_handoff_recipe_notebook/` — **שניהם ריקים**.

---

## 1. חסימת מחיקה — דרישות 1 עד 6

### 1.1 למה `NO ACTION DEFERRABLE INITIALLY DEFERRED` ולא `RESTRICT`

זו ההחלטה המרכזית של השלב, והיא הוכרעה ב**בדיקה מול המסד החי ולא בקריאת
תיעוד**. שתי התשובות הראשונות שלי היו שגויות:

| מה ניסיתי | מחיקה של מתכון בשימוש | מחיקת **חשבון** |
|---|---|---|
| `ON DELETE RESTRICT` | נחסמה ✓ | **נחסמה גם** ✗ |
| `ON DELETE NO ACTION` (לא deferrable) | נחסמה ✓ | **נחסמה גם** ✗ |
| `NO ACTION DEFERRABLE INITIALLY DEFERRED` | נחסמה ✓ | **עברה בהצלחה** ✓ |

הסיבה: `recipes.owner_id` הוא `ON DELETE CASCADE` מ-`auth.users`. מחיקת
משתמש מוחקת את המתכונים שלו, ו-`RESTRICT` נבדק שורה-שורה ומתפוצץ על מתכון
הבסיס גם כשהשורה שמפנה אליו עומדת להיעלם באותה הצהרה. `NO ACTION` ללא
deferral נבדק **בסוף ההצהרה המפעילה**, וה-cascade אל `ingredients` קורה
בהצהרה מקוננת — כך שהשורה המפנה עוד שם כשהבדיקה רצה. **תוצאת הבדיקה:
מחיקת החשבון נכשלה ב-23503.**

כלומר: משתמש שאי פעם קישר תת־מתכון לא היה יכול להימחק. זה לא פגם תיאורטי.

ה-deferral **אינו הקלה** — הוא מה שמפריד בין "למחוק את המתכון הזה" (מסורב)
ל"החשבון הזה נמחק" (מותר), וזו בדיוק ההבחנה שהחלטת המוצר צריכה.

### 1.2 למה אין snapshot ואין מקור אמת שני

לא נוצר שום עותק של מתכון הבסיס, לא נוספה עמודה, ולא נוצר טבלה חדשה.
המפתח הזר **הוא** האכיפה. הוא חל על `DELETE /recipes?id=eq.x` ישר מה-anon
key, על קריאה מה-repository, ועל קריאה מתוך כל אחת מפונקציות 0007 — אין
מסלול סביבו, וזה מה שדרישה 5 מבקשת.

### 1.3 מה `delete_recipe` (0009) מוסיף, ומה לא

**לא** מוסיף שום הבטחה. מתקן שני דברים בחוויית הפגישה עם ההבטחה:

1. `DEFERRED` אומר שההפרה נזרקת ב-**COMMIT**, לא ב-DELETE. אצל PostgREST
   ה-commit הוא בסוף הבקשה, ו**מגבלת ה-egress מונעת ממני לבדוק ב-HTTP איך
   זה מגיע לקליינט**. `SET CONSTRAINTS ... IMMEDIATE` בסוף הפונקציה מריץ
   את הבדיקה בתוך הקריאה, כך שהסירוב מגיע כשגיאה רגילה עם קוד לענות עליו.
2. הודעת FK גולמית היא `update or delete on table "recipes" violates
   foreign key constraint...` — הדבר הנכון להרים והדבר הלא-נכון להראות
   לקונדיטור.

### 1.4 דרישה 6 — למה זה לא יכול להפוך ל-oracle על חשבון אחר

מפתח זר נאכף ע"י המערכת ו**אינו כפוף ל-RLS**, ולכן בעקרון "המחיקה שלך
סורבה" יכול להיות "מתכון שאינך רואה מפנה לזה". זה לא קורה, בגלל **אינvariant
ולא בגלל policy**: `check_sub_recipe_link` דוחה כל קישור שבו לבעלים של
האב ושל התת־מתכון זהות שונה. לכן כל חוסם אפשרי הוא מתכון של אותו חשבון,
ו-`recipes_using()` — SECURITY INVOKER, כלומר מסוננת ב-RLS — מראה למשתמש
את כולם.

**מיגרציה 0008 מאמתת את האינווריאנט לפני שהיא מתקינה את החוסם**: אם היה
נשאר קישור חוצה-חשבונות מלפני 0007, המיגרציה נכשלת בקול ולא מתקינה דלת אחורית.

והמספר בהודעת השגיאה בא מ-`recipes_using()`, שיכולה לספור רק מה שהקורא כבר
רשאי לראות — כלומר הוא חסין-דליפה **מעצם הבנייה** ולא מהסתמכות על האינווריאנט.

### 1.5 ה-UI

| דרישה | מה יש |
|---|---|
| 3 — להסביר למה | הדיאלוג אומר "אי אפשר למחוק את X", לא "האם אתה בטוח" |
| 3 — לציין אילו מתכונים | רשימה עם שם כל מתכון, **כקישור לעורך שלו** — כי הסרת הקישור היא הדרך היחידה קדימה |
| 3 — "ככל שהמשתמש מורשה לראות" | הרשימה מגיעה מ-`recipes_using()`, מסוננת ב-RLS |
| — | אין כלל כפתור אישור מחיקה במצב הזה |

הדיאלוג גם מאמץ את התשובה של המסד: אם טאב אחר הוסיף תלות בין הטעינה
לאישור, המחיקה מסורבת וה**דיאלוג הופך להסבר** עם התלויות שהסירוב הגיע איתן,
במקום "המחיקה נכשלה" גנרי. יש טסט לזה.

---

## 2. השוואת גרסאות — דרישות 7 עד 14

### 2.1 דרישה 14 — מקור אמת אחד, ולכן `versionDiff` הפכה מבפנים

`versionDiff` החזירה מחרוזת אחת. מסך ההשוואה צריך את הפירוט שממנו המחרוזת
חושבה ואז נזרק. **לחשב אותם בנפרד זה בדיוק מקור האמת השני שההנחיה שוללת** —
המחרוזת השמורה עם הגרסה והמסך היו יכולים לחלוק על אותן שתי גרסאות.

לכן: `compareRecipes()` עושה את העבודה ומחזירה מבנה, ו-`versionDiff()` הוא
פורמטר על `summary` שלה. הנוסח של §9, סדר הסעיפים, תקרת שלושת השמות
ו-fallback התשואה — **כולם לא שונו**, וכל 21 הטסטים של §9 עוברים כמו שהם.

### 2.2 מה מושווה (דרישה 9)

**24 שדות מתכון** — שם, קטגוריה, מתכון בסיס, נעילה, יחידות, משקל ליחידה,
תשואה מעשית, משקלים לפני/אחרי, מצב בצק, DDT, טמפרטורות, חיכוך, יעד פוד
קוסט, תגיות, חיי מדף, אחסון, הקפאה, הפשרה, ציוד, הערות, אלרגנים ידניים,
הערת גרסה.

**11 שדות בכל רכיב** — כמות, יחידה, מחיר, יחידת מחיר, אחוז מים, צפיפות,
משקל ליחידה, קמח, נוזל, מתכון בסיס, הערה.

מה **הושמט במכוון**: `id`, `createdAt`, `versions`, `versionOf`, `savedFrom`
(זיהוי ורישום, לא הנוסחה); `trials`, `batches`, `issues` (רשומות אירועים,
ה-snapshot לא נושא אותן); `privateNotes` (§8 — לא עוזבות את המסך של הבעלים).

### 2.3 דרישות 11 ו-12 — null ≠ 0, ואין מחיר ≠ מחיר 0

`CellValue` מבדיל `null` (חסר) מ-`0`. `formatCell` הוא המקום **היחיד** שבו
ערך הופך לטקסט, והוא מציג חסר כ-`—` ולא כתא ריק: **תא ריק ליד 0 נקרא כאותו
דבר**. ובשם הנגיש כל שורה היא הכרזה אחת — `"מחיר, לפני ריק, אחרי 0"` —
כי `—` אינו מילה.

| מצב | לפני | אחרי | מדווח? |
|---|---|---|---|
| חסר → 0 | `—` | `0` | **כן** |
| 0 → חסר | `0` | `—` | **כן** |
| `500` → `'500'` | | | **לא** — אותה מדידה, הטופס מחזיק מחרוזות והמסד מספרים |
| `undefined` → `''` | | | **לא** — שניהם "לא נכתב כלום" |

### 2.4 דרישה 10 — רכיבים כפולים חוקיים

המפתח הוא זהות **ועוד** מספר המופע (`water#1`, `water#2`). שינוי במופע השני
מדווח כמופע השני בלבד, והוא מוצג כ-`מים (2)` כשיש יותר מאחד — "מים" פעמיים
בלי דרך להבדיל הוא חסר תועלת.

### 2.5 דרישה 13 — טלפון וטאבלט

טבלה עם שתי עמודות ערכים לא נכנסת ב-402px. לכן כל שינוי הוא בלוק אחד עם
שני הערכים מוערמים וחץ ביניהם, שנהפך לשורה אחת מעל 600px (container query על
`app`). הערך הקודם מסומן בקו חוצה ולא רק באפור — **צבע לבדו אינו הבחנה**.
0px גלילה אופקית בשני הרחבים, נבדק בדפדפן.

---

## 3. תקלות אמיתיות שנמצאו ותוקנו

### 3.1 `calcState` הציג חישוב שלם כשלא היה — התקלה החמורה בשלב הזה

נמצאה ע"י בדיקת שרשרת בת שלוש רמות (דרישה 18), והיא **תקלה אמיתית ולא טסט
חסר**. כל הפונקציות ב-`completeness.ts` קראו `computed.rows` ו-
`computed.unresolved` — **הרמה העליונה בלבד**. לשורת תת־מתכון יש משקל משלה
ועלות שהתגלגלה, ולכן היא נראתה שלמה בלי קשר למה שקרה בתוכה.

מה שנמצא בפועל, מול המנוע:

| התרחיש | לפני התיקון | אחרי |
|---|---|---|
| קקאו בכוס (בלי צפיפות אמינה) בתוך הגנאש → מילוי → עוגה | הגנאש `partial`, המילוי **`full`**, העוגה **`full`** | שלושתם `partial` |
| שוקולד **בלי מחיר** בתוך הגנאש, שתי רמות מתחת לעוגה | הגנאש `partial`, המילוי **`full`**, העוגה **`full`** + עלות מוגמרת | שלושתם `partial` |
| מתכון בסיס חסר באמצע השרשרת | העוגה `costLevel: full` | `partial` |

הפגם השני הוא **תקלה שעולה כסף**: קונדיטור רואה עלות מוגמרת לעוגה שהשוקולד
בה, שתי רמות מתחת, חושב באפס. זו אותה תקלה של "עלות כוללת ₪0" משלב 4,
שחזרה מעבר לגבול תת־המתכון.

**המנוע היה צודק לחלוטין.** כל רמה מדווחת נכון על עצמה, ו-`ComputedRow.sub`
נושא את כל העץ. מה שחסר היה שהשאלה "האם הנתון הזה שלם" היא שאלה על **העץ**
ולא על רמה אחת — ושם היא חיה, ב-`completeness.ts`. **לכן לא נדרש שום שינוי
ב-`packages/engine/src`.**

התיקון גם מדווח את **הנתיב** ולא רק את השם: `מילוי → גנאש → שוקולד מריר`.
רכיב שהקורא לא יכול למצוא אינו הסבר.

### 3.2 בדיקת ה-round-trip של שלב 4 הייתה **שבורה מאז שלב 5**

`npm run roundtrip:verify` נכשל:
```
updatedAt:
  expected undefined
  actual   "2026-09-16T04:20:59.277488+00:00"
```
שלב 5 הוסיף `updatedAt` ל-`bundleToRecipe` בתור טוקן ה-concurrency, אבל ה-
`digest()` של הסקריפט משמיט רק `id` ו-`createdAt`. **זה חמק כי בשלב 5
הרצתי את ה-round-trip של הגרסאות ולא את זה של שלב 4.** תוקן, ושניהם
בצ'ק-ליסט הסיום עכשיו.

### 3.3 שש פונקציות משלב 5 היו קריאות ל-`anon`

נמצא בעת צילום חתימות הפונקציות ל-`schema:check`. `CREATE FUNCTION` מעניק
EXECUTE ל-PUBLIC כברירת מחדל ו-Supabase מוסיף `anon` — ומיגרציה 0006 קבעה
את הכלל בדיוק בשביל זה, ואז 0007 לא עקבה אחריו. `/rest/v1/rpc/save_recipe`,
`restore_recipe_version`, `recipe_snapshot`, `next_version_tag`,
`recipes_using` ו-`replace_recipe_children` ענו לקורא לא-מזוהה.

**האם היה ניתן לנצל?** לא, וזה **נבדק ולא הונח**: כל אחת מהשש נקראה כ-`anon`
מול המסד החי וכולן סורבו ב-42501. כולן SECURITY INVOKER, הכתיבות נכשלות
ב-RLS (`owner_id = auth.uid()` אינו אמת כש-`auth.uid()` הוא null), והקריאות
נופלות ב-policies של טבלאות הבת שקוראות ל-`owns_recipe` — ואת זו 0006 כבר
שללה מ-anon.

תוקן ב-0010 בכל זאת, בנימוק של 0006 עצמה: **"אין מסלול פנימה" היא תכונה
טובה בהרבה מ"המסלול פנימה נכשל במקרה"**. שש נקודות קצה שעונות בכלל לקורא
לא-מזוהה הן שש מקומות שבהם שינוי עתידי ב-policy, ב-grant או בגוף פונקציה
הופך סירוב לחור.

### 3.4 ה-repository של הדמו שיקר לגבי תלויות

`recipesUsing()` החזירה `[]` עם הערה "לסט הדמו אין קישורי תת־מתכון". **יש
לו**: בריוש השוקולד משתמש בגנאש. מאז שלב 6 זו לא אזהרה חסרה אלא **תשובה
שגויה לשאלה "למה אני לא יכול למחוק את זה?"**. תוקן, עם 3 טסטים.

### 3.5 container query עם שם קונטיינר שלא קיים

כתבתי `@container frame (...)` בעוד ה-shell מגדיר `container-name: app`.
שם קונטיינר לא מוכר גורם לשאילתה **לא להתקיים לעולם** — פריסת הטאבלט של
ההשוואה הייתה מתה, בשקט, בלי שגיאה בשום מקום. תוקן ל-`app (min-width: 600px)`,
אותו breakpoint שהעורך כבר משתמש בו.

### 3.6 `schema:check` לא כיסה פונקציות

עכשיו כן, ומשתי סיבות שאינן על טיפוסים:

1. **PostgREST מעביר ארגומנטי RPC לפי שם.** שם ארגומנט שהשתנה אינו שגיאת
   טיפוס בשום מקום — הוא "function not found" בזמן ריצה, מקליינט שכל
   הטיפוסים שלו תקינים.
2. **ההרשאות.** grant שהתרופף צריך להיות בדיקה נכשלת, לא משהו שמגלים
   אחרי חודשים.

הבדיקה גם נופלת על פונקציה SECURITY DEFINER שנחשפת ל-role של קליינט.
**שלושת המצבים נבדקו negative-test** ונכשלו כמצופה:
```
delete_recipe(): argument names differ — database has (p_recipe_id), types declare (recipe_id)
save_recipe(): callable by `anon`
check_sub_recipe_link(): SECURITY DEFINER and directly callable by a client role
```

---

## 4. שגיאות שיטה שלי

| מה חשבתי | מה באמת | איך התגלה |
|---|---|---|
| `NO ACTION` יאפשר את ה-cascade של מחיקת חשבון | לא. נבדק ונחסם ב-23503 | הבדיקה עצמה, לפני הכתיבה |
| הבדיקה שלי מראה ש-A מחק את המתכון של B | ה-`exists` רץ כ-A, ו-RLS הסתיר את השורה — כך ששורה שלמה נראתה מחוקה. **האשמתי פונקציה תקינה במחיקת נתונים של חשבון אחר** | אימות כ-`postgres`, שעוקף RLS |
| `set constraints ... immediate` תקף להצהרה | תקף ל**טרנזקציה** כולה. probe אחד השאיר אותו immediate והבא דיווח שמחיקת חשבון חסומה | הרצה מחדש בטרנזקציה נקייה |
| מים#2 עבר מ-100 ל-300 | עבר מ-250 ל-300 | הטסט |
| `row.sub.unresolved` יראה פער שתי רמות מתחת | `sub` הוא ה-`Computed` של הרמה **הבאה**; הפער נמצא רמה אחת עמוק יותר | הטסט של השרשרת |
| מתכון בסיס חסר אמצע-שרשרת הופך את המשקל ל-לא-שלם | המשקל **שלם ונכון** — שורת תת־מתכון נשקלת כפי שנכתבה (§18.6), 150 גר' הם 150 גר'. ה**עלות** היא מה ששבור | אותו טסט |
| `::numeric::text` ישווה מספרים בלי scale | שומר את ה-scale (`420.000`). צריך `trim_scale` | 6 "כשלונות" שקריים |

כולן תוקנו. כולן באותו כיוון — **בדיקה שנראית תקינה בלי לבדוק דבר**, או
בדיקה שמאשימה קוד תקין.

---

## 5. הוכחות מול Postgres אמיתי

### 5.1 חוסם המחיקה — 17 בדיקות, כולן עברו

`supabase/tests/delete-guard.sql`. הורץ פעמיים: אחרי 0008+0009, ושוב אחרי 0010.

| תחום | בדיקה | תוצאה |
|---|---|---|
| DELETE גולמי | מתכון בשימוש אינו נמחק | blocked: 23503 |
| DELETE גולמי | והוא עוד שם אחרי כן | 1 |
| DELETE גולמי | הקישור התלוי **לא** אופס בשקט (ההתנהגות הישנה) | 1 |
| `recipes_using` | מונה את התלוי בשמו | עוגה עליונה |
| `delete_recipe` | מסרב בתוך הקריאה | blocked: 23503 |
| `delete_recipe` | וההודעה נושאת מספר, **לא שמות** | count only |
| `delete_recipe` | מתכון שאף אחד לא משתמש בו נמחק רגיל | deleted |
| דרישה 4 | לאחר הסרת הקישור המחיקה עוברת | deleted |
| דרישה 6 | מתכון של B **לא מרים כלום** להסיק ממנו | no error |
| דרישה 6 | מזהה שלא קיים מתנהג **זהה** | no error |
| דרישה 6 | `recipes_using` לא אומרת ל-A כלום על B | 0 |
| דרישה 6 | והמתכון של B ללא שינוי (**נבדק כ-postgres**) | 1 |
| anon | לא יכול לקרוא ל-`delete_recipe` בכלל | refused |
| חשבון | מחיקת חשבון עדיין עושה cascade, כולל קישורים | deleted |
| ניקוי | אין fixture שנשאר | 0 |

### 5.2 Hardening של תת־מתכונים — 15 בדיקות, כולן עברו

`supabase/tests/sub-recipe-hardening.sql`.

| דרישה | בדיקה | תוצאה |
|---|---|---|
| 15 | self-reference | refused: 23514 |
| 15 | מעגל ישיר A→B→A | refused: 23514 |
| 15 | קישור חוצה-חשבונות | refused: 42501 |
| 15 | מעגל עקיף, **ארבע רמות** | refused: 23514 |
| 18 | שרשרת של ארבע רמות מתקבלת | 4 levels |
| 16 | הפניית קישור למתכון בסיס אחר | accepted |
| 16 | והקישור החדש הוא שנשמר | 1 |
| 16 | מתכון הבסיס **הישן** נהפך לניתן למחיקה | deleted |
| 16 | והחדש עכשיו הוא זה שמוחזק | refused: 23503 |
| 17 | קיימת גרסה שמפנה לבסיס שנמחק | found |
| 17 | **השחזור מסורב** | refused: 23503 |
| 17 | שם המתכון ללא שינוי | ללא שינוי |
| 17 | שורות הרכיבים ללא שינוי | ללא שינוי |
| 17 | **לא נוספה גרסה** לשחזור שלא קרה | ללא שינוי |
| — | ניקוי | 0 |

דרישה 17 מעניינת במיוחד עכשיו: מאז שלב 6 **הדרך היחידה** להגיע למצב הזה
היא שהמשתמש הסיר את הקישור מהמתכון החי בעצמו, ואז מחק את הבסיס. כלומר מה
שהיה בשלב 5 "מגבלה שקשה להימנע ממנה" הוא עכשיו מצב שדורש פעולה מכוונת.

### 5.3 בידוד RLS — 42/42, שוב

`supabase/tests/rls-isolation.sql`, הורץ מחדש **אחרי** 0008/0009/0010:
```
passed: 42   failed: 0   failures: none   probe_status: probes complete
```

### 5.4 מסלול ה-snapshot/שחזור משלב 5 — 16/16, אחרי המיגרציות

`null` נשאר `null` ו-`0` נשאר `0` ב-snapshot **וגם** בשחזור; היצירה עוד לא
יוצרת גרסה; השחזור **מוסיף** גרסה ולא צורך אחת; `V1` עוד שם.

### 5.5 יועץ האבטחה ומצב המסד

```
get_advisors(security) → []   (רשימה ריקה)

users 0 · profiles 0 · recipes 0 · ingredients 0 · steps 0 · issues 0
versions 0 · trials 0 · batches 0 · private_notes 0 · calibrations 0 · catalog 0
density_table 34   (נתוני ייחוס מקצועיים — אמורים להישאר)

ingredients_sub_recipe_id_fkey:
  FOREIGN KEY (sub_recipe_id) REFERENCES recipes(id) DEFERRABLE INITIALLY DEFERRED
```
**אין fixtures שנשארו במסד.**

---

## 6. טסטים — 657 עוברים

| חבילה | קבצים | טסטים |
|---|---|---|
| `packages/engine` | 7 | 180 |
| `apps/web` | 25 | 477 |

חדש/מורחב בשלב 6:

| קובץ | טסטים | מה |
|---|---|---|
| `app/DeleteGuardFlow.test.tsx` | 11 | דרישות 1-6 דרך עץ הרכיבים האמיתי |
| `features/recipe/VersionCompare.test.tsx` | 20 | דרישות 7-13 |
| `features/recipe/versionDiff.test.ts` | 41 (+20) | `compareRecipes` — שדות, null/0, מחיר, כפילויות |
| `features/recipe/subRecipe.test.ts` | 36 (+11) | שרשרת שלוש רמות: משקל, עלות, אלרגנים, full/partial/none |
| `data/versions.test.ts` | 42 (+14) | דרישות 16, 17, והחלפת הטסט שבדק את ההתנהגות ההפוכה |
| `data/localDemoRepository.test.ts` | 7 (+3) | תלויות בסט הדמו |

**טסט שהוחלף ולא "תוקן":** שלב 5 בדק ש"מחיקת מתכון בסיס משאירה את השורה
התלויה בלי קישור". זו ההתנהגות ש**הוסרה במכוון** בשלב 6, ולכן הטסט נכתב
מחדש כדי לבדוק את הסירוב. זה מתועד בגוף הקובץ כדי שלא ייקרא בעתיד כרגרסיה.

---

## 7. E2E בדפדפן — 55 בדיקות (22 + 33)

`node apps/web/e2e/stage4.mjs` → 22/22 · `node apps/web/e2e/stage5.mjs` → 33/33

חדש בשלב 6: כפתור המחיקה **מושבת** בבילד ללא שרת עם סיבה מוצגת; אין כפתור
"השוואה בין שתי גרסאות" כשאין היסטוריה להשוות; ואין כפתור השוואה לכל גרסה.
0px גלילה אופקית בטלפון (402px) ובטאבלט (1024px), אפס שגיאות דף, אפס פניות
למקור חיצוני.

---

## 8. מגבלות — במפורש

### 8.1 מגבלת ה-egress עוד בתוקף. אין E2E אמיתי מול PostgREST/GoTrue.

`*.supabase.co` חסום. **שום טסט אינו פותח חיבור HTTP לפרויקט**, וה-E2E
בדפדפן רץ על בילד עם משתני Supabase ריקים. **אני לא מציג אותו כהוכחה
למסלול HTTP.**

בפרט: **דיאלוג החסימה אינו נגיש בבילד הזה** — וזה נכון ולא פער ב-UI. בלי
שרת המתכון הוא לקריאה בלבד, ולכן כפתור המחיקה מושבת **לפני** שהשאלה "האם
משהו משתמש בזה?" יכולה בכלל לעלות. לנסות להגיע לדיאלוג משם היה מחייב לרופף
את כלל הקריאה-בלבד כדי שטסט יעבור.

חלוקת העדויות לחוסם המחיקה:

| מה | מה זה מוכיח |
|---|---|
| `supabase/tests/delete-guard.sql` | האכיפה, מול Postgres, כ-`authenticated` וכ-`anon` |
| `app/DeleteGuardFlow.test.tsx` | הדיאלוג, שמות התלויים, הקישורים והעקיפה — דרך הרכיבים האמיתיים |
| E2E בדפדפן | שבילד לקריאה-בלבד משבית את המחיקה במקום להציע אחת שאינו יכול לבצע |

**מה שאף אחד מהם לא מוכיח:** איך PostgREST מחזיר הפרת constraint ב-commit.
זו בדיוק הסיבה ש-0009 מריץ את הבדיקה בתוך הקריאה — כדי שהתשובה לא תהיה
תלויה בזה.

### 8.2 מסך ההשוואה לא נבדק בדפדפן אמיתי

ל-repository של הדמו **אין** היסטוריית גרסאות (אין שרת), וזו התשובה הכשרה.
לכן `VersionCompare` נבדק ב-jsdom (20 טסטים) ולא במנוע דפדפן. מה שכן נבדק
בדפדפן: שנקודות הכניסה **נעדרות נכון** ולא נוכחות ושבורות.

### 8.3 מה לא נבנה

* **גילוי התלויות לפני ניסיון המחיקה** — "המתכונים שמשתמשים בזה" מוצג רק
  בדיאלוג המחיקה. להציג אותו על דף המתכון היה שיפור סביר, אבל דרישה 3
  מדברת על הודעת המחיקה ולא הרחבתי את השלב לשם.
* **גילוי בורר תת־המתכון** — עדיין בתוך "פרטים נוספים" של השורה. שורה
  **מקושרת** לא מסתירה את זה (תוקן בשלב 5), אבל **יצירת** קישור חדש דורשת
  לפתוח disclosure. תיקון ראוי הוא שינוי מבנה הטופס.
* **השוואה של שלוש גרסאות ומעלה**, או diff של טקסט השלבים מילה-במילה —
  מספר השלבים מושווה, לא תוכנם. לא בוקש.
* **`ingredient_catalog`** — קיימת בסכימה, בלי UI.

---

## 9. מה נשמר, ונבדק שנשמר

| התחייבות | מצב |
|---|---|
| Auth | לא נגעתי |
| RLS | 42/42 מול Postgres, **אחרי** המיגרציות |
| `SupabaseRepository` | הורחב, לא נכתב מחדש |
| Recipe Versioning | 16/16 מול Postgres + 42 טסטים ב-`versions.test.ts` |
| `null ≠ 0` | ב-snapshot, בשחזור, ב-mappers, ב-draft, **ובהשוואה** (דרישה 11) |
| `price 0 ≠ missing price` | מול Postgres, ובהשוואה (דרישה 12), **ועכשיו גם דרך גבול תת־המתכון** — §3.1 |
| נגישות | כל שורת שינוי היא הכרזה אחת; שמות ייחודיים לשני הבוררים; הדיאלוגים named |
| responsive | 0px גלילה אופקית בטלפון ובטאבלט, נבדק בדפדפן |
| בדיקות שלבים 1-5 | כולן עוברות. טסט אחד **הוחלף** במכוון (§6) |
| **הפרוטוטייפ המקורי** | `git status` — **ריק** |
| **`packages/engine/src`** | `git status` — **ריק**. §3.1 הוא תיקון ב-web, לא במנוע |
| **Presenzo Production** (`fglnzrpuffswapjsnppg`) | **לא נגעתי בו בשום צורה** |
| **WhatsApp AI Assistant** (`tbkcxrpenmpcwjicnxnd`) | **INACTIVE**. לא הופעל, לא נמחק, לא שונה |
| `service_role` בדפדפן | אין. רק המפתח הציבורי |

---

## 10. סיכום הרצות

```
npm test                 180 engine + 477 web = 657 עוברים
npm run typecheck        נקי (0 שגיאות)
npm run build            נקי
npm run schema:check     13 טבלות, 136 עמודות, 7 פונקציות מוצהרות, 11 נבדקות ל-grants
npm run seed:check       0005_density_seed.sql מעודכן (34 שורות)
npm run roundtrip:verify           ROUND TRIP OK  (היה שבור מאז שלב 5 — §3.2)
npm run roundtrip:versions:verify  VERSION ROUND TRIP OK
delete-guard.sql         17/17
sub-recipe-hardening.sql 15/15
rls-isolation.sql        42/42
snapshot/restore re-proof 16/16
e2e stage4               22/22
e2e stage5               33/33
get_advisors(security)   []
```
