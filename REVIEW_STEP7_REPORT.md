# דוח שלב 7 — מרכז חומרי גלם, תמחור מרכזי ו-Food Cost

קומיט: `8da4813` על `claude/recipe-notebook-initial-review-fwfb2e`
פרויקט Supabase: `qxdpsomelzpvphkhkqrw` (Recipe Notebook, eu-central-1)
מיגרציות חדשות: `0011`, `0012` — **שתיהן הוחלו בפועל**

---

## 0. הסריקה, ומה היא מצאה

| מה | מצב לפני שלב 7 | מה נעשה |
|---|---|---|
| `ingredient_catalog` | **קיימת מאז 0003** עם `key`, `name`, `price`, `price_unit`, `g_per_100`, `water_pct`, `allergens` ו-RLS מלא — **בלי UI, ולכן בלי אף שורה** | הורחבה ב-0011. **לא נבנתה מחדש** |
| RLS על הקטלוג | קיים ונכון: קריאה של שורות המשתמש + שורות מערכת, כתיבה של שורות המשתמש בלבד | **לא שונה**. נבדק בפועל — §5.1 |
| `compute()` — עלות לק"ג ולליטר | קיים ותקין | לא שונה |
| `compute()` — עלות **ליחידה** | **שבור** — ראו §3.1 | תוקן במנוע, עם תיעוד ו-15 regression tests |
| `calcState` / full-partial-none | קיים, כולל propagation דרך תת־מתכונים (שלב 6) | חובר ל-primitive החדש |
| `allergensFor(name)` + `manualAllergens` | קיימים | הקטלוג **מצטרף** אליהם, לא מחליף |
| שרשרת הצפיפויות §5.1 (מתכון → כיול → טבלה) | קיימת, מוכחת ונבדקת | **לא נגעתי**. ראו §8.3 |
| `mappers.ts`, `SupabaseRepository`, `AppDataProvider` | קיימים | הורחבו |

`git status design_handoff_recipe_notebook/` — **ריק**. הפרוטוטייפ לא נגע.

---

## 1. ההתנגשות הארכיטקטונית של דרישה 5, והפתרון

ההנחיה ביקשה לעצור בנקודה הזאת, להסביר את האפשרויות ולבחור. זה מה שנעשה.

**הסתירה, כפי שהיא נראית:** דרישה 2 אומרת שמחיר הוא מרכזי ושינוי שלו משפיע
על כל המתכונים. דרישה 5 אומרת שגרסה היסטורית חייבת לשמור על המשמעות
ההיסטורית שלה. אם מתכון פותר מחיר מהמרכז בזמן קריאה, אז גרסה מלפני שלושה
חודשים, כשתיקרא היום, תציג את מחירי היום — והמשמעות שלה השתנתה בשקט.

| אפשרות | דרישה 2 | דרישה 5 | ההשלכה |
|---|---|---|---|
| **א.** לשמור מחיר בכל שורת מתכון (המצב הקיים) | ✗ נכשלת — אין מקור אמת מרכזי | ✓ ההיסטוריה כשרה | מה שיש היום. שינוי מחיר = עריכה של מאות שורות |
| **ב.** לפתור מהמרכז בלבד, לא לשמור אף פעם | ✓ מושלמת | ✗ **נכשלת** — עלות של גרסה ישנה נסחפת | "העוגה הזאת עלתה ₪18.40 במרץ" מפסיק להיות נכון ביוני |
| **ג.** פתירה חיה למתכון החי + **הקפאה בתוך ה-snapshot** | ✓ | ✓ | **נבחרה** |

**למה (ג) אינה מקור אמת שני:** כי snapshot של גרסה **הוא כבר** מסמך קפוא —
זה מה ש-§9 עשה ממנו בשלב 5. להקפיא בתוכו את המחיר זו **אותה פעולה** כמו
להקפיא בתוכו את הכמויות. הוא אינו טענה על המחיר של היום, כי הוא אינו על היום.
לכן אין צורך במנגנון התאמה, ב-cache או בעותק denormalized בשורות החיות.

**המימוש:**
* מתכון חי: שורה ללא מחיר משלה **אינה שומרת מחיר**. `resolveFromCatalog`
  ממלא אותו לפני כל `compute()`.
* `recipe_snapshot()` (0011) כותב `coalesce(i.price, c.price)` — המחיר
  **האפקטיבי** — לתוך כל שורת רכיב ב-snapshot.
* הפתירה **אידמפוטנטית**, ולכן snapshot קפוא (כל מחיריו מלאים) עובר בה ללא
  שינוי — וזה מה שמאפשר להשוות גרסה למתכון חי על אותו מישור.

**ו-`ingredients.price` נשאר, כ-override.** שורה עם מחיר משלה שומרת עליו.
לכן **אין migration של נתונים בכלל** — ולכן אין מה לאבד: אף `0` מפורש לא
הפך ל"חסר" ואף מחיר חסר לא הפך ל-`0`. אימוץ מחיר מרכזי הוא פעולה מפורשת של
המשתמש, לא דבר שקורה לו.

---

## 2. מה נבנה — דרישות 1 עד 5

### 2.1 דרישה 1 — מרכז חומרי גלם

מסך חדש ב-`/ingredients`, מקושר מ"עוד". כל שדה שההנחיה מונה:

| נדרש | איפה |
|---|---|
| שם חומר הגלם | `name`, וה-`key` נגזר מזהות המנוע (`ingredientKeyOf`) כך ש-"חמאה 82% " מתאימה ל-"חמאה 82%" |
| יחידת רכישה | `purchase_unit` — `kg` / `g` / `l` / `ml` / `unit` |
| כמות באריזה | `package_qty` |
| מחיר אריזה | `package_price` |
| **מחיר מחושב ליחידת בסיס** | `price` + `price_unit` — **עמודות GENERATED** |
| ספק | `supplier` |
| תאריך עדכון מחיר | `price_updated_at`, מוטבע **רק** כשהאריזה משתנה |
| אלרגנים | `allergens`, עם הצעות מטבלת המנוע |
| צפיפות/כיול | **לא שוכפל** — ראו §8.3 |

### 2.2 דרישה 3 — אריזות ויחידות, לא הנחה שהכול בק"ג

`price` ו-`price_unit` הן **GENERATED STORED** מהאריזה. לכן אי אפשר שייסחפו
ממנה, ואף קליינט אינו יכול לכתוב אותן — נבדק מול המסד:

```
update ingredient_catalog set price = 999 → refused
```

ארבעת התרחישים שההנחיה מונה, מול Postgres אמיתי:

| חומר | אריזה | מחיר בסיס נגזר |
|---|---|---|
| קמח | שק 25 ק"ג ב-₪110 | **₪4.40 לק"ג** |
| חמאה | חבילה 200 גרם ב-₪8.90 | **₪44.50 לק"ג** |
| שמנת | 1 ליטר ב-₪18 | **₪18 לליטר** |
| ביצים | מגש 30 יחידות ב-₪39 | **₪1.30 ליחידה** |
| (בונוס) תמצית | 100 מ"ל ב-₪45 | **₪450 לליטר** |

ובדיקה שאין הנחה סמויה: אותם מספרי אריזה תחת חמש יחידות רכישה מחזירים חמש
תשובות שונות (`5 ק"ג`, `5000 ק"ג`, `5 ליטר`, `5000 ליטר`, `5 יח'`).

### 2.3 דרישה 2 — מחיר אחד, וכל המתכונים בעקבותיו

מול Postgres: מתכון נשמר, `ingredients.price` הוא **NULL**, המחיר בא מהמרכז.
המחיר במרכז הוכפל (`package_price` 8.90 → 17.80, כלומר ₪89 לק"ג) —
**אף שורת מתכון לא נכתבה מחדש**, והעלות בכל המתכונים זזה.

### 2.4 דרישה 4 — Food Cost

בכל מתכון: עלות חומרי הגלם, עלות לק"ג, עלות ליחידה, מחיר מכירה, ואחוז פוד
קוסט = עלות / מחיר מכירה × 100.

**כל שדה הוא `number | null`, ו-`null` אינו 0.** המסך מציג `—` ל-null
ומספר ל-0, כי ₪0 ערך חוקי ו-0% פוד קוסט ערך חוקי, ואף אחד מהם אינו "לא
הצלחנו לחשב". ארבע הדרכים שבהן אין פוד קוסט, כולן מובחנות **ומוסברות
למשתמש** (מקף בלי סיבה נקרא כמו באג):

| מצב | מה מוצג |
|---|---|
| אף חומר לא מתומחר | אין עלות, אין אחוז. "אפס אינו התשובה — פשוט אין נתון" |
| **עלות חלקית** | העלות מוצגת ומסומנת, **והאחוז נמנע**. אחוז מעלות חלקית תמיד נמוך מהאמת, וזה הכיוון שמפסיד כסף |
| לא הוגדר מחיר מכירה | האחוז נמנע, וזה נאמר |
| מחיר מכירה = 0 | חילוק באפס. **לא 0% ולא 100%** — לא קיים |

### 2.5 דרישה 5 — השפעת שינוי מחיר

`recipes_pricing_on(key)` — SECURITY INVOKER, ולכן RLS עונה על השאלה. היא
מונה **רק שורות שיורשות** את המחיר; שורה עם מחיר משלה אינה זזה, ולטעון
שכן היה שולח את המשתמש לחפש שינוי שלא קרה. המסך מונה את המתכונים בשמם,
כקישורים, ומציין בנפרד כמה שורות שם **לא** ישתנו.

---

## 3. תקלות אמיתיות שנמצאו ותוקנו

### 3.1 מחיר ליחידה חושב כמחיר לק"ג — הטעות של פי 18

`compute()` מחשב עלות ממשקל בגרמים. הוא טיפל במקרה של `'ליטר'` בנפרד
ובשום מקרה אחר — ולכן מחיר ב-`יח'` **נפל אל הנוסחה של מחיר לק"ג**:

```
3 ביצים ב-₪1.30 ליחידה  →  cost = 0.2145   (במקום 3.90)
warnings: []
```

`יח'` הוא ערך שאפשר לבחור בבורר בעורך וערך חוקי ב-CHECK של
`ingredients.price_unit`, כלומר **התקלה נגישה מה-UI, שקטה, ובפער של פי
שמונה-עשר**. בלי לתקן אותה דרישה 3 (מגש 30 ביצים) אינה ניתנת למימוש.

**זה שינוי במנוע**, ולכן מתועד לפני ביצועו כפי שההנחיה דורשת. שני חלקים,
שניהם מינימליים:

1. **`gramsPerItem` יוצא מ-`convert.ts`.** תמחור לפי פריט דורש דבר אחד
   שענף העלות לא יכול היה להשיג — כמה שוקל פריט אחד. `itemGrams` כבר ידע,
   ולכן זה **מייצא** אותו ולא מחשב תשובה שנייה שיכולה להיסחף.
2. **`ComputedRow.priced`** — ה-primitive שחסר. "לשורה הזאת יש מחיר"
   ו"העלות של השורה הזאת אמיתית" הם שני דברים שונים ברגע שמחיר יכול
   **להיכשל בהחלה**, וקורא שבודק רק `ing.price` מדווח על עלות מוגמרת לשורה
   שלא תרמה כלום. מחיר ליחידה בלי משקל ליחידה **אינו מחושב כאפס** — הוא
   מדווח.

15 regression tests ב-`packages/engine/test/per-unit-price.test.ts`,
כולל ש-`priced` נכון גם ל-price 0 מפורש (true), למחרוזת ריקה (false),
לשורה שלא נשקלה (false) ולשורת תת־מתכון (false — אין לה מחיר משלה).

והחצי השני: `calcState` עוד גזר "האם זה מתומחר" מ-`ing.price`. חובר
ל-`row.priced`, ולכן:

```
165 גר' ביצים במחיר ליחידה בלי משקל ליחידה:
  לפני:  costLevel = full,    unpriced = []        ← עלות מוגמרת שחסר בה רכיב
  אחרי:  costLevel = partial, unpriced = ["ביצים"]
```

### 3.2 שבעה design tokens שלא קיימים, בגיליונות שנשלחו בשלבים 5 ו-6

כתבתי CSS מול `--c-card`, `--c-ink-2`, `--c-clay`, `--c-clay-soft`,
`--c-clay-ink`, `--r-btn`, `--fs-h1` — **אף אחד מהם לא מוגדר**.
custom property שאינו מוגדר **אינו שוגה ואינו נופל ל-fallback**: ההכרזה
פשוט נזרקת. כלומר האלמנט מוצג בלי צבע, בלי רקע ובלי רדיוס, **ושום דבר
בשום מקום לא אומר את זה**. אותו סוג כשל שקט כמו container query שמצביע על
קונטיינר שלא קיים בשלב 6.

התקלה הייתה קיימת ונשלחה ב-`recipe.module.css` (הערך הקודם בהשוואת
הגרסאות, הקישור לתלויות במחיקה, הרדיוס של כפתור ההשוואה) וב-
`RecipeEditScreen.module.css`.

**נסגר, לא רק תוקן:** `tokens.test.ts` סורק כעת **כל** `*.module.css`
ומאמת שכל `var(--…)` מוגדר, **ובנוסף** שכל `@container` מצביע על
`container-name` שקיים — התאום של הטעות משלב 6. 26 בדיקות חדשות.

### 3.3 שני דברים באותו שם נגיש

ל-section של הטופס במרכז היה `aria-label` זהה לכפתור "עריכה" ברשימה —
`עריכת חמאה 82%`. מי שמנווט לפי שמות הגיע לשני דברים שונים באותו שם.
הטופס נקרא עכשיו לפי **מה שהוא**, לא לפי החומר.

### 3.4 ה-repository של הדמו החזיר מחירים אמיתיים? לא — וזה מכוון

`listCatalog()` מחזיר `[]`. זו **התשובה הכשרה**: המרכז מחזיק נתונים
עסקיים — מחירים, ספקים — שמשתייכים לחשבון. אין חשבון, אין מה להציג.
מחירי דמו היו בסיס עלות מומצא.

---

## 4. שגיאות שיטה שלי

| מה חשבתי | מה באמת | איך התגלה |
|---|---|---|
| ה-snapshot של השמירה שהוסיפה override של 0 יחזיק 0 | גרסה מחזיקה את המצב **שלפני** השמירה, ושם באמת לא היה override. **האשמתי קוד תקין בכך שאיבד את ה-override** | שמירה נוספת, ואז בדיקה של ה-snapshot הנכון |
| `order by created_at desc limit 1` יביא את הגרסה האחרונה | `now()` הוא זמן ה-**טרנזקציה**; כל הגרסאות בסקריפט חולקות אותו `created_at`, והבחירה שרירותית. כך נקראה גרסה ממצב אחר לגמרי | בחירה לפי `tag` |
| `formatNis` מרפד לשתי ספרות | `₪11.1`, לא `₪11.13`. זה הפורמטר של כל האפליקציה | הטסטים |
| `::numeric::text` ישווה מספרים בלי scale | שומר את ה-scale. צריך `trim_scale` | שוב, כמו בשלב 6 |
| `fs.globSync` מכבד `absolute: true` | לא בגרסה הזאת. הנתיבים חוזרים יחסיים | ENOENT |

שתי הראשונות הן אותה משפחה: בדיקה שמאשימה קוד תקין. שתיהן מתועדות בגוף
`supabase/tests/pricing.sql` כדי שלא ייווצרו מחדש.

---

## 5. הוכחות מול Postgres אמיתי

### 5.1 התמחור והבידוד — 27 בדיקות, כולן עברו

`supabase/tests/pricing.sql`:
```
passed: 27   failed: 0   probe_status: probes complete   cleanup: 0
```

| תחום | בדיקה | תוצאה |
|---|---|---|
| **דרישה 6** | A רואה רק את חומרי הגלם של עצמו | 1 |
| דרישה 6 | A **לא קורא את המחיר** של B | (none) |
| דרישה 6 | A **לא קורא את הספק** של B | (none) |
| דרישה 6 | UPDATE של A על חומר של B — אפס שורות | 0 |
| דרישה 6 | DELETE של A על חומר של B — אפס שורות | 0 |
| דרישה 6 | A לא יכול לשתול חומר גלם אצל B | refused |
| דרישה 6 | B רואה רק את שלו, **במחיר שלו** | 1 / 125 |
| דרישה 6 | `recipes_pricing_on` לא אומרת ל-B כלום על המתכונים של A | 0 |
| דרישה 3 | חבילה 200 גרם ב-8.90 → 44.5 לק"ג | 44.5 / ק"ג |
| דרישה 3 | המחיר הנגזר **אינו ניתן לכתיבה** מקליינט | refused |
| דרישה 2 | שורת המתכון **אינה שומרת מחיר** | 1 שורה NULL |
| דרישה 2 | המחיר המרכזי זז ל-89 | 89 |
| דרישה 2 | והשורה החיה עדיין לא שומרת מחיר | 1 |
| **דרישה 5** | ה-snapshot **הקפיא** את המחיר האפקטיבי | 44.5 |
| דרישה 5 | ואת היחידה | ק"ג |
| דרישה 5 | **הגרסה לא זזה** עם המחיר המרכזי | 44.5 |
| דרישה 5 | הקטלוג יודע אילו מתכונים מושפעים | 1 |
| דרישה 5 | ומפסיק לטעון שמתכון עם override מושפע | 0 |
| **דרישה 8** | חומר ללא מחיר → **NULL, לא 0** | null |
| דרישה 8 | 0 מפורש → **מחיר של אפס** | 0 |
| דרישה 8 | `0` מפורש במתכון נשמר כ-0 ולא מוחלף | 0 |
| דרישה 8 | snapshot של המצב הזה מקפיא 0, לא את המחיר המרכזי | 0 |
| דרישה 8 | חומר ללא מחיר בשום מקום **נשאר NULL ב-snapshot** | true |
| anon | לא רואה אף חומר גלם | 0 |
| anon | ולא יכול לקרוא ל-`recipes_pricing_on` | refused |
| ניקוי | אין fixture שנשאר | 0 |

### 5.2 שאר הסוויטות, אחרי 0011 ו-0012

0011 שינתה את `recipe_snapshot()`, שכל הסוויטות משתמשות בה — ולכן כולן
הורצו מחדש:

```
rls-isolation.sql          42/42   (probes complete, cleanup 0)
sub-recipe-hardening.sql   15/15   (cleanup 0)
roundtrip:verify           ROUND TRIP OK
roundtrip:versions:verify  VERSION ROUND TRIP OK
schema:check               13 טבלות, 143 עמודות, 8 פונקציות, 13 נבדקות ל-grants
seed:check                 מעודכן (34 שורות)
```

### 5.3 המסד נקי

```
users 0 · profiles 0 · recipes 0 · ingredients 0 · steps 0 · issues 0
versions 0 · trials 0 · batches 0 · private_notes 0 · calibrations 0
ingredient_catalog 0
density_table 34   (נתוני ייחוס מקצועיים — אמורים להישאר)
```

---

## 6. טסטים — 782 עוברים

| חבילה | קבצים | טסטים |
|---|---|---|
| `packages/engine` | 8 | 195 |
| `apps/web` | 29 | 587 |

חדש בשלב 7:

| קובץ | טסטים | מה |
|---|---|---|
| `engine/test/per-unit-price.test.ts` | 15 | §3.1 — מחיר ליחידה, ו-`priced` |
| `features/pricing/catalog.test.ts` | 30 | אריזות, null/0, פתירה, override, אידמפוטנטיות, אלרגנים |
| `features/pricing/foodCost.test.ts` | 19 | דרישה 4 במלואה, ושרשרת תת־מתכונים עם מחיר מרכזי |
| `routes/IngredientsScreen.test.tsx` | 21 | המסך, גזירה חיה, null/0, דרישה 5 |
| `app/PricingFlow.test.tsx` | 14 | כל המסלול דרך עץ הרכיבים, כולל בידוד והקפאת גרסה |
| `styles/tokens.test.ts` | +26 | §3.2 — tokens ו-container names בכל גיליון |

מכוסה במפורש מרשימת דרישה 8: שינוי מחיר שמשפיע על כמה מתכונים · חומר ללא
מחיר · מחיר מפורש 0 · שינוי גודל אריזה · kg/g/L/ml/units · תת־מתכון בשלוש
רמות · חומר חסר בתוך תת־מתכון (כולל הנתיב `מילוי → גנאש → שוקולד מריר`) ·
Food Cost מלא וחלקי · yield ביחידות ובמשקל · cross-account isolation ·
recipe versions לאחר שינוי מחיר · round-trip מול Postgres · ו**migration —
שאין**, ולכן אין אובדן משמעות (§1).

---

## 7. E2E בדפדפן — 67 בדיקות (22 + 45)

חדש בשלב 7: המרכז נגיש מ"עוד" ונטען; המצב הריק מסביר מה יקרה; ההוספה
**מושבתת** בלי חשבון במקום להיכשל בלחיצה; פאנל הפוד קוסט מציג עלות, עלות
לק"ג ומחיר מכירה, **מונע את האחוז** ומסביר למה. 0px גלילה אופקית.

---

## 8. מגבלות — במפורש

### 8.1 מגבלת ה-egress עוד בתוקף

`*.supabase.co` חסום. **אין שום טסט HTTP אמיתי מול PostgREST/GoTrue, ואני
לא מציג את ה-E2E בדמו כהוכחה לכך.**

ובפרט: ל-repository של הדמו **אין קטלוג** (§3.4), ולכן **התנהגות המרכז
עצמו אינה נבדקת בדפדפן**. הכיסוי שלה הוא `IngredientsScreen.test.tsx` (21)
ו-`PricingFlow.test.tsx` (14) ב-jsdom, ו-`pricing.sql` (27) מול Postgres.
מה שהדפדפן כן מוסיף: שהמסך קיים, נגיש, ומתנהג נכון כשאין מה להציג.

### 8.2 מה שלא נבנה

* **אימוץ מחיר מרכזי בהמוני שורות קיימות.** שורה עם מחיר משלה שומרת עליו,
  וזה מה ששומר על הנתונים הקיימים (§1). אין כרגע פעולה "אמץ את המחיר
  המרכזי בכל המתכונים" — היא תהיה שינוי נתונים בהיקף גדול, והחלטתי לא
  לבצע כזה בלי בקשה מפורשת. המרכז **כן** מראה לאיזה מתכונים זה נוגע.
* **היסטוריית מחירים** של חומר גלם. יש `price_updated_at` (מתי השתנה
  לאחרונה), אין טבלת שינויים. §13 לא ביקש.
* **`g_per_100` / `water_pct` בקטלוג** — ראו §8.3.
* **ספקים כישות** (רשימה, מחירים מושווים) — `supplier` הוא טקסט.

### 8.3 החלטה שקיבלתי במפורש: הקטלוג **אינו** מחזיק צפיפות

דרישה 1 מונה "צפיפות/כיול כאשר רלוונטי", וההנחיה באותה נשימה אומרת
"אל תכפיל מידע שכבר קיים כמקור אמת במקום אחר". השתיים מתנגשות כאן:

§5.1 מגדיר **שרשרת מדורגת** לצפיפות — הערך שהוזן במתכון, אחריו הכיול
האישי של המשתמש, אחריו הטבלה המקצועית — והיא מוכחת ונבדקת מאז שלב 1.
להוסיף דרג רביעי בתוכה היא **החלטה על provenance**, לא פיצ'ר תמחור: היא
משנה את משמעות ה-badge שמוצג לכל רכיב בכל מתכון.

לכן `g_per_100` ו-`water_pct` בקטלוג **נשארים ללא שימוש**, והמרכז **מציג**
את מצב הצפיפות/הכיול מהמקורות הקיימים ומציע לכייל, במקום להחזיק עותק
רביעי. אם תרצה שהקטלוג יהיה דרג בשרשרת — זו הכרעה שלך, ואשמח לממש אותה
כשתיתן אותה.

לעומת זאת **אלרגנים** כן נכנסו לקטלוג, כי שם אין שרשרת מדורגת: הם היו
מוסקים מהשם או מוזנים ידנית **בכל מתכון בנפרד**, והצהרה אחת על החומר היא
שיפור ולא כפילות. הזיהוי לפי השם נשאר כ-fallback.

---

## 9. מה נשמר, ונבדק שנשמר

| התחייבות | מצב |
|---|---|
| Recipe creation/editing | 782 טסטים עוברים, כולל כל מסלולי שלבים 4-6 |
| Versioning + restore | 42 טסטים + 15/15 מול Postgres |
| Version comparison | 41 טסטים. **ההשוואה עובדת על שני צדדים פתורי-מחיר**, ולכן גרסה קפואה ומתכון חי מושווים על אותו מישור |
| Sub-recipes | 36 טסטים + שרשרת 4 רמות מול Postgres |
| delete dependency guard | 15/15 מול Postgres, ללא רגרסיה |
| calibrations | לא נגעתי; 7 טסטים עוברים |
| allergens | הורחב (קטלוג), לא הוחלף |
| yield | נבדק ביחידות ובמשקל |
| `null ≠ 0` | בקטלוג, ב-snapshot, ב-Food Cost ובטופס — 27 בדיקות מול Postgres |
| `price 0 ≠ missing price` | אותו דבר, בשני הכיוונים |
| full / partial / none | נשמר, **והתחזק** — §3.1 |
| Auth · RLS | 42/42 ללא רגרסיה |
| responsive · accessibility | 0px גלילה; שמות נגישים ייחודיים (§3.3) |
| **הפרוטוטייפ** | `git status` — ריק |
| **`packages/engine/src`** | שונה **במכוון ובתיעוד** — §3.1. 87 שורות, 15 regression tests |
| **Presenzo Production** | **לא נגעתי בו בשום צורה** |
| **WhatsApp AI Assistant** | **INACTIVE**. לא הופעל, לא נמחק, לא שונה |

---

## 10. סיכום הרצות

```
npm test                 195 engine + 587 web = 782 עוברים
npm run typecheck        נקי
npm run build            נקי
npm run schema:check     13 טבלות, 143 עמודות, 8 פונקציות, 13 נבדקות ל-grants
npm run seed:check       מעודכן (34 שורות)
pricing.sql              27/27
rls-isolation.sql        42/42
sub-recipe-hardening.sql 15/15
roundtrip:verify         OK
roundtrip:versions       OK
e2e stage4               22/22
e2e stage5+6+7           45/45
המסד                     0 שורות בכל טבלה, חוץ מ-34 שורות ייחוס
```
