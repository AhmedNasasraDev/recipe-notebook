# דוח שלב 9 — תכנון ייצור ורכש

פרויקט Supabase: `qxdpsomelzpvphkhkqrw` (Recipe Notebook, eu-central-1)
מיגרציות חדשות: `0018`, `0019` — **שתיהן הוחלו בפועל**
ענף: `claude/recipe-notebook-initial-review-fwfb2e`

`git status design_handoff_recipe_notebook/` — **ריק**. הפרוטוטייפ לא נגע.

---

## 0. הסריקה, ומה היא מצאה

| מה חיפשתי | מה מצאתי | מה עשיתי |
|---|---|---|
| חישוב scale factor | **`scaleFactor(mode, value, baseline)` כבר קיים במנוע** מאז שלב 4 | השתמשתי בו. לא כתבתי נוסחה מקבילה |
| תפוקה ביחידות ובמשקל | `compute()` מחזיר `unitsActual`, `actualYield`, `theoretical`, `scaleWeight` | מהם נגזר כל scaling |
| תת־מתכונים | `compute()` מחשב רקורסיבית ומאחסן את הבסיס ב-`row.sub` (בפקטור 1), והשורה מחזיקה `row.g` | ה-explosion משתמש ב-`k = g / subYield` — **הקבוע של המנוע עצמו** |
| קטלוג חומרי גלם ומחירים | שלבים 7-8: `price` (שמיש), `purchase_price` (כפי שנקנה), אריזה, ניצולת | רשימת הרכש בנויה עליהם |
| שלבי הכנה וזמנים | `steps`: טקסט, טמפרטורה, דקות | **חסר** סוג השלב — ראו §1.1 |
| מעגלי תת־מתכונים | נאכפים במסד מאז 0007 | לא נגעתי; נבדק שוב — §5 |
| null ≠ 0 | בכל הרמות | נשמר, כולל ב-on-hand |

`packages/engine/src` — **שינוי אחד בלבד, וטיפוס בלבד.** ראו §1.1.

---

## 1. סכימה — מה נוסף, ולמה

### 1.1 `steps.kind` — שינוי המנוע היחיד, ותיעוד הסיבה לפניו

דרישה 8 מבקשת שלוח הזמנים יבחין בין עבודה, המתנה, קירור, התפחה ואפייה
"כאשר הנתונים מאפשרים". **הנתונים לא אפשרו**: לשלב היו טקסט, טמפרטורה ומשך,
ושום דבר שאומר מה *סוג* ההמתנה. להסיק את זה מהטקסט העברי הוא בדיוק ההמצאה
שההנחיות אוסרות.

לכן נוסף `steps.kind` (`active` / `passive` / `chill` / `proof` / `bake`,
ו-**NULL = לא מסווג**), ובמנוע נוסף **טיפוס בלבד**: `Step.kind?: StepKind`.
`compute()` לא נגע, אף חישוב אינו קורא את השדה, וכל שלב קיים מקבל אותו כחסר.
המסווג עצמו יושב בשכבת ה-web, כי "240°C זו אפייה" היא הצגה של עובדה שהמתכון
כבר מחזיק ולא primitive חדש במנוע.

ההיסק היחיד שנעשה הוא **מהטמפרטורה בלבד**: ≤ 8°C נקרא קירור, ≥ 100°C נקרא
אפייה/בישול, והמסך אומר במפורש "(לפי הטמפרטורה)". מהטקסט לא נקרא דבר.

### 1.2 `production_plans`, `production_plan_items`, `production_plan_stock`

התוכנית שומרת **כוונה בלבד**: תאריך, מוצרים, כמויות, יחידות יעד, שעת מוכנות,
הערות, ואופציונלית מה שכבר יש במחסן. **אף נתון נגזר אינו נשמר** — לא הדרישה,
לא רשימת הרכש, לא העלות ולא לוח הזמנים. כולם מחושבים מהמתכונים ומהמרכז בכל
פתיחה, וזו כל הנקודה: תוכנית לשבוע הבא מציגה את המחירים של השבוע הבא.

`production_plan_stock.on_hand` הוא **NULLABLE וזה נושא משקל**: NULL פירושו
שאף אחד לא אמר מה יש במחסן, ואז מוצגת הדרישה המלאה. הנחה של 0 הייתה אומרת
למישהו לקנות 12 ק"ג קמח שכבר יש לו.

**זו לא מערכת Inventory** ואינה הופכת לאחת: אין תנועות, אין יתרות ואין
היסטוריה — רק מה שהוקלד ברשימת הרכש של התוכנית הזאת.

### 1.3 בידוד ושומר בעלות

- `production_plans` — מדיניות אחת: השורות של החשבון.
- הילדים דרך `owns_plan(plan_id)`, באותה צורה בדיוק שילדי מתכון עוברים דרך
  `owns_recipe`.
- **RLS אינו חל על אימות מפתח זר**, ולכן אותו חור שנמצא בשלב 5 ב-`sub_recipe_id`
  פתוח גם כאן: ה-FK מסתפק בכל מתכון קיים. הוא נסגר באותה דרך —
  `check_plan_item_owner()` משווה בעלים, ומחזיר את **אותה הודעה** ל"לא שלך"
  ול"לא קיים", כדי שהשגיאה לא תענה על השאלה "האם ה-id הזה קיים אצל מישהו אחר".

---

## 2. הכרעה ארכיטקטונית: מה live ומה snapshot (דרישה 14)

ההנחיה ביקשה לתכנן זאת במפורש ולתעד. זו ההכרעה:

| | תוכנית פעילה | תוכנית שסומנה כבוצעה |
|---|---|---|
| דרישת חומרי גלם | מחושבת live | מוקפאת |
| רשימת רכש | מחושבת live | מוקפאת |
| עלות צפויה | לפי המחיר הפעיל **היום** | לפי המחיר שהיה ברגע הסימון |
| עריכה | מותרת | **חסומה במסד** |

הכלל שורה אחת ואין בו אמצע: **נעולה → קוראים את ה-snapshot. לא נעולה →
מחשבים live.** `locked` ו-`snapshot` נעים יחד, ו-CHECK במסד אוסר על כל אחד
מהם להתקיים בלי השני.

**ביטול הנעילה מוחק את ה-snapshot.** תוכנית שחוזרים לערוך אינה תיעוד של שום
דבר, ולהשאיר עלות מוקפאת ישנה לצד עלות חיה זה בדיוק מקור האמת השני שדרישה 14
אוסרת. מי שרוצה את התיעוד — משאיר אותה נעולה.

זה אותו מכשיר בדיוק שהוכרע בשלב 7 לגרסאות מתכון: מסמך קפוא הוא לא מקור אמת
שני על מחירי היום, כי הוא לא על היום.

---

## 3. תקלה אמיתית שנמצאה — `steps.kind` לא נשמר בכלל

**מיגרציה 0019.** נמצאה בהרצת `planning.sql` מול המסד החי, בבדיקה הראשונה
בקובץ.

0018 הוסיפה את העמודה, הלקוח שולח אותה, ו-`replace_recipe_children` (מ-0007)
**מונה** את עמודות השלב שהיא כותבת — ולכן כל `kind` הגיע כ-NULL. לוח הזמנים
היה מציג כל שלב כ"לא מסווג" בלי קשר למה שהמשתמש בחר, והבחירה הייתה נראית
כאילו לא נשמרה — כי היא באמת לא נשמרה.

זו **אותה תקלה בדיוק** ש-`sale_price` עבר בשלב 8 (מיגרציה 0014), באותו סוג
מקום, שלב אחד אחרי. שתי הערות שראוי לרשום:

1. רשימת עמודות מנויה בפונקציית כתיבה היא מלכודת קבועה. תיקון שלב 8 העביר את
   שדות ה**מתכון** ל-helper אחד בדיוק מהסיבה הזאת; כותב ה**ילדים** עוד מונה,
   וזה המחיר. לא עשיתי שם refactor: `replace_recipe_children` משותפת לשמירה
   ולשחזור והיא המקום היחיד שכל עמודת ילד נכתבת בו, ולכן התיקון הוא להוסיף את
   העמודה, לא להוסיף שכבה.
2. **רק Postgres אמיתי יכול היה לתפוס את זה.** הכפיל בזיכרון מחיל שורות
   שלמות, ולכן ה-`kind` שרד שם וכל 728 הטסטים עברו.

שני ה-round-trips מול המסד מגנים על זה עכשיו לצמיתות: שלב אחד **מסווג** ואחד
שנשאר בכוונה בלי סיווג, ושניהם נבדקים אחרי שמירה ואחרי שחזור. גם רשימות
העמודות ב-`mapper-roundtrip.mts` נגזרות עכשיו ממפתחות אובייקט השורה (ולא
כתובות ביד), כך שאותה סחיפה לא תחזור בשקט — זה המשך ישיר לתיקון §2.6 בשלב 8.

---

## 4. איך כל דרישה מומשה

| דרישה | מה נעשה |
|---|---|
| **1** תוכנית ומוצרים | מסך "תכנון ייצור" עם תאריך, ולכל שורה: מתכון, כמות יעד, יחידת יעד, שעת מוכנות והערה. factor מוצג ליד תפוקת המתכון: 20 → 100 = **פי 5** |
| **2** scaling מקצועי | דרך `scaleFactor` של המנוע. ביחידות, במשקל (ק"ג/גרם), עם תשואה מעשית או תאורטית, ובכל עומק תת־מתכונים. **אין המצאת yield**: בלי תפוקה ביחידות אין factor — `null`, לא 1 — והמסך אומר מה חסר |
| **3** explosion | תת־מתכון **אינו** מופיע כחומר לקנייה. הוא נפרק, וגם ילדיו. "3 ק"ג בצק" → 1.8 ק"ג קמח + 1.2 ק"ג חמאה. עומק בלתי מוגבל |
| **4** רשימה מאוחדת | צבירה לפי **identity** (`ingredient_key`, אחרת מפתח המנוע) ולא לפי שם. אותו חומר בעשרה מתכונים = שורה אחת; שני חומרים באותו שם עם מפתחות שונים = שתי שורות |
| **5** מלאי קיים | שדה "יש במחסן" אופציונלי לכל שורה. `Required − On hand = To buy`. **חסר ≠ 0**: בלי נתון מוצגת הדרישה המלאה, ועם 0 מוצג שצריך לקנות הכול |
| **6** התאמה לאריזות | "כמה צריך לייצור" ו"כמה צריך לקנות" נפרדים. 8.2 ק"ג → **2 × 5 ק"ג = 10 ק"ג, עודף 1.8 ק"ג**. כמות המתכון לא מעוגלת. כשיש ניצולת, הקנייה גדלה כדי שהחלק השמיש יכסה את הדרישה |
| **7** עלות רכש | לפי המחיר **כפי שנקנה** (`purchase_price`), כי מה שמתומחר הוא קנייה. עלות לכל חומר + סכום + full/partial/none + רשימת החומרים ללא מחיר. **מחיר 0 נשאר מחיר**; total נמנע כשחסר מחיר של חומר שצריך לקנות |
| **8** חישוב לאחור | מהשעה שהמוצר צריך להיות מוכן, שלב אחר שלב. **משך חסר עוצר את החישוב** — אותו שלב וכל מה שלפניו בלי זמן, הלוח מסומן חלקי והשלב מוזכר בשמו |
| **9** לילות | חשבון אריתמטי במילישניות, ולכן חוצה חצות וגם מעבר שעון. קירור 12 שעות לפני שישי 08:00 = **חמישי 19:30**, והתווית כוללת את היום |
| **10** מוצרים במקביל | timeline משולב בסדר זמנים. **אין אופטימיזציה** של כוח אדם או ציוד ואיני מתיימר לכזו; חפיפות מוצגות ולא מוזזות |
| **11** תלויות | לכל מוצר מוצגים תת־המתכונים שלו (בעומק), וכל אחד מתוזמן להסתיים **לפני השלב הראשון** של המוצר. הכלל השמרני נאמר על המסך, כי המודל מקשר מתכון לתת־מתכון ולא **שלב** לתת־מתכון. שרשרת A → B → C נבדקת; מעגל נותר בלתי אפשרי |
| **12** UX | מוצרים גלויים תמיד; חומרי גלם, רכש+עלות, ולוח זמנים מאחורי disclosure. אפס גלישה אופקית בטלפון ובטאבלט. חסר מסומן ב-"—" **ובנוסח שאומר מה חסר** |
| **13** שמירה ובידוד | `save_production_plan` אטומית (תוכנית + שורות + מלאי בטרנזקציה אחת), פתיחה, עריכה ומחיקה. RLS מלא, נבדק — §5 |
| **14** live מול snapshot | §2 |

---

## 5. הרצה מול Postgres החי

| מערך | תוצאה |
|---|---|
| `supabase/tests/planning.sql` (חדש) | **32/32** |
| `supabase/tests/costing.sql` (שלב 8) | **43/43** |
| `supabase/tests/pricing.sql` (שלב 7) | **28/28** |
| `supabase/tests/rls-isolation.sql` | **42/42** |
| `supabase/tests/delete-guard.sql` | **17/17** |
| `supabase/tests/sub-recipe-hardening.sql` | **15/15** |
| `roundtrip:verify` (mapper) | OK, 12 assertions |
| `roundtrip:versions:verify` | OK, 30 assertions |

מה ש-`planning.sql` מוכיח מול המסד: ש-`kind` נשמר ונקרא חזרה; שהתוכנית
והשורה שורדות round-trip עם הכמות, היחידה, השעה וההערה; ש-on-hand של 4 נשמר,
של 0 נשאר 0, ושדה ריק נשאר NULL; שכמות יעד 0, יחידת יעד לא מוכרת ומלאי שלילי
נדחים; ששורת תוכנית **אינה** יכולה להצביע על מתכון של חשבון אחר — לא
ב-INSERT ולא ב-UPDATE; שנעילה בלי snapshot נדחית ו-snapshot בלי נעילה נדחה
ב-CHECK; שתוכנית נעולה אינה ניתנת לעריכה ושביטול הנעילה מוחק את ההקפאה;
שמעגל תת־מתכונים עוד בלתי אפשרי; ושמשתמש B אינו רואה, משנה, שותל או מוחק
כלום משל A — כולל `owns_plan` שמחזיר false ו-`anon` שנדחה בכל נתיב.

---

## 6. מה שלא נשבר

| יכולת | איך נבדקה |
|---|---|
| Auth, RLS | `rls-isolation.sql` 42/42 |
| Recipes, Versioning, שחזור | `sub-recipe-hardening.sql`, שני ה-round-trips |
| Sub-recipes ומחסום המחיקה | `sub-recipe-hardening.sql`, `delete-guard.sql` |
| מרכז חומרי הגלם, מחירי קנייה, ניצולת | `pricing.sql`, `costing.sql` |
| Food Cost, מחיר מכירה, Gross Margin, Markup | 728 טסטים ב-web, `costing.sql` |
| null ≠ 0, מחיר 0 ≠ מחיר חסר | שני ה-round-trips + `costing.sql` + `planning.sql` |
| snapshots היסטוריים | `costing.sql` §24-28 |
| נגישות ו-responsive | `stage5.mjs` — **62/62**, אפס גלישה אופקית |
| שרשרת הצפיפויות §5.1 | **לא נגעה.** `g_per_100` של הקטלוג לא נכנס לשרשרת |

סה"כ: **728 טסטים ב-web** (35 קבצים) ו-**195 במנוע** (8 קבצים) — כולם עוברים.
`typecheck`, `build`, `schema:check` (17 טבלות, 189 עמודות, 16 פונקציות
מוצהרות, 22 נבדקות להרשאות) ו-`seed:check` — נקיים.
`get_advisors` security — **רשימה ריקה**.

---

## 7. מגבלות שנותרו — במפורש

1. **HTTP E2E אמיתי עדיין לא הוכח.** מגבלת ה-egress על `*.supabase.co` בעינה.
   `stage5.mjs` רץ מול build שבו משתני Supabase **ריקים**, כלומר מול
   ה-Repository של הדמו: **אין בו אף בקשת PostgREST או GoTrue**, והוא אינו
   תחליף. הצד של המסד מוכח דרך ה-management API בלבד (§5).
2. **אין אופטימיזציה של כוח אדם או ציוד** (דרישה 10, במכוון). אין מודל של
   כמה תנורים או ידיים יש, ולכן אין מה לייעל ושום משימה לא מוזזת אוטומטית.
3. **תלות משויכת למוצר, לא לשלב.** המודל מקשר מתכון לתת־מתכון; אין קישור בין
   *שלב* לתת־מתכון שהוא צורך. לכן הכלל הוא השמרני — תת־מתכון מסתיים לפני השלב
   הראשון של המוצר — והוא נאמר על המסך במקום לנחש איזה שלב צורך מה.
4. **סיווג שלבים תלוי במשתמש.** מתכון קיים יציג "לא מסווג" עד שיסווגו אותו,
   למעט מה שהטמפרטורה מאפשרת. זו עלות מכוונת של "לא להמציא".
5. **המרת נפח דורשת צפיפות.** חומר שנמכר בליטרים ואין לו צפיפות אמינה מדווח
   כ"אי אפשר להמיר ליחידת רכישה" ואינו נכנס ל-total. אין נפילה ל-1.0 גר'/מ"ל
   ברשימת רכש (ב-`compute()` יש נפילה כזאת עם אזהרה, כי שם מתמחרים מתכון קיים
   ולא אומרים למישהו כמה בקבוקים לסחוב).
6. **תוכניות ייצור אינן נגישות במצב דמו** — אין חשבון, ולכן אין תוכניות. זה
   מכוון: תוכנית דמו הייתה נתון עסקי מומצא.
7. **מחיקת מתכון מוחקת שורות תוכנית שמפנות אליו** (`on delete cascade`).
   תוכנית **נעולה** שומרת את התוכן שלה ב-snapshot, וזה מה שהופך את זה לבטוח:
   התיעוד של מה שיוצר שורד את מחיקת המתכון. תוכנית פעילה תאבד את השורה — וזו
   ההתנהגות הנכונה, כי שורה בלי מתכון אינה אומרת כלום.
8. הטקסט שהוחל בפועל ב-`0018`/`0019` מקוצר בהערות לעומת הקבצים בריפו;
   הפקודות זהות. אומר זאת במקום להציג התאמה בית-בבית.

---

## 8. Presenzo ו-WhatsApp AI Assistant

**לא נגעתי בשום צורה** ב-Presenzo Production (`fglnzrpuffswapjsnppg`), ולא
ב-WhatsApp AI Assistant (`tbkcxrpenmpcwjicnxnd`) — שנשאר INACTIVE. כל הפעולות
בדוח הזה בוצעו על `qxdpsomelzpvphkhkqrw` בלבד. מפתח `service_role` לא נכנס
לדפדפן.

---

## 9. מצב המסד בסוף הריצה

```
users 0 · profiles 0 · recipes 0 · ingredients 0 · steps 0 · issues 0
versions 0 · notes 0 · calibrations 0 · ingredient_catalog 0
ingredient_purchases 0 · production_plans 0 · production_plan_items 0
production_plan_stock 0
density_table 34   ← נתוני עזר מקצועיים, אמורים להיות שם
```

כל ה-fixtures נמחקו, וכל מערך בדיקות מאשר את הניקוי של עצמו כשורה בטבלת
התוצאות שלו.
