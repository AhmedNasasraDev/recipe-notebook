# שלב 2 — תקלות קריטיות · שלב 3 — תוכנית לפני שינוי קוד

תאריך: 23.09.2026 · אחרי שלב 1 (`ae44689`). **לא שונה קוד.**

## שלב 2 — תיקון תקלות קריטיות

לפי הגדרת סעיף 6.2, "קריטי" = אובדן נתונים, שמירה שלא עובדת, קריסה, חישוב שגוי של כמות/עלות.
בסריקה של שלב 1 **לא נמצאה תקלה קריטית**. שתי הקריטיות שנמצאו בבדיקת הקבלה (מסך לבן בטעינה/כשל; מחיקה שנכשלת שמחקה תמונות) תוקנו ב־`f4b807d` ואומתו מחדש בבניית ייצור ב־22.09 (`QA-STATUS-2026-09-22.md`, ממצאים 1–2, וסקריפטים `v01`/`v02`/`v06`).
בדיקת שמירה/עריכה/שכפול/מחיקה/תמונות/כניסה מחדש/טיימרים/חישובים בבניית הייצור: `scripts-status/v05.out`, `v06.out`, `v04-status.out` — עוברות.

**תבנית דיווח (סעיף 10) — שלב 2**
- שלב: 2 — תקלות קריטיות
- קבצים ששונו: אין (רק הדוח הזה)
- תקלות שטופלו: אין — אין קריטיות פתוחות בטבלאות 6.1/6.2
- מה נעשה: אימות שאין פריט בדרגה "קריטי" בטבלאות, והפניה לראיות של התיקונים הקודמים
- הוכחת בדיקה: ראו `QA-STATUS-2026-09-22.md` סעיפים 3 ו־5, `scripts-status/*.out`
- מה לא נבדק: —
- בעיות חדשות: אין
- Commit: "spec stage 2: no critical defects open (no code changes)"
- ממתין לאישור להמשך לשלב 3

## שלב 3 — תקלות חמורות: מה עומד להשתנות (סעיף 7: "לפני כל שינוי קוד הצג אילו קבצים עומדים להשתנות, ולמה, וחכה לאישור")

התקלות החמורות בטבלה 6.2: A-1, A-4, A-5, A-6, A-11. A-1 (התפריט) שייכת לפי סעיף 7 ל**שלב 5** (סעיף 8), ולכן לא נוגעים בה כאן. נשארות ארבע, בשתי תת־קבוצות (סעיף 7: עד 4 תקלות, עצירה אחרי כל תת־קבוצה).

### תת־קבוצה 3א — A-11 + A-4 (תיקונים לזרימות קיימות)

**A-11 — קובצי תמונה נשארים אחרי מחיקת מתכון**
- `supabase/migrations/0040_recipe_image_uploader_read.sql` (חדש, additive): policy הקריאה `recipe_images_object_read` ב־`storage.objects` תקבל תנאי נוסף `or owner_id = auth.uid()::text` — אותו תנאי שכבר קיים ב־policy המחיקה (0039). בלי זה שירות ה־Storage "לא רואה" את הקבצים אחרי שהמתכון נמחק ומחזיר `200 []`. לא נמחק מידע, לא מורחבת קריאה של אף אחד אחר (רק מי שהעלה את הקובץ). **מחייב אישור להחלה על הפרויקט.**
- `apps/web/src/data/supabaseRepository.ts` (`deleteRecipe`): אחרי `remove()` לבדוק כמה קבצים באמת נמחקו (התשובה מכילה את הרשימה) ולרשום ל־console אזהרה כשהמספר קטן מהצפוי — בלי לשנות את סדר המחיקה ובלי לגעת בלוגיקת המחיקה עצמה (כלל ברזל 2).
- `apps/web/src/data/deleteOrder.test.ts`: בדיקה לאזהרה.
- אימות: `scripts-status/v08-delete-files.mjs` — אחרי התיקון התשובה צריכה להכיל את הקובץ, ובדיקת DB שאין קובץ חדש ללא רשומה.

**A-4 — הדבקה שומרת ישירות במקום להיפתח בעריכה**
- `apps/web/src/routes/PasteScreen.tsx`: במקום `saveRecipe(...)` + ניווט למתכון — ניווט ל־`/recipe/new` עם הטיוטה ב־`location.state` (המנגנון קיים: העורך כבר קורא `location.state.stage`), הכפתור ייקרא "המשך לעריכה ואישור". שום דבר לא נכתב ל־DB עד ש"שמירת המתכון" בעורך.
- `apps/web/src/routes/RecipeEditScreen.tsx`: קריאה של `location.state.draft` (מתכון מפוענח) לאתחול הטיוטה במקום `emptyDraft()`, כולל הערת הגרסה "יובא מהדבקת טקסט".
- `apps/web/src/features/recipe/draft.ts`: אין שינוי צפוי (`draftFromRecipe` קיים).
- בדיקות: `routes/PasteScreen.test.tsx` (קיים — הבדיקות שמצפות לשמירה ישירה ישתנו), `routes/RecipeEditScreen.test.tsx` (טיוטה מ־state).
- אימות: הדבקה → "פענוח" → נפתח העורך עם הרכיבים → ביטול → **אין מתכון חדש במחברת**; שמירה → קיים אחרי רענון.
- **דורש הכרעה שלך** (שלב 0, שאלה על A-4): לאשר את הזרימה החדשה.

### תת־קבוצה 3ב — A-5 + A-6 (יכולות ליבה חסרות מסעיף 5.1)

אלה **הוספות**, לא תיקונים. כלל ברזל 1 ("אל תשנה דבר שלא מופיע במשימה") — לכן אני מציע היקף מינימלי ומחכה לאישור או לדחייה:

**A-5 — ייצוא וגיבוי**
- `apps/web/src/features/backup/exportNotebook.ts` (חדש): בניית קובץ JSON של כל המחברת (מתכונים, רכיבים, שלבים, הערות, חומרי גלם, תוכניות) מהנתונים שכבר טעונים — בלי שדות פנימיים.
- `apps/web/src/routes/SettingsScreen.tsx`: כרטיס "גיבוי וייצוא" עם כפתור "הורדת גיבוי (JSON)".
- `apps/web/src/routes/RecipeScreen.tsx` (בתפריט ⋮ בשלב 5): "ייצוא מתכון (JSON)" — יתווסף רק בשלב 5 כדי לא לגעת במסך פעמיים.
- ייבוא חזרה — **לא** בהיקף (אין באפיון). בדיקות: `features/backup/exportNotebook.test.ts`.
- **דורש אישור ואפיון של שורה אחת**: מה נכלל (כל המחברת / מתכון בודד / שניהם).

**A-6 — יומן ניסויים**
- `apps/web/src/routes/RecipeEditScreen.tsx`: סעיף "יומן ניסויים" בשלב 4 של העורך (תאריך + הערה, הוספה/הסרה) על `recipe.trials` שכבר קיים במנוע וב־DB.
- `apps/web/src/features/recipe/draft.ts`: `trials` בטיוטה (draft ↔ recipe).
- `apps/web/src/routes/RecipeScreen.tsx`: תצוגת היומן בתוך "פרטים מקצועיים" (בשלב 5 יאוחד לכפתור "נתוני ייצור ועלויות").
- `data/mappers.ts` — לבדוק שהמיפוי של `trials` קיים בשמירה (RPC `save_recipe` כבר מקבל `p_trials`).
- בדיקות: `RecipeEditScreen.test.tsx`, `RecipeScreen.test.tsx`.
- **דורש אישור**.

### מה אני צריך ממך כדי להתחיל את 3א
1. אישור להחיל את מיגרציה 0040 (additive, policy קריאה ל־Storage בלבד).
2. אישור לזרימת ההדבקה החדשה (A-4).
3. תשובות לשאלות 1–4 משלב 0 (תמונות, יכולות שנדחו, הדפסה בכל המסכים, "שיתוף") — הן משפיעות על שלבים 4–5, לא על 3א, אבל עדיף לקבל אותן עכשיו.
