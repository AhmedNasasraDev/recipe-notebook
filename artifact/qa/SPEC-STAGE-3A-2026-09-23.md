# שלב 3 · תת־קבוצה 3א — A-11 + A-4

תאריך: 23.09.2026 · אחרי שלב 2 (`5e42804`) · אישור המשתמש: "מאשר 0040 ואת A-4, תמשיך ל-3א".
בדיקות דפדפן על **בניית ייצור מקומית** (`npm run build` → `vite preview` על 127.0.0.1:5199, קובץ `assets/index-DESj16AQ.js`), חשבון `nasasraah+qa1@gmail.com`, טלפון 402×874 ומחשב 1440×900. ה־Preview ב־Vercel **לא** נפרס מחדש בשלב זה (עדיין `f4b807d`).

## תבנית דיווח (סעיף 10)

**שלב:** 3, תת־קבוצה 3א (תקלות חמורות A-11, A-4).

**קבצים ששונו:**
| קובץ | מה השתנה |
|---|---|
| `supabase/migrations/0040_recipe_image_uploader_read.sql` | חדש. policy הקריאה `recipe_images_object_read` מקבלת תנאי נוסף `or owner_id = auth.uid()::text`. **הוחל על הפרויקט** `qxdpsomelzpvphkhkqrw` (אומת ב־`pg_policies`). |
| `apps/web/src/data/supabaseRepository.ts` | `deleteRecipe`: אחרי `remove()` משווה את הרשימה שחזרה לרשימה שנשלחה ורושם `console.warn` כשקבצים נשארו. סדר המחיקה ולוגיקת המחיקה לא השתנו. |
| `apps/web/src/data/deleteOrder.test.ts` | 2 בדיקות חדשות: אזהרה כשנמחקו פחות קבצים; שקט כשכולם נמחקו. |
| `apps/web/src/routes/pastedDraft.ts` | חדש. טיפוס `PastedDraftState` + `pastedDraftFrom(state)` + הקבוע `PASTE_VERSION_NOTE`. |
| `apps/web/src/routes/PasteScreen.tsx` | הכפתור "שמירה למחברת" הוחלף ב־"המשך לעריכה ואישור": ניווט ל־`/recipe/new` עם הטיוטה ב־`location.state`. **אין יותר `saveRecipe` במסך הזה.** כותרת הכרטיס "המשך לעריכה"; טקסט עזר מסביר ששום דבר לא נשמר עד "שמירת המתכון" בעורך. |
| `apps/web/src/routes/RecipeEditScreen.tsx` | קורא `location.state` פעם אחת במאונט; אם יש טיוטה מודבקת — הטופס מתחיל ממנה (id ריק). `original` נשאר ריק ⇒ יציאה בלי שמירה שואלת. הערת הגרסה בשמירה הראשונה: "יובא מהדבקת טקסט". |
| `apps/web/src/routes/PasteScreen.test.tsx` | הבדיקה "saves what was parsed" הוחלפה ב־"hands what was parsed to the editor… saves NOTHING itself" (+ בדיקת הטקסט); שמות כפתורים עודכנו. |
| `apps/web/src/routes/RecipeEditScreen.test.tsx` | describe חדש (3 בדיקות): נפתח כ"מתכון חדש" עם שם/שורות/שלבים; לא נשמר עד הלחיצה ואז נשמר בדיוק מה שהודבק עם id חדש; `/recipe/new` רגיל עדיין ריק. |
| `DEPLOY.md` | שורת תיעוד למיגרציה 0040. |
| `artifact/qa/…` | הדוח הזה, `scripts-spec/s3a-*.mjs` + `*.out`, `shots-spec-s3a/`. |

**תקלות שטופלו:** A-11 (קובצי תמונה נשארים ב־Storage אחרי מחיקת מתכון), A-4 (הדבקה שומרת ישירות במקום להיפתח בעריכה).

**מה נעשה:**
- A-11 — שורש התקלה: `remove()` של Supabase Storage קודם *בוחר* את הקבצים לפי policy הקריאה ומוחק רק מה שמצא; אחרי מחיקת שורת המתכון `can_read_recipe` לא מזהה יותר את הבעלים ⇒ `200 []` וקבצים יתומים. 0040 מוסיף לקריאה את מי שהעלה את הקובץ (בדיוק כמו 0039 למחיקה). בקוד — הדליפה הופכת לגלויה (אזהרה) במקום שקטה.
- A-4 — מסך ההדבקה לא כותב יותר ל־DB. התוצאה המפוענחת עוברת לעורך כטיוטה דרך `location.state` (לא ב־URL ולא ב־storage, כך שרענון של `/recipe/new` מתחיל ריק). המתכון נוצר רק ב"שמירת המתכון" בעורך.

**הוכחת בדיקה:**

אוטומטי (23.09.2026):
| בדיקה | תוצאה |
|---|---|
| `npm test` (מנוע + web) | מנוע 250/250, web **1437/1437** עוברות (76 קבצים) |
| `npm run typecheck` | עובר |
| `npm run lint` | 0 שגיאות, 35 אזהרות `no-non-null-assertion` — כולן קיימות מלפני (לא בקבצים ששונו) |
| `npm run build` | עובר (`index-DESj16AQ.js`) |

דפדפן — A-11 (`scripts-spec/s3a-delete.mjs` → `s3a-delete.out`, צילומים `A11-1..3`):
1. נוצר מתכון בדיקה `בדיקה-QA מחיקת קובץ 3א` (id `18efbbe3-…`), הועלתה תמונה → `POST /storage/v1/object/recipe-images/18efbbe3-…/95ec10f1-….webp` = 200.
2. רענון → "עוד פעולות" → "מחיקת בדיקה-QA…" → "כן, למחוק".
3. `DELETE /storage/v1/object/recipe-images` = 200 עם גוף **`[{"name":"18efbbe3-…/95ec10f1-….webp", …}]`** (לפני 0040: `[]`, ראו `QA-STATUS-2026-09-22.md` S1). אין אזהרות console.
4. SQL אחרי: `test_files_left = 0`, `test_recipe_rows = 0`, `orphan_files_total = 5` (ללא שינוי — 5 היתומים הישנים נשארו, לא נגעתי בהם).

דפדפן — הוכחה ש־0040 לא מרחיב קריאה (`s3a-rls.mjs` → `s3a-rls.out`): בקשת signed URL לתמונת הבריוש של qa1:
| חשבון | תוצאה | הסבר |
|---|---|---|
| qa1 (בעלים) | 200 | כמו קודם |
| qa2 (תלמיד בקבוצה שבה הבריוש פורסם) | 200 | דרך `can_read_recipe` — כמו קודם, לא דרך התנאי החדש |
| qa3 (לא בקבוצה, לא העלה) | **400 / "Object not found"** | נדחה — אין הרחבה |

דפדפן — A-4 (`s3a-paste.mjs` → `s3a-paste.out` + `s3a-paste.json`, צילומים `A4-1..6`):
| צעד | תוצאה |
|---|---|
| מתכונים במחברת לפני | 3 |
| הדבקה → "פענוח" | כפתורים במסך: פענוח · **המשך לעריכה ואישור** · המחברת; טקסט "שום דבר לא נשמר במחברת עד שלוחצים "שמירת המתכון" בעורך." |
| "המשך לעריכה ואישור" | `/recipe/new`, כותרת "מתכון חדש", שם `בדיקה-QA בריוש מהדבקה` |
| שלב 2 בעורך | `קמח לחם|500|g`, `חלב|60|ml`, `מלח|11|g` |
| שלב 3 בעורך | 2 שלבים עם הטקסט המקורי |
| קריאות `save_recipe` עד כאן | **0** |
| "ביטול" | דיאלוג "יש שינויים שלא נשמרו. לצאת בלי לשמור?" → אישור → חזרה ל־`/paste` |
| מתכונים אחרי ביטול / קריאות שמירה | **3 / 0** — לא נוצר כלום |
| הדבקה שוב → עורך → שלב 4 → "שמירת המתכון" | `/recipe/9b95207c-…`, קריאת `save_recipe` אחת |
| רענון הדף | הכותרת והרכיבים קיימים; מתכונים במחברת 4 |
| הערת גרסה (`s3a-note.mjs`, מתכון נפרד `b4f90a36-…`) | הגוף של `save_recipe` מכיל `p_version_note: "יובא מהדבקת טקסט"` |
| מחיקת שני מתכוני הבדיקה | מתכונים במחברת חזרו ל־3 |
| `/recipe/new` רגיל אחרי הכול | ריק (אין "הדבקה חוזרת") |
| מחשב 1440×900 | אותה זרימה: העורך נפתח עם השם (צילום `A4-6-editor-desk.png`) |

**מה לא נבדק:**
- ה־Preview ב־Vercel — לא נפרס מחדש (לא ביקשת פריסה בשלב זה), ולא ניתן לפתוח אותו מהסביבה הזאת (חסימת `*.vercel.app`, כפי שתועד ב־`QA-STATUS-2026-09-22.md`). כל האימות בבניית ייצור מקומית זהה בקוד.
- מכשיר Android פיזי — אין בסביבה.
- כרטיס "גרסאות" במסך המתכון אחרי השמירה הראשונה — לא הוצג (אין עדיין גרסה קודמת להשוואה); ההערה אומתה בגוף הבקשה לשרת ולא בממשק.
- מחיקה כשמשתמש אחר העלה תמונה למתכון שלי (מקרה תלמיד/מדריך) — לא נבדק בשלב זה; ההתנהגות שם לא השתנתה (הקובץ של המעלה נשאר קריא/מחיק לו, כמו ב־0039).

**בעיות חדשות:** אין. (הערה: שרת ה־preview המקומי נפל פעם אחת בין ריצות — בעיית סביבה, לא של האפליקציה.)

**Commit:** ראו את ההודעה בקומיט הזה ("spec stage 3a: A-11 storage read policy 0040 + delete warning; A-4 paste opens the editor as a draft").

**ממתין לאישור** להמשך לתת־קבוצה 3ב (A-5 ייצוא/גיבוי, A-6 יומן ניסויים) — או להנחיה לדלג עליהן לשלב 4.
