# שלב 10 (בהמשך לסדר עדיפויות) — A-2: הקטנת כפתור ההדפסה במצב הכנה

תאריך: 23.09.2026 · אחרי שלב 9 (`bcce601`) · הנחיית המשתמש: "כן, תקטין גם שם".

## מה שונה

| קובץ | מה השתנה |
|---|---|
| `apps/web/src/routes/CookScreen.module.css` | `.printBtn` (הכפתור "הדפסה / PDF" בסרגל העליון של מצב הכנה) עבר מסגנון מלא בצבע ההדגשה לסגנון שקט זהה ל-"מסך מלא" שלידו: רקע שקוף, מסגרת `--c-cook-line`, דיו `--c-cook-muted`, בלי `font-weight: 600`. `printBtn:focus-visible` שונה מ-`--c-cook-text` ל-`--c-cook-accent` כדי להתאים לטבעת הפוקוס של "מסך מלא". מיקום, תווית, אייקון וגובה 44px לא השתנו — רק המשקל החזותי. |

**לא נגעתי** בשום קובץ אחר. הכפתור עדיין באותו מקום, עדיין קורא ל-`window.print()`, עדיין בגודל 44 פיקסל.

## הוכחת בדיקה

אוטומטי: מנוע 250/250; web **1480/1480**; typecheck, lint (0 שגיאות), build — עוברים.

דפדפן (`scripts-spec/s10-cook-verify.mjs`; צילום `shots-spec-s10/1-cook-top-bar-phone.png`):
| בדיקה | תוצאה |
|---|---|
| סגנון כפתור "הדפסה / PDF" | רקע שקוף, מסגרת `#CDD4CE`, דיו `#616B65`, גובה 44px, רוחב 126px |
| סגנון כפתור "מסך מלא" (להשוואה) | **זהה**: רקע שקוף, מסגרת `#CDD4CE`, דיו `#616B65`, גובה 44px |
| הכפתור עדיין מפעיל הדפסה | כן (נבדק עם מוק ל-`window.print`) |

## מה לא נבדק

Preview ב-Vercel; Android פיזי; הדפסה פיזית בפועל.

## בעיות חדשות

אין.

**Commit:** "spec follow-up: quiet the cook-screen print button to match 'מסך מלא' (A-2, §3.3, Ahmed's approval)".

## סיכום — מה עוד פתוח

**שום דבר.** זו הייתה ההכרעה האחרונה שנותרה מרשימת חמש הנקודות המקוריות (תמונות נסגרה בתשובתך הקודמת ללא שינוי קוד). כל שבעת שלבי האפיון וכל ארבעת תיקוני ההמשך נמצאים בענף `claude/recipe-notebook-full-system-qa-fb7p1y`, נבדקו ותועדו.
