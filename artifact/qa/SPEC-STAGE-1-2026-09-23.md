# שלב 1 — סריקה מלאה ומילוי טבלה 6.2 (ללא שינוי קוד)

תאריך: 23.09.2026 · ענף: `claude/recipe-notebook-full-system-qa-fb7p1y` · נקודת התחלה: `34b5dec` (שלב 0) · קוד נבדק: `f4b807d` (בניית ייצור מקומית, `vite preview`, קובץ `assets/index-BDv2EgnB.js`)
מסכי בדיקה: טלפון 402×874 ומחשב 1440×900, חשבון `nasasraah+qa1@gmail.com`. צילומי מסך: `artifact/qa/shots-spec-s1/` (36 מסכים + 6 בדיקות ממוקדות). סקריפט ופלט: `artifact/qa/scripts-spec/s1-scan.mjs`, `s1-scan.json`, `s1-scan.out`.
**לא שונה קוד. לא נמחק שום נתון.**

## 1. אימות טבלה 6.1 (התקלות שהזנת)

| # | קיימת? | מה נמצא בפועל | קבצים אחראים |
|---|---|---|---|
| U-1 | **כן, מאומת** | בתחתית מסך המתכון יש אזור מתקפל "עוד פעולות" (הדפסת המתכון, תווית מוצר, דף הזמנה, שכפול, מחיקה). בסרגל העליון יש כבר כפתור ⋮ (44×44, aria-label "עוד פעולות על המתכון") — אבל לחיצה עליו **גוללת 2,555px למטה** ופותחת את האזור התחתון; אין תפריט (`role="menu"` = 0), אין `aria-haspopup`, Escape לא סוגר. פעולות נוספות מפוזרות בגוף המסך: "מצב הכנה" (כפתור גדול), "עריכה" ו"☆ הוספה למועדפים" (שורת כפתורים), "התאמת מיקום התמונה" / "מחיקת התמונה" / "הוספת תמונה" (בכרטיס התמונות), "הדפסה / שמירה כ-PDF" (סרגל עליון). צילום: `U1-after-menu-click.png` | `routes/RecipeScreen.tsx` (כפתור ⋮ בשורות ~604–625, האזור התחתון ~1118–1180, שורת עריכה/מועדפים ~1100), `features/recipe/recipe.module.css` (`.menuBtn`, `.moreSummary`, `.printRow`, `.actionRow`) |
| U-2 | **כן, מאומת** | כפתור "הדפסה / שמירה כ-PDF" בסרגל העליון של מסך המתכון: 207×44px, מלא בירוק (`#496451`), הבולט ביותר במסך. אותו כפתור קיים גם במצב הכנה ("הדפסה / PDF") ובדף ההזמנה (למעלה ולמטה). | `routes/RecipeScreen.tsx` ~594–603 (`.printTop`), `routes/CookScreen.tsx` ~481–490 (`.printBtn`), `routes/OrderScreen.tsx` ~214–220 ו־~541 |
| U-3 | **חלקית — התמונה כן מוצגת, אבל לא לפי סעיף 8.3** | תמונת Hero קיימת: רוחב מלא בטלפון (402/402), 640px בתוך עמודת הקריאה במחשב, `object-fit: cover`, מיקוד תמונה נשמר; שם המתכון מתחתיה. **פערים מול 8.3**: (א) הגובה אינו קבוע — 262px בטלפון, 270px במחשב, נגזר מהתמונה; (ב) מתכון **בלי תמונה** — אין Hero ואין fallback מעוצב (רק "הוספת תמונה" בכרטיס התמונות למטה); (ג) **קובץ חסר** (חתימת ה־URL נכשלת) — אין Hero, ההודעה "התמונה לא נמצאה בשרת." מופיעה רק בכרטיס למטה; (ד) יש רדיוס מעוגל תחתון ומסגרת כרטיס. צילומים: `U3-no-image.png`, `U3-broken-image.png`, `phone-recipe.png`, `desk-recipe.png` | `routes/RecipeScreen.tsx` ~655–670, `features/recipe/recipe.module.css` (`.hero`, `.heroPhoto`), `features/images/useRecipeImages.ts` |
| U-4–U-6 | ריקות במסמך | — | — |

## 2. טבלה 6.2 — תקלות ופערים שנמצאו בסריקה

דרגות לפי סעיף 6.2: **קריטי** (אובדן נתונים / שמירה / קריסה / חישוב שגוי) · **חמור** (פעולה שלא עובדת או לא נגישה) · **קוסמטי** (עיצוב, יישור, טקסט, חוסר עקביות עם סעיף 3).

| # | מסך / קובץ | תיאור | איך משחזרים | חומרה | הערות |
|---|---|---|---|---|---|
| A-1 | מסך מתכון · `RecipeScreen.tsx`, `recipe.module.css` | = U-1: אין תפריט ⋮ אמיתי; הכפתור גולל לאזור "עוד פעולות" בתחתית; פעולות מפוזרות ב־5 מקומות במסך | פתח מתכון → לחץ ⋮ → הדף גולל לתחתית; Escape לא סוגר | חמור | מטופל בשלב 5 (סעיף 8.1–8.2). רשימת הפעולות הקיימות היום (כדי ששום פעולה לא תאבד): המחברת (חזרה) · הדפסה/PDF · מצב הכנה · עריכה · מועדפים · התאמת מיקום התמונה · החלפת התמונה · מחיקת התמונה · הוספת תמונה · תווית מוצר · דף הזמנה · שכפול · מחיקה · הערה אישית · השוואת/צפייה/שחזור גרסאות. "שיתוף" — **לא קיים** (שאלה 4 משלב 0) |
| A-2 | מסך מתכון, מצב הכנה, דף הזמנה | = U-2: כפתור הדפסה גדול ובולט בסרגל העליון (207×44, ירוק מלא) | פתח מתכון | קוסמטי | תלוי בתשובה לשאלה 3 (האם גם במצב הכנה ובדף הזמנה) |
| A-3 | מסך מתכון · `RecipeScreen.tsx`, `recipe.module.css` | = U-3: Hero בלי גובה קבוע, בלי fallback למתכון בלי תמונה, בלי fallback לקובץ חסר, עם רדיוס/מסגרת | פתח `בדיקה-QA גנאש` (בלי תמונה) → אין Hero; חסום את `storage/v1/object/sign` → אין Hero והודעה רק למטה | קוסמטי | שלב 5 (8.3). הצעת גובה: 220px טלפון / 320px מחשב |
| A-4 | הדבקת מתכון · `routes/PasteScreen.tsx:121` | תוצאת הפענוח **נשמרת ישירות למחברת** בלחיצה על "שמירה למחברת" ועוברת למסך המתכון — לא נפתחת במסך העריכה לאישור, כפי שסעיף 5.1 דורש ("תמיד נפתחת במסך עריכה לאישור ולעולם לא נשמרת אוטומטית") | הדבקה → "פענוח" → "שמירה למחברת" → נוצר מתכון; העריכה רק אחרי כן | **חמור** (סטייה מכלל מחייב בסעיף 5.1) | לא "אוטומטי" במובן שהמשתמש לוחץ שמירה אחרי תצוגה מקדימה — אבל לא דרך מסך העריכה. **דורש הכרעה שלך**: לשנות לזרימה "פענוח → מסך עריכה (טיוטה) → שמירה"? |
| A-5 | כל האפליקציה | **אין ייצוא/גיבוי** של המחברת (JSON/קובץ). קיים רק PDF דרך הדפסה. סעיף 5.1: "גיבוי וייצוא; ייצוא מתכון ל־PDF" | חפש "ייצוא"/"גיבוי" בכל המסכים — אין | חמור (יכולת ליבה חסרה) | זו הוספת יכולת, לא תיקון — מחייבת אישור ואפיון קצר (פורמט, מה נכלל) |
| A-6 | מסך מתכון / עורך | **אין ממשק ליומן ניסויים** ("יומן ניסויים עם תאריכים", סעיף 5.1). הטבלאות `trials`/`issues` קיימות ב־DB ובמנוע, אבל אין שדה בעורך ואין תצוגה במסך המתכון | פתח עריכה → אין סעיף ניסויים | חמור (יכולת ליבה חסרה) | הוספת יכולת — מחייבת אישור ואפיון |
| A-7 | 13 מסכים (טלפון) · CSS של כל מסך, `styles/tokens.css` (`--hit-compact: 40px`) | **אזורי לחיצה מתחת ל־44×44** (סעיף 3.3): 71 רכיבים בטלפון. רובם כפתורים בגובה 40–42px (צ׳יפים "גרמים/ביתי/כמו במתכון", קטגוריה, "התאמת מיקום התמונה", "מחיקת התמונה", "מסך מלא" ו"הדפסה / PDF" במצב הכנה, "שיעור חדש"/"מחיקת הקורס"/"הסרה" בקבוצה, כפתורי ההזמנות בהרשאות, 14 צ׳יפי יחידות ו"שמירת השם" בהגדרות, "הסרת שורה" בתוכנית); תיבות סימון 24×24 (עורך, הרשאות) ו־28×28 (שקילה); קישורים בתוך טקסט: "להזין מחיר במרכז חומרי הגלם" 157×19 (דף הזמנה), שם התוכנית 134×24 (רשימת תוכניות), "הדבקה" 64×40 (מחברת) | `s1-scan.json` → `phone.*.small` | קוסמטי (סעיף 3) — אבל נגישות | שלב 4: העלאת `--hit-compact` ל־44 מתקנת את הרוב במכה אחת; תיבות סימון וקישורי טקסט דורשים טיפול נקודתי |
| A-8 | כל המסכים · `styles/tokens.css`, `styles/fonts.css` | **גופנים**: Heebo בלבד בכל הרכיבים כולל כותרות ועמוד המתכון; **Frank Ruhl Libre לא קיים** (3.2). **צבעים**: כל הערכים שונים מ־3.1 (רקע `#f7f5ef`≠`#E9ECE8`, נייר `#fffefa`≠`#FBFBF9`, דיו `#292d29`≠`#171A18`, ירוק `#496451`≠`#1E6B4C`, קווים `#ded8cc`≠`#CDD4CE`; אין ענבר/אדום/שוליים חמים לפי הקוד המדויק) | `s1-scan.json` → `fonts`; `tokens.css` | קוסמטי (סעיף 3) | שלב 4 — כולל הוספת Frank Ruhl Libre כקובץ בפרויקט ועדכון מבחני העיצוב |
| A-9 | הדבקת מתכון · `routes/PasteScreen.module.css` | **מיקוד מקלדת לא נראה** על הכפתור הראשון ("פענוח"): `outline: none`, בלי חלופה (3.3: "מיקוד מקלדת נראה"). בשאר המסכים יש מסגרת מיקוד 2px | Tab אל "פענוח" | קוסמטי (נגישות) | 14 קובצי CSS מגדירים `:focus-visible`; זה חסר |
| A-10 | מסך מתכון | **נתוני ייצור מפוזרים בעמוד** (3.3 דורש "מאחורי כפתור אחד — נתוני ייצור ועלויות"): שורת התפוקה מתחת לשם ("12 יחידות · 100 גר' ליחידה · 2 שע' 12 דק'"), באנר "נתונים חלקיים — … העלות והמחיר מחושבים מ-6 רכיבים", בלוק "כמה להכין?" עם אזהרת "האצווה מפיקה כ-8.2 יחידות", "נוסחה מאושרת לייצור". הכפתור הקיים נקרא "פרטים מקצועיים", לא "נתוני ייצור ועלויות". 33 שורות עם מספרים מחוץ לכפתור (מתוכן 8 שורות רכיבים לגיטימיות) | פתח מתכון | קוסמטי (סעיף 3.3) | שלב 5 — צריך הכרעה מה נשאר גלוי (הצעה: רק שם, תמונה, רכיבים, שלבים; התפוקה והסקיילינג מאחורי הכפתור) |
| A-11 | מחיקת מתכון · `data/supabaseRepository.ts` (`deleteRecipe`), policy קריאה 0029 | אחרי מחיקה **מוצלחת** של מתכון עם תמונה, קובצי התמונה נשארים ב־Storage: בקשת המחיקה חוזרת `200 []` כי שירות ה־Storage בוחר קבצים לפי policy הקריאה, שמזהה בעלות דרך מתכון שכבר איננו | מתכון חדש → תמונה → מחיקה → הקובץ נשאר (`scripts-status/v08-delete-files.mjs`) | חמור (פעולת ניקוי שלא עובדת; **אין** אובדן נתונים או דליפה) | תיקון מוצע: מיגרציה additive 0040 (policy קריאה גם למי שהעלה). ידוע מדוח המצב (S1) |
| A-12 | טעינה ראשונה | טעינה קרה של האפליקציה 3.4–5.9 שנ׳ בבניית ייצור (חבילת JS יחידה 813 kB) | פתח כתובת ישירה | הערה (לא בסולם) | ידוע (#15); לא בהיקף התיקונים אלא אם תחליט |
| A-13 | הדבקת מתכון | הטקסט "פענוח חכם דרך מודל שפה אינו זמין: הוא מחייב שרת מתווך עם מגבלת קצב, כדי שמפתח ה-API לא יהיה בדפדפן" — הסבר טכני למשתמש | פתח "הדבקה" | קוסמטי | שלב 6 |
| A-14 | מסך המתכון במחשב | ה־Hero תופס 640 מתוך 760px של המסגרת (עמודת הקריאה); לפי 8.3 "רוחב מלא של אזור התוכן" — אם "אזור התוכן" = העמודה, זה תקין | `desk-recipe.png` | לבירור | לא סופר כתקלה עד הכרעה |

**מה נסרק ונמצא תואם** (בלי ממצא): RTL בכל 18 המסכים (`dir=rtl`); הידרציה מוצגת רק כשיש קמח (גנאש — לא), טמפ' מים (DDT) רק במצב בצק; המנוע והמסכים כבר מכסים: תפוקה/פחת אפייה/תפוקה בפועל מול תיאורטית, עלות ליחידה/Food Cost/מחיר מוצע, אלרגנים, חיי מדף/אחסון/הקפאה/הפשרה, המרות עם צפיפות, גרסאות עם תאריכים ונעילה, הקטנה/הגדלה לפי רכיב מגביל ("לפי מלאי"), תת־מתכונים עם גלגול, ציוד, PDF. אין סימנים פנימיים (V1, (88), §, קודי יחידות) בשום מסך; אין טקסט מתחת ל־12px (מדוח המצב).

## 3. יכולות שנדחו (5.3) — מצב בפועל

מצב מטבח עם טיימרים, תווית מוצר ואחוז מים — **קיימים ועובדים** בקוד. לפי שאלה 2 משלב 0 אני לא נוגע בהם עד הכרעתך.

## תבנית דיווח (סעיף 10)

- **שלב**: 1 — סריקה מלאה ומילוי טבלה 6.2
- **קבצים ששונו**: `artifact/qa/SPEC-STAGE-1-2026-09-23.md`, `artifact/qa/shots-spec-s1/` (42 צילומים), `artifact/qa/scripts-spec/` (סקריפט + פלט). **אפס שינויי קוד.**
- **תקלות שטופלו**: אין (שלב ללא תיקונים)
- **מה נעשה**: אימות U-1/U-2/U-3 בדפדפן ואיתור הקבצים; סריקת 18 מסכים בטלפון ובמחשב (גופנים, RTL, אזורי לחיצה, מיקוד מקלדת); בדיקות ממוקדות של מסך המתכון מול סעיף 8 (תפריט, מקלדת, Hero עם/בלי תמונה/קובץ חסר); בדיקת היקף 5.1 מול הקוד; 14 שורות בטבלה 6.2
- **הוכחת בדיקה**: `shots-spec-s1/*.png`, `scripts-spec/s1-scan.out` ו־`s1-scan.json` (מדידות: מיקום וגודל כפתורים, גלילה 2,555px בלחיצת ⋮, גובה Hero, רשימת רכיבים <44px לכל מסך)
- **מה לא נבדק ולמה**: ה־Preview ב־Vercel עצמו (חסום מהסביבה — נבדקה בניית ייצור של אותו קומיט); ניגודיות מול פלטת 3.1 (הפלטה עוד לא קיימת בקוד — תיבדק בשלב 4 אחרי ההחלפה); Android אמיתי
- **בעיות חדשות שהתגלו**: A-4 (הדבקה שומרת ישירות), A-5 (אין ייצוא/גיבוי), A-6 (אין יומן ניסויים), A-9 (מיקוד לא נראה בהדבקה), A-13 (טקסט טכני בהדבקה); השאר ידועות או נגזרות של U-1–U-3 וסעיף 3
- **Commit**: "spec stage 1: full scan, table 6.2 (no code changes)"
- **ממתין לאישור להמשך לשלב 2** (תיקון תקלות קריטיות). **שים לב**: בטבלה 6.2 **אין תקלה קריטית** לפי הגדרת הסעיף (אובדן נתונים/שמירה/קריסה/חישוב שגוי) — שתי הקריטיות שנמצאו בסבבים הקודמים כבר תוקנו ואומתו. לכן שלב 2 ריק, ואפשר לעבור ישר לשלב 3 (חמורות: A-1, A-4, A-5, A-6, A-11) — אבל A-4/A-5/A-6 דורשות הכרעה ואפיון קצר ממך לפני שאגע בהן, ו־A-11 דורשת אישור למיגרציה additive. עדיין פתוחות גם שאלות 1–4 משלב 0.
