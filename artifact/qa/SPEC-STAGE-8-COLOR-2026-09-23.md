# שלב 8 (בעקבות "תאשר") — החשכת האפור והענבר לנגישות מלאה

תאריך: 23.09.2026 · אחרי שלב 7 (`512d2f3`) · הנחיית המשתמש: "תאשר" בתגובה לרשימת חמש ההכרעות הפתוחות.

## איך פירשתי "תאשר" — חמש ההכרעות, אחת אחת

מילה אחת שהתייחסה לחמישה פריטים שונים. כדי לא לנחש בשקט, זו הפרשנות שלי לכל אחד, וכתבתי אותה גם בקוד:

1. **תמונות (URL מול העלאה):** לא בוצע שינוי. פירשתי כאישור למצב הקיים — העלאת קבצים כפי שהיא.
2. **כפתור הדפסה במצב הכנה/הזמנה:** לא בוצע שינוי. שתי האפשרויות הוצגו כשוות ("להקטין... או להשאיר"), ובלי הבהרה נוספת בחרתי להשאיר את המצב הקיים — זו הפעולה הבטוחה יותר.
3. **צבעי אפור וענבר:** **בוצע שינוי** — זו ההמלצה היחידה שנתתי במפורש בדוח שלב 4 ("אני ממליץ על ב"), ולכן "תאשר" מתייחס אליה בבירור. פורט למטה.
4. **תוכן גלוי בדף המתכון (A-10):** לא בוצע שינוי. פירשתי כאישור להיקף המצומצם שכבר יישמתי בשלב 5ב (כותרת וכרטיס הסקיילינג נשארים גלויים).
5. **יכולות קיימות (מצב הכנה, טיימרים, תווית):** לא בוצע שינוי. פירשתי כאישור שאי-הנגיעה בהן הייתה נכונה.

**אם הפרשנות לסעיפים 1, 2 או 4 שגויה — תגיד, וזה שינוי קטן וממוקד בכל אחד מהם.**

## מה שונה בפועל (סעיף 3)

| קובץ | מה השתנה |
|---|---|
| `apps/web/src/styles/tokens.css` | `--c-muted-ink` (אפור): `#6E7A73` → `#616B65`. `--c-amber` (ענבר): `#A56A0E` → `#8C5A0C`. `--c-amber-bg` (רקע ההודעה): `#EFE7D8` → `#EBE4D8` (התערובת המחושבת מחדש של הענבר החדש על הנייר). אותו גוון בשני המקרים, רק כהה יותר — לא צבע חדש. |
| `apps/web/src/styles/tokens.test.ts` | טבלת ה-PALETTE, החריגים ב-SPEC_PAIRS, וסף הניגודיות בכל הבדיקות שמדדו את הערכים הישנים — עודכנו לערכים החדשים ולסף המלא 4.5:1 (בלי הנחות). |

**לא נגעתי בשום דבר אחר** — לא בלוגיקה, לא בפריסה, לא בגופנים.

## הוכחת בדיקה

אוטומטי: מנוע 250/250; web **1480/1480** (81 קבצים, ללא שינוי במספר); typecheck, lint (0 שגיאות), build — עוברים. בדיקת ההגנה על מערכת העיצוב (`tokens.test.ts`) עברה עם **סף מלא 4.5:1 בכל מקום**, בלי אף חריג חדש.

דפדפן (`scripts-spec/s8-color-verify.mjs`, `s8-color-verify2.mjs`; צילומים `shots-spec-s8/`), נמדד בפועל בצבעים המחושבים בדף אמיתי:
| מקום | לפני | אחרי (מדוד בדפדפן) |
|---|---|---|
| טקסט משני (שורת התפוקה מתחת לשם המתכון) על משטח העבודה | 3.76:1 | **4.64:1** |
| תג "נתון מערכת" (ענבר) על נייר הממשק | 4.34:1 | **5.66:1** |
| הודעת אזהרה ("לא הוגדר מחיר מכירה…") — הענבר על רקע ההודעה שלו | 3.66:1 | **4.64:1** |

כל שלושת המספרים בעמודה האחרונה עברו סף 4.5:1 — נגישות AA מלאה לטקסט רגיל, לא רק לטקסט גדול.

## מה לא נבדק

Preview ב-Vercel (לא נפרס מחדש); Android פיזי; קורא מסך אמיתי (הניגודיות המספרית נבדקה, לא חוויית משתמש עם טכנולוגיה מסייעת).

## בעיות חדשות

אין.

**Commit:** "spec follow-up: darken grey and amber for full AA contrast (Ahmed's 'תאשר')".

זהו תיקון ממוקד בעקבות אישורך, לא שלב חדש בתהליך. כל שבעת שלבי האפיון נותרים כפי שהיו בדוח הסיום (`SPEC-STAGE-7-FINAL-2026-09-23.md`), עם ארבע הכרעות שנותרו פתוחות (סעיפים 1, 2, 4, 5 למעלה) ככל שהפרשנות שלי להן שגויה.
