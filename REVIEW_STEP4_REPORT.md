# דוח שלב 4 — יצירה ועריכה של מתכונים, וכיול אישי

קומיט: `33756af` על `claude/recipe-notebook-initial-review-fwfb2e`
פרויקט Supabase: `qxdpsomelzpvphkhkqrw` (Recipe Notebook, eu-central-1)

---

## 0. הסריקה שקדמה לקוד

לפני שכתבתי שורה אחת סרקתי את המימוש הקיים. זה מה שנמצא ולכן **לא** נבנה
מחדש:

| מה | מצב | שימוש בשלב 4 |
|---|---|---|
| טיפוסי הדומיין (`Recipe`, `IngredientLike`, `Step`, `Pan`, `RecipeIssue`, `RecipeTrial`, `Batch`) | מלאים מ-§1.1 | הטופס נבנה סביבם, לא הוגדר מודל חדש |
| `UNITS` — 15 יחידות + `LEGACY_UNIT_NAMES` + `unitLabel` | קיים | בורר היחידות בטופס |
| `compute()`, `scaleFactor()`, `calcState()` | קיימים | התצוגה החיה בעורך קוראת להם ישירות |
| **מנוע הכיול כולו** — `createCalibration` (מקפיא `toolMl`), `upsertCalibration`, `findCalibration`, `suggestCalibrations`, `TOOL_OPTIONS` | **מלא ותקין** | נבנה **רק UI**. אף שורה במנוע לא נגעה |
| `mappers.ts` בשני הכיוונים כולל `null ≠ 0` | קיים | שימוש ישיר |
| `SupabaseRepository` | list / get / save / prefs / calibrations | **נוסף רק `deleteRecipe`** |
| `DEMO_CATEGORIES` | קיים | בורר הקטגוריות |
| כפילי הטסטים (fakeSupabase שמדמה policies, fakeAuth, memoryIdb) | קיימים | בסיס טסטי שלב 4 |

**מנוע החישוב לא שונה בכלל.** `git status packages/engine/src/` ריק.

והתגלית שקבעה את עיצוב הטופס: `num('') → 0` אבל `numOrNull('') → null`.
המשמעות בסעיף 2.

---

## 1. מה נבנה

### מסך יצירה ועריכה (`/recipe/new`, `/recipe/:id/edit`)

מסך אחד לשני המקרים, כי הם חולקים כל שדה. מה שיש בו:

- **זהות המתכון** — שם, קטגוריה, תגים, סימון "מתכון בסיס" (§18.6)
- **רכיבים** — שם, כמות, יחידה (15 יחידות בשלוש קבוצות), והוספה/הסרה/סידור.
  תחת `details` מתקפל: מחיר ויחידת מחיר, צפיפות לפי המתכון (§5.1 דרג 2),
  משקל ליחידה, אחוז מים, הערה, וסימוני קמח/נוזל לנוסחת האופה ולהידרציה
- **תשואה ותמחור** מאחורי disclosure (§3) — יחידות, משקל ליחידה, תשואה
  שנמדדה, יעד פוד קוסט
- **שלבים** — טקסט, טמפרטורה, דקות, עם סידור
- **אחסון והערות** — חיי מדף, אחסון, ציוד, הערות
- **התצוגה החיה** — סעיף 4 להלן

### כפתור "מתכון חדש"

שני מקומות: כפתור ירוק בכותרת המחברת, ומצב ריק אמיתי. קודם המחברת הריקה
אמרה "המחברת ריקה." וזה הכול — נכון, וללא שום עזרה. עכשיו יש הסבר מה יישמר
כאן ו-CTA.

### מחיקה עם אישור (דרישה 7)

דיאלוג שמזכיר את **שם** המתכון ואומר **מה נמחק איתו**: רכיבים, שלבים, תקלות,
יומן הניסיונות, האצוות וההיסטוריה. `window.confirm` לא יכול לעשות אף אחד
מהשניים, ושורות הבן הן `ON DELETE CASCADE` — כלומר זה המקום היחיד שבו
המשתמש לומד את זה בכלל.

### שכפול (דרישה 8)

החלק המחשבתי כאן הוא מה שעותק **לא** מקבל. `features/recipe/duplicate.ts`
מתעד שדה-שדה:

| לא עובר | למה |
|---|---|
| `locked` | §9 — "נוסחה מאושרת לייצור" הוא אישור של נוסחה אחת. עותק לא אושר, ו-§18.7 חוסם עריכה כשהדגל דולק — כלומר ירושה הייתה מייצרת עותק שאי אפשר לערוך |
| `batches` | §13a — **רשומות HACCP**. העתקה שלהן מייצרת רשומות בטיחות מזון שלא התרחשו. זה הדבר הרציני ביותר בקובץ |
| `trials` | §13 — יומן ניסיונות הוא רשומה של מה שנאפה בפועל |
| `versions`, `versionOf`, `versionNote` | §9 — היסטוריה שייכת למתכון שהיא קרתה לו |
| `privateNotes` | §8 — הערה על אפייה אחת אינה על אחרת |
| `savedFrom`, `createdAt`, `id` | מקור קבוצה / המסד מחזיק אותם |

**כן עובר:** הנוסחה — רכיבים, שלבים, תשואה, יעד פוד קוסט, טקסטים, תגים,
קטגוריה, ו-`issues` (ידע על הנוסחה, לא רשומת ייצור).

השם מתקדם ולא נערם: `לחם (עותק)` → `לחם (עותק 2)`, לא `לחם (עותק) (עותק)`.
המקור לא נוגע — כל המערכים משוכפלים לעומק ויש טסט שמוודא שעריכת העותק לא
מגיעה חזרה למקור.

### כיול אישי (דרישה 12)

זה המסך הכי שימושי בשלב. המנוע היה שלם; מה שחסר היה דרך להגיע אליו — וזה
היה חסר קריטי, כי **כיול אישי הוא הדרך הכנה היחידה לצאת מחישוב חלקי**: 5
שורות צפיפות ב-`pending-verification` ו-7 ב-`pending-form` לא מחזיקות ערך
בכוונה.

- נגיש מדף המתכון (דרך גיליון ההמרה) ומתוך העורך, על שורה שלא ניתן לשקול
- **מציג את נפח הכלי שהוא עומד להקפיא** — כך ש-B5 גלוי ולא מרומז
- כיול קרוב מוצע אך **לא מוחל מעצמו** — זה B4. ההחלה דורשת לחיצה, והרשומה
  נוצרת תחת הזהות של הרכיב הנוכחי ולא מועתקת, כך שרשומה אחת לא משרתת שני
  רכיבים
- רשימת הכיולים בלשונית "עוד", עם הסרה. כיול שגוי גרוע מהיעדר כיול: הוא
  מקבל עדיפות על כל ערך בטבלה (§5.1 דרג 1) ולובש תג ירוק בזמן שהוא עושה זאת

---

## 2. `null ≠ 0` — איך זה נשמר מהמקלדת עד העמודה

**כל שדה מספרי ב-draft הוא מחרוזת.** זה נראה כמו רשלנות וזה ההפוך.

הסכימה מבחינה בין `NULL` ל-`0` בשש עמודות — `yield_actual`, `weight_before`,
`weight_after`, `water_pct`, `g_per_100`, `unit_weight` — ובכל אחת מהן `NULL`
פירושו "לפי החישוב / לפי הטבלה המשותפת" ו-`0` פירושו "מישהו מדד אפס"
(§1.1, §18.11).

`<input type="number">` שקשור ל-`number` **לא יכול לבטא את ההבדל**: שדה
שרוקנו מגיע כ-`0` או כ-`NaN`, ובשני המקרים ההבחנה נעלמה לפני שהמאפר ראה
אותה.

כמחרוזות: שדה ריק נשאר `''`, `toNullableNumber('')` מחזיר `null`, והעמודה
באמת `NULL`. המנוע משתף פעולה — השדות שלו מוגדרים `number | string`,
`num('')` הוא 0 ו-`numOrNull('')` הוא null — ולכן ה-draft נמסר ישר
ל-`compute()` בלי שלב המרה, וזו גם הסיבה שהתצוגה בעורך לא יכולה להתנתק ממה
שיישמר.

**המסך אומר את זה למשתמש**, ליד שדה התשואה: *"שדה ריק פירושו 'לפי החישוב'.
אפס פירושו שנמדדה תשואה של אפס — שני דברים שונים."* בלי המשפט הזה אף אחד לא
היה מנחש.

מאומת בשלוש רמות: unit (`draft.test.ts`), מסך (`RecipeEditScreen.test.tsx`),
ו**מול Postgres אמיתי** (סעיף 6).

---

## 3. RLS ובידוד (דרישה 13)

שלב 4 לא שינה SQL, אבל **הוסיף מסלול DELETE מהאפליקציה**, ולכן בדקתי מחדש
ולא הנחתי:

| בדיקה | תוצאה |
|---|---|
| א' מוחק את המתכון של ב' | **0 שורות** |
| א' מוחק את הרכיבים של ב' | **0 שורות** |
| המתכון של ב' עדיין קיים אחרי הניסיון | ✓ |
| הרכיב של ב' עדיין קיים | ✓ |
| מה שא' רואה, ללא פילטר | 1 (שלו בלבד) |
| policies ב-`public` | 14 |
| טבלאות עם RLS דולק | **13 מתוך 13** |

ברמת האפליקציה, ב-`RecipeFlow.test.tsx`: א' יוצר מתכון דרך ה-UI, ב' מתחבר
לאותו מסד ורואה "המחברת ריקה."; קישור ישיר ל-`/recipe/<id של א'>` מחזיר "לא
נמצא במחברת"; וגם `/recipe/<id>/edit` מסרב. המזהה אמיתי — RLS הוא מה שהופך
אותו ל-404.

וברמת ה-repository: `deleteRecipe` על מתכון של חשבון אחר הוא **no-op ולא
שגיאה**. דיווח על כשל היה מספר לקורא שהמזהה קיים, וזו עצמה דליפה.

הפרויקט נוקה לחלוטין אחרי הבדיקות: 0 users, 0 recipes, 0 ingredients,
0 steps, 0 profiles, 0 calibrations, 34 שורות צפיפות ללא שינוי.

---

## 4. full / partial / none בתוך העריכה (דרישה 11)

התצוגה החיה בעורך קוראת ל-`compute()` ול-`calcState()` — **אותן שתי קריאות
שדף המתכון עושה** — על ה-draft בדיוק כפי שהוא יישמר. לא עותק מפושט.

לכל שורה: המשקל המחושב ותג המקור, או — אם אין נתון אמין — המקף והסיבה
המלאה. למשל לקקאו: *"יש שני נתונים סותרים לקקאו (42 ו-45.8 גרם ל-100 מ"ל)
שטרם אומתו, ולכן לא נציג אף אחד מהם."*

ולשורה כזו מוצעות **שתי דרכים כנות לצאת, ואין שלישית**:
«לכייל אצלי במטבח» ו«לשקול בגרמים». שתיהן לא מספר מנוחש.

ברמת המתכון:

| מצב | מה מוצג |
|---|---|
| `full` | "חישוב מלא — לכל הרכיבים יש נתון אמין." |
| `partial` | "נתונים חלקיים — חסרים נתונים עבור X רכיבים…" + שמות הרכיבים + תג **"חלקי"** על כל מספר שהוא סכום |
| `none` | "לא ניתן לחשב" + **מקף במקום מספר** |

---

## 5. תקלה אמיתית שמצאתי בעצמי — ועוד שתיים

### ₪0 למתכון בלי מחירים

**איך נמצאה:** קראתי צילום מסך של העורך בטאבלט וראיתי "עלות כוללת ₪0".
לא טסט מצא את זה.

כל המשקלים היו ידועים, ולכן ציר המסה היה `full` **בצדק** — אבל אף אחד לא
הזין מחיר, ו-₪0 נקרא "המתכון הזה בחינם". זו **בדיוק התקלה שדרישה 8 נועדה
למנוע, ציר אחד הלאה.**

התיקון: `calcState()` מחזיק עכשיו ציר תמחור נפרד:

| `costLevel` | מה מוצג |
|---|---|
| `full` | המספרים כרגיל |
| `partial` | המספר + תג "חלקי" + "העלות מחושבת מ-N רכיבים שיש להם מחיר. ל-M אין מחיר, ולכן העלות נמוכה מהעלות בפועל" + שמות הרכיבים |
| `none` | **מקף** + "לא הוזנו מחירים לאף רכיב… אפס אינו התשובה — פשוט אין נתון." |

עם שלוש הבחנות עדינות שיש להן טסטים:
- **מחיר 0 מפורש הוא מחיר.** מים מהברז באמת בחינם. מה שאסור הוא לקרוא שדה
  *ריק* כאפס.
- **שורה שאי אפשר לשקול לא נחשבת "חסרת מחיר"** — המשקל הוא החסם, ודיווח על
  שניהם היה מדווח פעמיים על אותו פער.
- **שני הצירים בלתי תלויים.** מתכון עם משקלים מלאים ובלי מחירים מציג את כל
  מספרי המשקל כרגיל ורק את העלות כמקף.

### דליפת `navigator.onLine` בטסט משלב 3

הטסט של "אין רשת" קבע `navigator.onLine = false` ושחזר ב-`finally` לפי
`Object.getOwnPropertyDescriptor(navigator, 'onLine')` — שמחזיר **undefined**,
כי `onLine` הוא accessor על `Navigator.prototype` ולא own property של
המופע. כלומר **השחזור מעולם לא רץ**, וכל טסט שהוכרז אחריו היה רץ מול דפדפן
שחשב שאין לו רשת. זה לא התגלה בשלב 3 כי הטסט היה האחרון בקובץ; ברגע שהוספתי
טסטים אחריו, שלושה נכשלו עם "אין חיבור לאינטרנט" בלי סיבה נראית.

תוקן עם `whileOffline()` שמוחק את ה-own property, **ועם טסט לדליפה עצמה**.

### ה-`+` בשמות הנגישים

שני כפתורי ההוספה נקראו "+ הוספת רכיב" ו-"+ הוספת שלב", וקורא מסך היה
מקריא את הפלוס הדקורטיבי. עכשיו `aria-hidden` על הגליף ו-`aria-label` נקי.

---

## 6. האם הנתונים באמת ב-Supabase

**כן, ואימתתי את זה מול המסד החי.**

זו השאלה שלא הסתפקתי בה בשלב 3, כי כפיל in-memory לא יכול לתפוס
`numeric(7,2)` שמעגל בשקט, `CHECK` שהמאפר מפר, `jsonb` שלא מקבל את מה
שנמסר לו, או `text[]` שמגיע כמחרוזת.

`supabase/scripts/mapper-roundtrip.mts` מפצל את העבודה:

1. **`--emit`** מריץ את הצינור האמיתי — `draftToRecipe` → `recipeToRow` /
   `ingredientsToRows` / `stepsToRows` — וכותב את ה-payloads המדויקים + digest
   של ה-`Recipe` שהשורות אמורות לשחזר.
2. ה-INSERT וה-SELECT רצים **על הפרויקט האמיתי**.
3. **`--verify`** מריץ את השורות שחזרו דרך `bundleToRecipe` האמיתי ומשווה.

ה-draft בבדיקה נבנה כדי לדרוך על כל מה שמסוכן: שורה שקולה ושורה בנפח, שורה
עם מחיר ואחת בלי, `waterPct: 0` (אפס שנמדד) לצד `waterPct: ''` (לפי הטבלה),
`yieldActual` שלא נגעו בו, וטקסט עברי עם גרשיים — שהוא גם מפגע quoting ב-SQL.

**התוצאה:**

```
digest צפוי:  7c89252f9b3256238db37b00be431f361a7a2c9b1eff4c7ee6ea9caea38c4e78
digest בפועל: 7c89252f9b3256238db37b00be431f361a7a2c9b1eff4c7ee6ea9caea38c4e78

ok   an untouched measured yield came back ABSENT, not as 0
ok   a measured water percentage of 0 came back as 0
ok   an unset water percentage came back absent
ok   the tags array survived as an array
ok   the gershayim in the notes survived
ok   the ingredient order survived
```

זהה. `yield_actual` חזר `NULL`, `water_pct` של שמן הזית חזר `0`, ושל המלח
חזר חסר. הפיקסטורה נמחקה ואומת שלא נשארה שורה.

השורות שחזרו שמורות ב-`supabase/tests/mapper-roundtrip.rows.json`, כך
ש-`npm run roundtrip:verify` חוזר בלי גישה למסד.

---

## 7. בדיקת E2E למסלול שביקשת

המסלול שרשמת נבדק **מקצה לקצה בטסט אחד**, `apps/web/src/app/RecipeFlow.test.tsx`:

> כניסה → Onboarding → מחברת ריקה → מתכון חדש → מילוי רכיבים → שמירה →
> refresh → פתיחה מחדש → עריכה → שמירה → שכפול → מחיקה

כל מה שמתחת לרשת אמיתי: הראוטר, `AuthProvider`, `AuthGate`,
`AppDataProvider`, המסכים, מודל ה-draft, `mappers.ts` ו-`SupabaseRepository`.
רק ה-hop של HTTP הוא כפיל — וכפיל **שאוכף את ה-policies מהמיגרציות**.

**"refresh" הוא `unmount()` ואחריו `render()` חדש** — זה מה שרענון עושה
לאפליקציית React: כל קומפוננטה נהרסת, כל ה-state נעלם, והסשן משוחזר מהאחסון.
ערך ששרד את זה שרד מהסיבה הנכונה: הוא חזר מהמסד ולא מ-closure.

מה שהטסט מאמת בכל צעד, לא רק על המסך אלא **בשורות במסד**: `owner_id` נכון,
שמות הרכיבים והשלבים, `yield_actual` שנשאר `NULL`, אותו `id` בעריכה (ולא
מתכון שני), 450 כ**מספר** בעמודה (כי הצורה המחרוזתית חיה רק בטופס), לעותק
שורות רכיבים משלו, ו-`CASCADE` שלקח את הבן של העותק ורק אותו.

### ובדפדפן אמיתי — 22/22

`npm run e2e` מריץ Playwright מול ה-bundle **הבנוי**:

```
ok  the built app boots and runs the onboarding
ok  the demo data is labelled as demo, not as the account's own notebook
ok  the notebook header offers "new recipe"
ok  the editor route renders in the production bundle
ok  the live calculation reports a complete computation
ok  a cup of cocoa turns the calculation partial
ok  and the notice names the missing ingredient
ok  the running total is marked as partial -- 4 marker(s)
ok  the calibration sheet opens
ok  it states the tool volume it will freeze
ok  the calibration closes the gap: partial becomes full
ok  the save button is disabled with no server
ok  and the screen explains why rather than failing silently
ok  the calibration list shows it after a full page reload
ok  and reports the frozen tool volume
ok  no horizontal overflow on a phone -- 0px
ok  every control is at least 40px tall
ok  the frame widens on a tablet instead of staying phone-sized -- 760px
ok  the ingredient fields use the wider grid -- 3 cols
ok  no horizontal overflow on a tablet -- 0px
ok  no page errors anywhere in the run
ok  nothing tried to reach an external origin
```

הכיול עובר דרך IndexedDB אמיתי ושורד **רענון עמוד מלא** — לא unmount, רענון.

---

## 8. Responsive ונגישות (דרישות 14, 15)

### responsive

**היה פער אמיתי:** ה-frame היה קבוע על 402×874 גם בטאבלט, כלומר טאבלט קיבל
פריסת טלפון עם הרבה שטח ריק, והעורך נדחס לטור צר אחד.

מ-700px של viewport ה-frame גדל ל-760×1040. מתחת לזה חוויית הטלפון **לא
נגעה**. אותם tokens, אותן קומפוננטות, אפליקציה חד-טורית.

והדקות: הגריד בעורך מגיב ל-**frame** ולא ל-viewport, דרך container query
(`container-type: inline-size` על ה-frame). זה משנה: בדסקטופ ה-frame צר
מהחלון, ושאילתת viewport הייתה נותנת תשובה שגויה. יש גם fallback ל-`@media`
לדפדפן בלי container queries.

אומת בדפדפן: 760px, 3 עמודות בגריד הרכיבים, 0px overflow אופקי בשני הגדלים.

### נגישות

- **כל פקד פר-שורה נושא את שם השורה שלו**: "הסרת קמח לבן" ולא "הסרה",
  "כמות של קמח לבן", "יחידת המדידה של קמח לבן", "הורדת קמח לבן למטה". בלי
  זה משתמש קורא-מסך שמנווט לפי שם מקבל רשימת כפתורים זהים. לפני שיש שם
  לרכיב ה-fallback הוא מספר השורה ("הסרת רכיב 1")
- כפתורי ההזזה **מושבתים** בקצוות ולא מוסתרים, כך שהמיקוד לא קופץ
- דיאלוג המחיקה הוא `role="alertdialog"` עם `aria-label`
- הודעות: `role="alert"` לשגיאות, `role="status"` להודעות מצב,
  `aria-live="polite"` על מגירת השגיאות, ומיקוד עובר אליה בכשל שמירה
- אזורי מצב עם שם: "שלמות החישוב", "שלמות התמחור", "נפח הכלי שיישמר",
  "המשקל המחושב של X"
- אומת בדפדפן: **אף פקד לא מתחת ל-40px** (`--hit-compact`; שדות וכפתורים
  ראשיים ב-44px+ לפי §15)

---

## 9. תוצאות הבדיקות

| מה | תוצאה |
|---|---|
| `npm test` — engine | **180 / 180** (7 קבצים) — ללא שינוי משלב 1 |
| `npm test` — web | **283 / 283** (18 קבצים) — היו 174 |
| **סה"כ** | **463 / 463** |
| `npm run typecheck` | נקי, strict + `noUncheckedIndexedAccess` |
| `npm run build` | נקי |
| `npm run e2e` (דפדפן) | **22 / 22** |
| `npm run roundtrip:verify` (מול המסד החי) | **OK**, digest זהה |
| RLS — מחיקה חוצה-חשבון | **0 שורות**, 14 policies, RLS על 13/13 |
| `npm run seed:check` | 34 שורות, מעודכן |
| `npm run schema:check` | 13 טבלאות, 136 עמודות |
| הפרוטוטייפ המקורי | **0 שינויים** |
| `packages/engine/src` | **0 שינויים** |

הטסטים החדשים: `draft.test.ts` (26) · `duplicate.test.ts` (20) ·
`RecipeEditScreen.test.tsx` (26) · `CalibrateSheet.test.tsx` (13) ·
`RecipeFlow.test.tsx` (7) · תוספות ל-`completeness.test.ts` (+6),
`RecipeScreen.test.tsx` (+6), `supabaseRepository.test.ts` (+5)

---

## 10. מה לא הושלם או לא נבדק — במפורש

1. **ה-hop של הדפדפן ל-Supabase לא נבדק בדפדפן.** מדיניות ה-egress של
   הסביבה חוסמת `*.supabase.co` (`CONNECT tunnel failed, 403`). ה-E2E בדפדפן
   רץ על build שבו משתני הסביבה ריקים (מצב דמו), ולכן הוא מכסה את הרכיבים,
   הפריסה וה-bundle אבל לא את הבקשה עצמה. שני חצאים כן מכוסים: המסלול המלא
   מול repository אמיתי ב-jsdom, והצינור מול Postgres אמיתי ב-round trip.
   **מה שנשאר לא מכוסה בפועל הוא PostgREST/GoTrue over HTTP.** בדיקה אמיתית
   דורשת סביבה בלי החסימה או הרצה מהמכונה שלך.
2. **שדות של §1.1 שלא נכנסו לטופס:** `pan` (גאומטריה), מצב בצק
   (`doughMode`/`ddt`/`flourTemp`/`roomTemp`/`friction`), `manualAllergens`,
   `weightBefore`/`weightAfter`, `freezing`/`thawing`, ו-`issues`. הם קיימים
   בסכימה, במאפר ובמנוע, ונשמרים אם הם על המתכון — פשוט אין להם שדה בטופס.
   בחרתי בליבה כדי לא להפוך את המסך לבלתי שמיש.
3. **`privateNotes` (§8)** — טבלה, RLS וטסטי בידוד קיימים; אין UI. לא נגעתי.
4. **`recipe_versions` (§9)** — טבלה, RLS וקריאה במאפר; **אין יצירת גרסה ואין
   restore**. העורך שומר "על המקום" ולא יוצר snapshot. זה פער אמיתי: §9 מצפה
   שעריכה תדחוף את המצב הקודם להיסטוריה. לא מומש.
5. **`trials`, `batches` (§13, §13a)** — טבלאות, RLS וקריאה; אין UI.
6. **`ingredient_catalog`** — טבלה ו-RLS; לא בשימוש. עדכון מחיר מרוכז לא מומש.
7. **תת-מתכונים** — המנוע מחשב אותם, המאפר קורא `sub_recipe_id`, ובעורך יש
   `subId` ב-draft — **אבל אין בורר תת-מתכון בממשק**. אי אפשר לקשר מתכון בסיס
   מהמסך.
8. **`locked` לא ניתן לשינוי מהמסך.** העורך מזהיר כשעורכים נוסחה מאושרת
   ומציע לשכפל, אבל אין מסלול לאשר או לבטל אישור.
9. **`window.confirm` ליציאה עם שינויים שלא נשמרו** — עובד, אבל לא עקבי עם
   דיאלוג המחיקה שבניתי בתוך האפליקציה. שווה לאחד.
10. **Storage, AI proxy, Cook Mode, קבוצות, קורסים, שיתוף** — לא התחלתי.
11. **תבניות האימייל ו-redirect URL** בפרויקט — ברירות מחדל של Supabase.
12. **5 ערכי הצפיפות בהמתנה ו-7 לפי צורה** — עדיין ללא ערך, כפי שאישרת.
    לא המצאתי כלום. הכיול האישי הוא מה שנותן למשתמש דרך לעקוף אותם.

---

## 11. החלטות וחריגות

1. **שדות מספריים כמחרוזות ב-draft** — סעיף 2. ההחלטה המשמעותית בשלב.
2. **`deleteRecipe` נוסף ל-`Repository`.** דרישה 7 מחייבת מחיקה, הסכימה
   תומכת (`ON DELETE CASCADE`), ו-§1.1 מתיר. `LocalDemoRepository` **מסרב**
   ולא מדמה — "מחיקה" שנעלמת מהמסך וחוזרת ברענון היא סוג השקר שהשכבה הזאת
   נועדה למנוע (B8).
3. **`saveRecipe`/`deleteRecipe` ב-context **זורקים** ולא בולעים לבאנר**,
   בשונה מ-`setPrefs`. הקורא הוא טופס שצריך לדעת אם לנווט, וטופס שמנווט
   ממתכון שלא נשמר מאבד את עבודת המשתמש.
4. **שכפול הוא פונקציה טהורה באפליקציה, לא במנוע.** זו מדיניות מוצר (מה עותק
   יורש) ולא חישוב. המנוע נשאר חישוב בלבד.
5. **הטופס נטען פעם אחת.** זריעה מחדש מ-`recipes` בכל שינוי הייתה מוחקת את
   מה שהמשתמש מקליד ברגע שמשהו אחר מרענן את הרשימה.
6. **ולידציה קצרה בכוונה.** המסד אוכף את הכלל הקשה (שם לא ריק) והמנוע מטפל
   במספרים חסרים בכנות. ולידטור ארוך היה בעיקר ממציא דרישות שלמוצר אין.
   **מתכון בתהליך הוא דבר לגיטימי לשמור** — שורה עם שם ובלי כמות ("קורט
   מלח") נשמרת.
7. **שורה ריקה לגמרי נזרקת בשמירה**, שורה עם שם או כמות נשמרת.
8. **`--frame-w-wide: 760px` ו-`--frame-h-wide: 1040px` הם tokens חדשים
   שאינם ב-§16.** §16 מגדיר את ה-frame של הטלפון שהפרוטוטייפ צויר בו ואינו
   אומר דבר על טאבלט, ולכן זו **הוספה ולא שינוי** של ערך מוגדר. מתועד
   ב-`tokens.css`. אף צבע, טיפוגרפיה, רדיוס או ריווח לא שונו — טסטי §16
   ממשיכים לעבור כפי שהם.
9. **container query ולא media query** בעורך — סעיף 8.
10. **ציר תמחור נפרד** — סעיף 5. חריגה מהותית היחידה מ"רק מה שביקשת", והיא
    נובעת ישירות מהרוח של דרישה 8 ושל דרישה 10.
11. **Playwright אינו dependency של הפרויקט.** `npm run e2e` משתמש בהתקנה
    שקיימת בסביבה (`PW=`/`CHROME=` לעקיפה). הוספת הורדת דפדפן של ~300MB
    ל-`npm ci` בשביל smoke test אחד היא עסקה גרועה.
12. **`dist-demo/` ו-`.e2e-shots/` ב-`.gitignore`.** תוצרי בנייה וצילומים.
13. **לא יצרתי Pull Request** ולא נגעתי ב-Presenzo Production. WhatsApp AI
    Assistant נשאר `INACTIVE` — לא הופעל, לא נמחק, לא שונה.

---

## 12. איך לבדוק בעצמך

```bash
npm ci
npm test                 # 463
npm run typecheck
npm run build
npm run e2e              # 22 בדיקות בדפדפן + צילומים ב-.e2e-shots/
npm run roundtrip:verify # השוואה מול השורות שחזרו מהמסד
npm run seed:check
npm run schema:check
```

להרצה מול הפרויקט החי נדרש `apps/web/.env.local` (לא בריפו):

```
VITE_SUPABASE_URL=https://qxdpsomelzpvphkhkqrw.supabase.co
VITE_SUPABASE_ANON_KEY=<המפתח ה-publishable מהדשבורד>
```

ואז `npm run dev`. בלי הקובץ האפליקציה רצה על מתכוני הדמו לקריאה בלבד
ואומרת זאת על המסך.
