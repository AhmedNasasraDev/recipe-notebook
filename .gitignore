node_modules/
dist/
# The packed demo: a built artifact, rebuilt by `npm run demo`.
dist-demo/

# Local Supabase configuration. Never committed - see apps/web/.env.example.
.env.local
.env.*.local
.e2e-shots/
# Built Figma scripts: regenerated by `npm run figma:build`.
figma/dist/

# The walkthrough take: a 15MB WebM and its caption panels are build output,
# not source. The finished MP4 and its chapter list are committed.
.film/

# Viewing exports (MHTML screen documentation): built output, not source.
exports/
