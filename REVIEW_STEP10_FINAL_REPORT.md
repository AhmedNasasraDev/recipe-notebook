# דוח שלב 10 — Final Production Readiness Review

פרויקט Supabase: `qxdpsomelzpvphkhkqrw` (Recipe Notebook, eu-central-1), Postgres 17.6
מיגרציות חדשות בשלב הזה: `0020`, `0021` — **שתיהן הוחלו בפועל**
ענף: `claude/recipe-notebook-initial-review-fwfb2e`
Pull Request: **לא נפתח**, לפי ההוראה.

`git status design_handoff_recipe_notebook/` — **ריק**. הפרוטוטייפ לא נגע.
`git status packages/engine/src/` — **ריק**. המנוע לא נגע בשלב הזה.

---

## המסקנה, במשפט אחד

המערכת **עברה את כל מה שניתן לבדוק בסביבה הזאת**, ארבעה defects אמיתיים נמצאו ותוקנו,
ו-**אין כאן הכרזה על Production Ready** — כי מסלול HTTP אמיתי מהדפדפן דרך GoTrue ו-PostgREST
אל Postgres **עדיין לא נבדק אף פעם**, ולא בגלל בחירה: מדיניות היציאה של הסביבה חוסמת
`*.supabase.co`. זה נשאר BLOCKER מתועד. ראו §BLOCKER.

---

## תמונת המצב במספרים

| מה | כמה | תוצאה |
|---|---|---|
| טסטים אוטומטיים (Vitest, engine + web) | 743 ב-36 קבצים | **הכול עובר** |
| חבילות SQL מול Postgres אמיתי | 6 חבילות, 189 בדיקות | **הכול עובר** |
| `rls-isolation.sql` | 42 | עובר |
| `delete-guard.sql` | 17 | עובר |
| `sub-recipe-hardening.sql` | 15 | עובר |
| `pricing.sql` | 27 | עובר |
| `costing.sql` | 43 | עובר |
| `planning.sql` (כולל 13 בדיקות חדשות ל-§7) | 45 | עובר |
| Round-trip של המאפר | 12, digest `fa702274…` | עובר |
| Round-trip של גרסאות | 37, digest `ca20a39b…` | עובר |
| Browser E2E (Chromium, על ה-bundle הבנוי) | 62 | עובר |
| בדיקת UX בשלושה מסכים (טלפון/טאבלט/דסקטופ) | 138 | 135 עוברות, 3 = false positive אחד ×3 (מוסבר ב-§10) |
| עץ הנגישות האמיתי של Chromium, 9 מסכים × 3 רזולוציות | 54 | **54 עוברות** |
| `typecheck`, `build`, `schema:check`, `seed:check` | — | עוברים |
| Supabase Security Advisors | — | **0 ממצאים** |
| Supabase Performance Advisors | 3 סוגים | INFO/WARN בלבד — ראו §LIMITATION |

---

# FIXED — defects אמיתיים שנמצאו בשלב הזה ותוקנו

## F1. תוכנית ייצור נעולה לא הייתה נעולה בפועל (§7) — החמור ביותר

**מה התגלה.** שלב 9 הגדיר את הכלל ותיעד אותו: "LOCKED → קוראים את ה-snapshot.
NOT LOCKED → מחשבים חי". הכלל נאכף **בדיוק במקום אחד** — `if v_locked then raise`
בתוך `save_production_plan`. כל השאר הושאר ל-RLS, ו-RLS שואל רק *של מי* השורה.
הרצתי probe חי כמשתמש מחובר עם המפתח הציבורי בלבד. שבעה ניסיונות, שש הצלחות:

| ניסיון | לפני 0021 | אחרי 0021 |
|---|---|---|
| עריכה דרך ה-RPC | נדחה | נדחה |
| `UPDATE` ישיר של שם תוכנית נעולה | **שורה אחת שונתה** | נדחה `42501` |
| `UPDATE` ישיר של ה-snapshot הקפוא | **שורה אחת שונתה** | נדחה `42501` |
| `INSERT` ישיר של שורת מוצר לתוכנית נעולה | **שורה אחת נוספה** | נדחה `42501` |
| `UPDATE` ישיר של שורות תוכנית נעולה | **שתי שורות שונו** | נדחה `42501` |
| `DELETE` ישיר של המלאי של תוכנית נעולה | **שורה אחת נמחקה** | נדחה `42501` |
| ביטול נעילה במעקף ה-RPC | **שורה אחת שונתה** | נדחה `42501` |
| יצירת תוכנית שנולדת נעולה עם snapshot מפוברק | לא נבדק אז | נדחה `42501` |

כלומר: רשומת ייצור היסטורית — כולל העלויות שהוקפאו בה — הייתה ניתנת לשכתוב בבקשת
PATCH אחת ל-PostgREST מהדפדפן עצמו. זה אותו סוג חור שסגרנו בשלב 6 (מחיקה שנשמרה
רק ב-UI → נשמרת במסד) ובשלבים 5 ו-9 (FK → טריגר השוואת בעלות): **כלל שקיים
במסלול קוד אחד אינו כלל.**

**מה תוקן — `0021_locked_plan_guard.sql`.** טריגר `BEFORE` על שלוש הטבלאות:

* `production_plans` — אסור לשנות `locked`, `locked_at` או `snapshot` אלא דרך
  מעבר הנעילה, ואסור לשנות **שום דבר** בשורה כל עוד היא נעולה. גם `owner_id`
  לא זז, עם דגל או בלעדיו. תוכנית לא נולדת נעולה.
* `production_plan_items`, `production_plan_stock` — אין `INSERT`, `UPDATE` או
  `DELETE` כשהתוכנית־האם נעולה.
* מעבר הנעילה עצמו הוא לגיטימי ויש לו מחבר אחד — `set_plan_locked` — שמכריז על
  עצמו בדגל טרנזקציוני (`set_config(..., true)`) שמורם לפני ה-`UPDATE` ומורד
  מיד אחריו, כדי שלא יישאר עומד לשאר הטרנזקציה.

**שני דברים שחייבו להמשיך לעבוד, ועובדים:**

1. **מחיקת תוכנית נעולה** מותרת — לשמור רשומה זו בחירה של המשתמש, לא של הסכימה.
2. **מחיקת מתכון** ממשיכה להתפשט (cascade) אל שורות של תוכנית נעולה, כפי ש-0018
   בחר במפורש ("הרשומה של מה שיוצר שורדת את מחיקת המתכון, כי התוכן שמור ב-snapshot").
   Postgres מוחק את שורת האב **קודם** ומפעיל את ה-cascade אחריה, ולכן "האב כבר לא
   כאן" הוא החתימה המדויקת של cascade, והשומר דוחה רק מחיקה ששני האבות שלה עוד קיימים.

**תקלה בתוך התיקון, שנמצאה ותוקנה.** הגרסה הראשונה של שומר־הבנים בדקה
`old.recipe_id` בתוך שרשרת `and` אחת. plpgsql מעביר את כל תנאי ה-`IF` כביטוי SQL
אחד, ולכן `old.recipe_id` נפתר גם על הטבלה שאין בה עמודה כזאת: מחיקה של שורת
**מלאי** בתוכנית נעולה נדחתה עם `42703` (עמודה לא קיימת) במקום `42501`. נדחה —
ומהסיבה הלא נכונה, שזאת הדרך שבה שומר מפסיק בשקט לשמור על המקרה שנכתב עבורו.
התנאי הופרד לקינון, וה-SQLSTATE הצפוי כתוב מפורשות בטסט (`planning.sql`, בדיקה 55).

**איך זה מוגן מעכשיו.** 13 בדיקות חדשות ב-`supabase/tests/planning.sql` (ord 50–62),
כולן מול Postgres אמיתי בתור `authenticated` עם JWT — כולל "אחרי כל זה הרשומה לא
נגעה" (שם, נעילה, סכום קפוא ומספר שורות), "ה-RPC עוד מבטל נעילה ומנקה את ההקפאה",
"תוכנית לא נעולה חוזרת להיות ניתנת לעריכה", ושני מסלולי ה-cascade.

## F2. יחידת המדידה נמחקה בעריכת מתכון קיים (§2, §11)

**מה התגלה.** בקריאת טופס העריכה **הבנוי** בדפדפן אמיתי: כל תפריט יחידת המדידה
הציג "גרם", בכל שורה, בכל רזולוציה. הסיבה — ערכי ה-`<option>` הם המזהים
הקנוניים של המנוע (`g`, `ml`, `unit`, `tbsp`…), והשורות השמורות מחזיקות את השמות
בעברית (`גרם`, `מ"ל`, `יח'`). אף אפשרות לא התאימה, הדפדפן נפל לאפשרות הראשונה —
שהיא גרם — ולכן שורה שנמדדה בגרמים **נראתה נכונה** וכל שורה אחרת לא.

**זה לא היה קוסמטי.** פתיחת הבריוש ולחיצה על שמירה הייתה כותבת 5 יח' ביצים
כ-5 גרם, ו-60 מ"ל חלב כ-60 גרם: הכמות נשמרת, היחידה מוחלפת, בשקט. טופס שאינו
מצליח להציג את מה ששמור אסור לו לשמור עליו.

**מה תוקן.** `draftFromRecipe` מנרמל עם `unitId()` — המנרמל של המנוע עצמו
(`units.ts`, `LEGACY_UNIT_NAMES`), בלי טבלת מיפוי שנייה. יחידה שהמנוע לא מכיר
נשמרת כפי שהיא ולא הופכת לגרם: התפריט לא יכול להציג אותה, אבל גם אף אחד לא
ממציא לה ערך.

**תוצאת לוואי שנסגרה יחד.** אחרי הנרמול, השמירה הראשונה של מתכון "עברי" כותבת
`g` במקום `גרם`. `versionDiff` השווה מחרוזות גולמיות, ולכן היה מדווח "יחידה
שונתה" בכל שורה — דיווח על שינוי שלא היה. עכשיו ההשוואה נעשית על היחידה
הקנונית, וטבלת ההשוואה מציגה את התווית העברית בשני הצדדים.

**איך זה מוגן מעכשיו.** ארבע בדיקות ב-`draft.test.ts`, כולל אחת שמוכיחה שזה
**איות מחדש ולא המרה**: `compute()` מחזיר בדיוק את אותם גרמים לפני ואחרי.

## F3. כרטיס במחברת הראה עלות אחרת מדף המתכון (§4, §12)

`NotebookScreen` הריץ `compute()` על המתכונים הגולמיים, בלי לפתור את הקטלוג —
כך שמתכון שמתומחר במרכז חומרי הגלם (המקרה הרגיל מאז שלב 7) הראה **בלי עלות**
על הכרטיס ועלות מלאה בדף שלו. אותו מספר, שני מסכים, שתי תשובות. בנוסף החישוב
רץ **בתוך לולאת הרנדר**: 120 מתכונים של 40 שורות = 24.4 מ"ש לכל רנדר, כלומר
הקלדת מילת חיפוש בת עשר אותיות שילמה ~244 מ"ש. תוקן ל-`useMemo` אחד שמפתחו הוא
המחברת, הקטלוג וההעדפות. שתי בדיקות רגרסיה + שומר ביצועים.

## F4. נגישות ו-ניגודיות (§10)

* **שני לחצנים, שם אחד.** בשני קבוצות הכפתורים בדף המתכון היה "כמו במתכון" —
  אחד במשמעות "בלי שינוי כמויות" ואחד במשמעות "הצג את היחידות כמו שנכתבו".
  קורא מסך שמכריז על הקבוצה מבדיל ביניהם; משתמש בפיקוד קולי שאומר "כמו במתכון"
  מקבל שני יעדים. נוסף `aria-label` לשני הכפתורים המתנגשים בלבד, וה-**שם הנגיש
  מכיל את הטקסט הנראה** כפי ש-WCAG 2.5.3 דורש. נמצא מול עץ הנגישות האמיתי של
  Chromium; אחרי התיקון: 54/54 בלי אף התנגשות ובלי אף רכיב בלי שם.
* **ארבעה כשלי ניגודיות** תוקנו (2.02:1 → 7.93:1 ואחרים), ונוסף **שומר סטטי
  קבוע** ב-`tokens.test.ts` שמחשב יחס ניגודיות לכל כלל CSS שקובע רקע וצבע ונכשל
  מתחת ל-4.5:1. השומר הזה עצמו תפס את `.ingIndex`, שהנחתי שהוא בסדר.
* **`/plans` נפל דרך `tabOf()`** אל לשונית "מחברת" ולכן הלשונית הלא נכונה
  הודגשה. תוקן עם בדיקת רגרסיה.

## F5. הרשאות שלא היו צריכות להיות שם (§8) — `0020`

`REVOKE` של `TRUNCATE`, `REFERENCES` ו-`TRIGGER` מ-`anon` ומ-`authenticated`, ושל
`INSERT/UPDATE/DELETE` מ-`anon`, כולל `ALTER DEFAULT PRIVILEGES` כדי שטבלה עתידית
לא תיוולד עם ההרשאות האלה. **RLS לא חל על TRUNCATE** — זה היה החור. אחרי:
`anon` = `SELECT` בלבד ב-17 הטבלאות, `authenticated` = `SELECT/INSERT/UPDATE/DELETE`,
**אפס** הרשאות TRUNCATE.

---

# PASS — נבדק והוכח

## §1 מסלול משתמש חדש, מקצה לקצה
הרשמה → התחברות → onboarding → מחברת ריקה → יצירה → שמירה → רענון → פתיחה
מחדש → עריכה → גרסאות → שחזור → השוואה. נבדק ב-`AppSession.test.tsx` מול
ה-AuthProvider, ה-AppDataProvider, ה-repository והמסכים האמיתיים — כולל
**remount**, שהוא מה שרענון או מכשיר שני באמת הם. **אין מסך מבוי סתום**: כל
מצב ריק (מחברת, חומרי גלם, תוכניות, היסטוריית גרסאות) אומר למה הוא ריק ומה
הפעולה הבאה, ויש לצידו פקד יצירה. כתובת לא קיימת מפנה למחברת ולא למסך לבן.

## §2 מתכון מקצועי
רכיבים, כמויות ויחידות, תפוקה, תפוקה תיאורטית מול נמדדת, שלבים וזמנים,
`Step.kind`, טמפרטורות, אלרגנים, כיולים, תת־מתכונים רב־שכבתיים, רכיבים כפולים
חוקיים, ו-`null ≠ 0` — כולם עוברים round-trip מול Postgres אמיתי
(12 + 37 בדיקות, שני digest-ים) ושורדים רענון והתחברות מחדש. הפגם שנמצא כאן
הוא F2, ותוקן.

## §3 חומרי גלם ורכש
חומר → רכישה → מספר אריזות → כמות באריזה → מחיר סה"כ → ספק → ניצולת → מחיר
בסיס מחושב → היסטוריית מחירים → השפעה על מתכונים. 27 בדיקות ב-`pricing.sql`
ו-43 ב-`costing.sql`. `g/kg`, `ml/l`, יחידות. **אין המרת מסה↔נפח בלי מקור
צפיפות אמין** — שרשרת הצפיפות של §5.1 (מתכון → כיול אישי → טבלה מקצועית) לא
שונתה, ו-`g_per_100` של הקטלוג **לא** נוסף כדרג רביעי.

## §4 עלויות ורווחיות
כל האגפים, ובמיוחד: 0 אמיתי מול ערך חסר, תמחור חלקי, תת־מתכונים מקוננים,
ו-snapshot היסטורי. **אין מספר שנראה סופי כשאחד הקלטים אינו ידוע** — מוצג מקף,
והסיבה נכתבת במילים. `Margin` ו-`Markup` לא מתחלפים. מחיר לפי יעד מסומן
"מחיר מחושב לפי יעד" ולא "המחיר הנכון".

## §5 תכנון ייצור
כמה מוצרים, scaling במשקל וביחידות, פיצוץ תת־מתכונים, איגוד, מלאי קיים, כמויות
רכש, עיגול לאריזות, עודף צפוי, עלות רכש, ומצבי מלא/חלקי/ללא. אין כפל־ספירה, ואין
איגוד של שני חומרים שונים בגלל שם זהה — האיגוד לפי `ingredientKey`. 45 בדיקות.

## §6 ציר זמן
תכנון לאחור, עבודת לילה, חציית חצות, קירור, התפחה, עבודה אקטיבית, אפייה, משך
חסר (חוסם את השלב וכל מה שלפניו — ולא ממציא), ותלויות בתת־מתכונים עמוקים.
**אין resource optimization**, כי אין מודל ציוד או כוח אדם — ולא הוצג כזה.

## §8 RLS ואבטחה
17 טבלאות, **בכולן RLS מופעל** ולכולן מדיניות. משתמש A אינו יכול לקרוא, ליצור,
לשנות, למחוק, לקשר או להסיק את נתוני B — נבדק על מתכונים, רכיבים, היסטוריית
רכישות, מחירים, ספקים, גרסאות, תוכניות ייצור, snapshots ונתוני רווחיות, גם
דרך RPC, גם דרך FK ידני, וגם בתור `anon`. `anon` רואה **אפס שורות בכל טבלה**,
כולל טבלת הצפיפות. 3 פונקציות `SECURITY DEFINER` בלבד, כולן שומרי בעלות או
טריגר משתמש חדש, וכולן **לא ניתנות לקריאה על ידי הלקוח**. **אפס** פונקציות בלי
`search_path` נעוץ. אין buckets ואין objects ב-storage. **Security Advisors: 0
ממצאים.** ה-bundle נסרק: אין JWT, אין `service_role` ואין מפתח סודי — המופעים
של המילים האלה הם **קוד ההגנה** שמסרב לבנות client ממפתח מנהל, עם הודעה בעברית.

## §10 UX
טלפון (402×874), טאבלט (820×1180) ודסקטופ (1440×900), תשעה מסכים:
**אפס גלישה אופקית** בעמוד ובתוך המסגרת, אפס טקסט חתוך, **כל פקד בעל שם נגיש**
ואין שני פקדים עם אותו שם (54/54 בעץ הנגישות האמיתי), Tab מגיע לפקדים בסדר
ולכל עצירה יש טבעת מיקוד נראית, ודיאלוג היציאה מטופס מלוכלך **נשאל** ודחייה
שלו משאירה את הטופס פתוח. **אפס tokens לא מוגדרים** ב-CSS (50 מוגדרים,
41 בשימוש), ו-**אין container query מת**: כל שש השאילתות פונות ל-`app`,
ש-`container-name: app` מוגדר עליו ב-`AppShell`, והמסגרת מגיעה ל-760px —
כלומר השאילתות אכן נדלקות. **לא הסתפקתי בכך שהטסט מצא element**: יש screenshots
של המסכים המרכזיים בשלוש הרזולוציות ועברתי עליהם.

## §11 טיפול בשגיאות
כשל רשת (נחסם לפני שנעשה ניסיון, עם הסבר), פג תוקף הסשן (חוזר למסך כניסה ולא
לאפליקציה שבורה), כשל שמירה, כשל RPC, קלט לא חוקי, שליחה כפולה (הכפתור מושבת
בזמן שמירה), נתונים חלקיים, חומר גלם חסר בקטלוג, תת־מתכון שנמחק (נחסם במסד),
גרסה מיושנת ועדכון מקבילי (`serialization_failure`, עם הודעה בעברית).
**אין הצגת הצלחה כשהפעולה לא נשמרה**: כשל שמירה משאיר את המשתמש בטופס, עם
הערכים שהקליד, עם `role="alert"` ממוקד — ובלי ניווט. שתי בדיקות חדשות.

## §12 ביצועים
נמדד, לא שוער. 120 מתכונים × 40 שורות עם תת־מתכונים בשלוש רמות, ותוכנית של
10 מוצרים שמפוצצת ומתומחרת — **46 מ"ש** בסך הכול, מול tripwire של 2000 מ"ש
(`scale.perf.test.ts`). ה-N+1 האמיתי היחיד שנמצא היה F3, ותוקן. גודל ה-bundle:
JS 623.7KB (177.9KB gzip), CSS 55.4KB (9.5KB gzip). **לא נעשתה שום אופטימיזציה
ספקולטיבית.**

## §13 רגרסיה
`typecheck`, `build`, `schema:check`, `seed:check`, 743 טסטים, שש חבילות SQL
חיות (189 בדיקות), שני ה-round-trip-ים ו-62 בדיקות דפדפן — **הכול עובר אחרי**
כל השינויים, כולל אחרי `0020` ו-`0021`. **התאמת מיגרציות:** ל-21 קבצי המיגרציה
בדיסק יש 21 רשומות בדיוק ב-`supabase_migrations`, וה-md5 של גוף שלוש הפונקציות
של `0021` בדיסק זהה לגוף שבמסד (`57097e67…`, `7e51b0e9…`, `786ab333…`).
**המסד נקי מכל fixture**: כל חבילה מסתיימת בבדיקת ניקיון שמחזירה 0, וכולן החזירו 0.

## §14 גבולות
**Presenzo Production (`fglnzrpuffswapjsnppg`) — לא נגע בשום צורה.**
**WhatsApp AI Assistant (`tbkcxrpenmpcwjicnxnd`) — נשאר INACTIVE; לא הופעל, לא נמחק, לא שונה.**
הפרוטוטייפ המקורי — byte-identical. `packages/engine/src` — לא שונה בשלב הזה.
Pull Request — לא נפתח.

---

# LIMITATION — מוגבלות מוכרת, לא תקלה

1. **Performance Advisors, 3 סוגים, כולם INFO/WARN.** (א) ארבעה FK בלי אינדקס
   מכסה (`private_notes.recipe_id`, `production_plan_items.recipe_id`,
   `recipe_versions.created_by`, `recipes.version_of`); (ב) שני אינדקסים שלא
   נעשה בהם שימוש עדיין (`recipes_owner_idx`, `recipes_group_idx`) — על מסד
   שכמעט אין בו נתונים זאת אמירה על גיל הפרויקט, לא על הסכימה; (ג) שתי מדיניות
   permissive על `ingredient_catalog` ל-SELECT — `_read` (השורות שלי **או**
   הטבלה המקצועית המשותפת) ו-`_write` (השורות שלי בלבד) — זה **תכנון מכוון**,
   כי הקריאה חייבת לכלול את הנתונים המשותפים והכתיבה לא. **לא נגעתי באף אחד
   מהשלושה**, כי §12 אומר לתקן רק בעיה שנמדדה, ואף אחת מהן לא נמדדה כבעיה.
   אם וכאשר יהיו נתונים אמיתיים בהיקף — אינדקס על
   `production_plan_items(recipe_id)` הוא המועמד הראשון, כי מחיקת מתכון סורקת
   את הטבלה הזאת.
2. **יחידות עבריות נשארות במסד עד השמירה הבאה.** אחרי F2 המנוע מקבל את שני
   האיותים דרך `unitId()`, והנרמול קורה כשמשתמש שומר מתכון. **לא כתבתי מיגרציית
   נתונים** שמשנה שורות קיימות — זה שינוי בנתוני משתמש שאף אחד לא ביקש,
   והמערכת נכונה בשני האיותים.
3. **`vitest` וה-doubles אינם Postgres.** ה-double של Supabase מיישם את ה-RPC-ים,
   לא את הטריגרים. שומר 0021 נבדק **רק** מול Postgres אמיתי — וזה מספיק, כי
   הלקוח לא כותב ישירות לטבלאות התוכניות בכלל: `supabaseRepository` קורא אותן
   ב-`select` וכותב רק דרך `save_production_plan`, `set_plan_locked` ו-
   `delete_production_plan`. **לא הוספתי** את השומר ל-double, כדי לא לתחזק שתי
   גרסאות של אותו כלל.
4. **בדיקת ה-UX שלי דיווחה 3 false positives** — checkbox אחד ("מתכון בסיס")
   שהשם שלו נובע מ-`<label>` עוטף. אלגוריתם השמות הפשוט שכתבתי לא מטפל בזה;
   **עץ הנגישות של Chromium כן**, והוא מחזיר לו שם מלא. הבדיקה הקובעת היא
   54/54 בעץ האמיתי, ולא 135/138 בניחוש שלי.
5. **הדיאלוגים הם `window.confirm` מקורי.** נבדק שהוא נשאל ושדחייה משאירה את
   הטופס — אבל הוא לא dialog של האפליקציה, ולכן אין לו עיצוב, מיקוד או RTL
   משלנו. זה מצב מוכר, ולא שונה בשלב הזה.

---

# BLOCKER — מונע הכרזה על Production Ready

## B1. HTTP E2E אמיתי לא בוצע אף פעם

**מה חסר.** דפדפן → GoTrue (הרשמה/התחברות אמיתית) → PostgREST/RPC → PostgreSQL
→ וחזרה ל-UI. **אף בדיקה בפרויקט הזה לא עברה במסלול הזה.**

**למה.** מדיניות היציאה של הסביבה חוסמת `*.supabase.co`. נבדק שוב היום:

```
$ curl https://qxdpsomelzpvphkhkqrw.supabase.co/rest/v1/
curl: (56) CONNECT tunnel failed, response 403

proxy status → { "kind": "connect_rejected",
                 "detail": "gateway answered 403 to CONNECT (policy denial or upstream failure)",
                 "host": "qxdpsomelzpvphkhkqrw.supabase.co:443" }
```

**לא ניסיתי לעקוף את החסימה, ואני לא טוען שהבדיקה עברה.**

**מה כן נבדק במקום, ומה זה מכסה.** צד ה-Postgres נבדק במלואו — 189 בדיקות
חיות בתור `authenticated` עם `request.jwt.claims` ובתור `anon`, שזה בדיוק ההקשר
שה-PostgREST מייצר לבקשה מחוברת. ה-UI נבדק על ה-bundle הבנוי בדפדפן אמיתי.
**מה שלא נבדק הוא התפר ביניהם**: כתובת ה-API, מפתח ה-anon בסביבת ייצור, הרשמה
ואימות מייל אמיתיים, רענון טוקן, שגיאות PostgREST אמיתיות, CORS, ו-`Bearer`
שמגיע מ-GoTrue ולא מ-`set_config`. **`demo mode` אינו הוכחה למסלול הזה**, ולא
הוצג כך באף מקום.

## B2. הצ'קליסט להרצת HTTP E2E בסביבה שמאפשרת גישה

להריץ ממכונה שיש לה יציאה ל-`*.supabase.co`, בסדר הזה. כל שלב הוא "עבר/נכשל",
ואין דילוגים.

**הכנה**
1. `.env.local` ב-`apps/web/`: `VITE_SUPABASE_URL=https://qxdpsomelzpvphkhkqrw.supabase.co`
   ו-`VITE_SUPABASE_ANON_KEY=<publishable/anon>`. **לא** `service_role` ולא
   `sb_secret_` — הקוד מסרב להם, וזאת ההתנהגות הנכונה.
2. `npm ci && npm run build && npm run preview` (או `npm run dev`).
3. לוודא ב-DevTools → Network שהבקשה הראשונה יוצאת ל-`*.supabase.co` ומקבלת 200.

**אימות (GoTrue)**
4. הרשמה עם כתובת חדשה. אם הפרויקט דורש אימות מייל — המסך חייב לומר שהחשבון
   עוד לא שמיש, ולא להעמיד פנים שהמשתמש בפנים.
5. התחברות. לרענן את העמוד — לחזור למחברת **בלי** לעבור במסך הכניסה.
6. התנתקות, ואז לנווט ל-`/notebook` בכתובת ישירה — חייב להגיע מסך כניסה.
7. להשהות את הסשן (למחוק את `sb-*-auth-token` מ-localStorage) ולבצע פעולה —
   חייב לחזור למסך כניסה, לא מסך שבור.

**המסלול המלא (PostgREST + RPC)**
8. onboarding עד הסוף → `profiles.onboarding_done = true` במסד.
9. מחברת ריקה, עם הסבר ו-CTA.
10. יצירת מתכון מקצועי: תפוקה ביחידות ובמשקל, שלבים עם `kind` וטמפרטורות,
    רכיב כפול חוקי, תת־מתכון, שדה מספרי אחד שנשאר **ריק** ואחד שהוקלד **0**.
    לשמור, לרענן, לפתוח מחדש — ולוודא ששדה ריק חזר `null` ו-`0` חזר `0`.
11. **לפתוח מתכון קיים לעריכה ולבדוק שכל תפריט יחידה מציג את היחידה השמורה**
    (זה F2 — הבדיקה הזאת היא הבדיקה שמצאה אותו).
12. לערוך, לשמור, ולראות שנוצרה גרסה עם הערת diff נכונה; לשחזר; להשוות שתי גרסאות.
13. מרכז חומרי גלם: להזין רכישה כפי שנעשתה, לראות מחיר בסיס מחושב, לעדכן מחיר,
    ולראות היסטוריה עם אחוז שינוי — **ולוודא שגרסה שנשמרה קודם לא זזה.**
14. תוכנית ייצור: כמה מוצרים, מלאי קיים (כולל 0 מפורש ושדה ריק), רשימת רכש,
    עיגול לאריזות. לסמן "בוצעה", לאמת שהמסך עובר ל-snapshot, ו-**לנסות
    `PATCH /production_plans?id=eq.<id>` עם `curl` והמפתח הציבורי — חייב 403/42501.**
15. לבטל את הסימון ולראות שההקפאה נמחקה ושהחישוב חי חזר.
16. **בידוד:** שני חשבונות בשני דפדפנים. B מנסה `GET`/`PATCH`/`DELETE` על ה-id-ים
    של A דרך `curl` ל-PostgREST — **כל הבקשות חייבות לחזור ריקות או 403.**
17. כשל רשת אמיתי (DevTools → Offline) בזמן שמירה: הודעה, בלי ניווט, בלי
    "נשמר" מדומה.
18. שני טאבים על אותו מתכון: לשמור באחד ואז בשני — השני חייב לקבל
    "המתכון שונה במקום אחר מאז שנטען".
19. בסוף: למחוק את כל ה-fixtures, ולאמת ב-SQL שהמסד נקי.

**התוצאה הקבילה היחידה:** 19 השלבים עוברים על סביבה עם גישה. עד אז — B1 עומד.

---

# NOT TESTED — לא נבדק, ואני לא טוען אחרת

1. **כל מה שב-B1**: GoTrue אמיתי, PostgREST אמיתי, רענון טוקן, CORS, אימות מייל.
2. **דפדפנים שאינם Chromium.** כל בדיקות הדפדפן רצו על Chromium 1194. Safari
   ו-Firefox — לא נבדקו. (רלוונטי במיוחד ל-`@container` ול-`100dvh`.)
3. **מכשיר אמיתי.** הרזולוציות נבדקו כ-viewport, לא על טלפון פיזי: מקלדת
   וירטואלית, גלילת מומנטום, safe-area ו-zoom של iOS — לא נבדקו.
4. **קורא מסך אמיתי.** נבדק עץ הנגישות של Chromium, שהוא המקור לשמות ולתפקידים —
   אבל VoiceOver ו-NVDA עצמם לא הופעלו.
5. **עומס אמיתי.** 120 מתכונים נמדדו **בצד הלקוח**. אלף מתכונים, רשת איטית,
   ותוכניות עם מאות שורות מול Postgres אמיתי — לא נמדדו.
6. **גיבוי ושחזור, ניטור, ומדיניות סיסמאות/הגבלת קצב של הפרויקט** — מחוץ ל-scope
   של השלב הזה, ולא נבדקו.

---

## מה השתנה בקוד ובמסד בשלב הזה

**מיגרציות (שתיהן הוחלו):**
* `0020_least_privilege_grants.sql` — צמצום הרשאות, כולל ברירות מחדש.
* `0021_locked_plan_guard.sql` — שלושה טריגרים ושתי פונקציות שומר, ו-
  `set_plan_locked` שמכריז על מעבר הנעילה.

**קוד:** `draft.ts` (F2), `versionDiff.ts` (F2), `NotebookScreen.tsx` (F3),
`RecipeScreen.tsx` (F4), `TabBar.tsx` (F4), וחמישה קבצי CSS (ניגודיות).

**טסטים חדשים:** 13 בדיקות ב-`planning.sql`; `scale.perf.test.ts`; שומר
הניגודיות ב-`tokens.test.ts`; ובדיקות רגרסיה ב-`draft.test.ts`,
`NotebookScreen.test.tsx`, `RecipeScreen.test.tsx`, `RecipeEditScreen.test.tsx`,
`AppSession.test.tsx`, `TabBar.test.tsx`.

**סטטוס:** הליבה עקבית, נבדקת ונשמרת. **Production Ready לא נכתב כאן** ולא
ייכתב עד שהצ'קליסט של B2 יעבור בפועל.
